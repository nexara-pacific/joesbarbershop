---
phase: 01-scaffold
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - site/package.json
  - site/tsconfig.json
  - site/astro.config.mjs
  - site/src/pages/index.astro
autonomous: true
requirements:
  - SCAF-01
must_haves:
  truths:
    - "An Astro project exists at site/ with TypeScript strict mode enforced"
    - "npm run build inside site/ exits 0 with no TypeScript errors"
    - "tsconfig.json extends astro/tsconfigs/strict (the v5+ default preset)"
  artifacts:
    - path: "site/package.json"
      provides: "Astro 6 project root with build/dev/preview scripts"
      contains: '"astro":'
    - path: "site/tsconfig.json"
      provides: "TypeScript strict mode preset extension"
      contains: 'astro/tsconfigs/strict'
    - path: "site/astro.config.mjs"
      provides: "Astro config entrypoint (integrations added in Plan 02)"
      contains: "defineConfig"
    - path: "site/src/pages/index.astro"
      provides: "Default minimal-template index page (replaced by Plan 03)"
  key_links:
    - from: "site/package.json scripts.build"
      to: "astro build"
      via: "npm script"
      pattern: '"build":\s*"astro build"'
---

<objective>
Initialize a new Astro 6 project in the `site/` subdirectory with TypeScript strict mode enforced. This is the foundational scaffold every later phase builds on.

Purpose: Create the npm-managed `site/` package that Plans 02 and 03 will extend with integrations and a base layout. TypeScript strict mode (the Astro v5+ default) catches type errors at build time, which becomes load-bearing once Phase 2 adds content collections.

Output: A `site/` directory containing `package.json`, `tsconfig.json`, `astro.config.mjs`, and the minimal-template starter page. `npm run build` succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-scaffold/01-RESEARCH.md
@.planning/phases/01-scaffold/01-VALIDATION.md
</context>

<interfaces>
<!-- Astro 6 minimal-template project shape after `npm create astro@latest`. -->
<!-- Executor uses these defaults verbatim — do NOT hand-author tsconfig.json. -->

Expected default `site/tsconfig.json` (Astro v5+):
```json
{
  "extends": "astro/tsconfigs/strict",
  "include": [".astro/types.d.ts", "**/*"],
  "exclude": ["dist"]
}
```

Expected default `site/astro.config.mjs` (Astro 6 minimal):
```javascript
import { defineConfig } from 'astro/config';

export default defineConfig({});
```

Expected default `site/package.json` scripts:
```json
{
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  }
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Initialize Astro 6 project in site/ with TypeScript strict default</name>
  <files>site/package.json, site/tsconfig.json, site/astro.config.mjs, site/src/pages/index.astro</files>
  <read_first>
    - .planning/phases/01-scaffold/01-RESEARCH.md (sections "Standard Stack", "Architecture Patterns", "Common Pitfalls" — note: Astro v5+ ships TS strict as DEFAULT; no `--typescript` flag exists in v6; do NOT hand-author tsconfig.json)
    - CLAUDE.md (project constraints — site code lives in site/ subdirectory; repo root stays npm-free; no Tailwind ever)
  </read_first>
  <action>
    Run from the repository root (`/Users/darrelltang/dtconsulting/joesbarbershop`):

    1. Verify the `site/` directory does not yet exist:
       ```bash
       test ! -d site || (echo "ERROR: site/ already exists; aborting" && exit 1)
       ```

    2. Initialize Astro 6 with the minimal template (this command is from the research; use it verbatim — Astro CLI accepts `<dir>` as a positional arg):
       ```bash
       npm create astro@latest site -- --template minimal --yes --no-git --skip-houston --install
       ```
       This creates `site/`, installs dependencies, and writes `package.json`, `tsconfig.json`, `astro.config.mjs`, `src/pages/index.astro`, and the default `.gitignore`.

    3. Verify the default `tsconfig.json` extends `astro/tsconfigs/strict` (it should — this is the v5+ default). If for any reason the generated tsconfig does NOT extend strict, edit it to extend `astro/tsconfigs/strict`. Do NOT hand-author it from scratch.

    4. Verify `site/package.json` declares `astro` at version `^6` (not 5.x or earlier). The install picks up the latest Astro 6 release (research verified 6.2.2 is current).

    5. Verify the install succeeded by running a clean build:
       ```bash
       cd site && npm run build
       ```
       Build must exit 0 and produce `site/dist/index.html`.

    Do NOT add any integrations in this task — Vercel adapter, Sitemap, and Image config are Plan 02's job. Do NOT create `src/layouts/`, `src/components/`, or extra pages — that's Plan 03's job. Keep this task to the literal output of `npm create astro@latest`.
  </action>
  <verify>
    <automated>test -f site/package.json && test -f site/tsconfig.json && test -f site/astro.config.mjs && grep -q '"astro":' site/package.json && grep -q 'astro/tsconfigs/strict' site/tsconfig.json && cd site && npm run build &gt; /dev/null 2&gt;&amp;1 &amp;&amp; test -f dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - `site/package.json` exists and contains `"astro":` (version `^6.x`)
    - `site/tsconfig.json` exists and contains the literal string `astro/tsconfigs/strict`
    - `site/astro.config.mjs` exists and contains `defineConfig`
    - `site/src/pages/index.astro` exists (default minimal-template page)
    - `cd site && npm run build` exits 0
    - `site/dist/index.html` exists after build
    - `site/node_modules/` exists (deps installed)
    - The repo root (`/Users/darrelltang/dtconsulting/joesbarbershop`) does NOT contain a `package.json` — npm-management stays inside `site/`
  </acceptance_criteria>
  <done>
    Astro 6 project lives in `site/`. TypeScript strict is the active preset. `npm run build` is green. SCAF-01 satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Untrusted package code executes during install (postinstall scripts) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | npm package supply chain (`astro`, transitive deps) | accept | npm 11.x verifies registry signatures by default; Astro is a well-maintained official package; no `--ignore-scripts` flag needed for trusted core deps. Risk is informational at this stage. |
| T-01-01-02 | Information Disclosure | `site/.gitignore` correctness (must exclude `node_modules/`, `dist/`, `.env`, `.vercel/`) | mitigate | The Astro template's default `.gitignore` covers `node_modules/`, `dist/`, and `.env`. After init, verify these entries exist; add `.vercel/` if missing (it's added by `vercel link` in Plan 04). |
</threat_model>

<verification>
After Task 1 completes:
- `cd site && npm run build` exits 0 with TypeScript strict mode active
- `cat site/tsconfig.json` shows `"extends": "astro/tsconfigs/strict"`
- `cat site/package.json` shows astro `^6.x` in dependencies
</verification>

<success_criteria>
- SCAF-01 satisfied: Astro project initialized in `site/` with TypeScript strict mode
- The repo is ready for Plan 02 (integrations) and Plan 03 (base layout)
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md` capturing:
- Astro version installed (verify with `cat site/package.json | grep '"astro":'`)
- Whether `tsconfig.json` extends `astro/tsconfigs/strict` (expected: yes, v5+ default)
- Build duration for the empty project (baseline for later phases)
- Any deviation from the research (e.g., if Astro CLI changed its flag set since research date 2026-05-06)
</output>
