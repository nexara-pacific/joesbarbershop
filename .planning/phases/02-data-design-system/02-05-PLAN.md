---
phase: 02-data-design-system
plan: 05
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - site/src/components/Hero.astro
  - site/src/components/FactStrip.astro
  - site/src/components/PriceBoard.astro
autonomous: true
requirements: [DESN-02]

must_haves:
  truths:
    - "Hero renders a two-column section with h1, BLUF paragraph, CTA buttons, meta grid, and the interior photo using <Picture> with AVIF/WebP formats"
    - "Hero photo uses loading=eager and fetchpriority=high (LCP optimization for Phase 5)"
    - "FactStrip renders 5 fact tiles with business.ratings.google.value and business.ratings.google.count from business.ts"
    - "PriceBoard renders the letter-board with prices from business.ts (business.prices.haircut etc.) and the SectionMark component"
    - "No data-checker, data-font, or tweaks remnants in any of these components"
    - "Honors CONTEXT.md decisions D-03: 12 named components LOCKED (Hero, FactStrip, PriceBoard in this plan); D-06: per-component scoped <style>; D-10: live-tweaks panel three-layer strip applies to component CSS; D-14: <Picture formats={['avif','webp']}/> for hero photo with loading=eager + fetchpriority=high, <Image/> for everything else"
    - "npm run build exits 0 after this plan"
  artifacts:
    - path: "site/src/components/Hero.astro"
      provides: "Two-column hero with Picture component for interior photo"
      contains: "hero-grid"
    - path: "site/src/components/FactStrip.astro"
      provides: "5-column fact strip with ratings from business.ts"
      contains: "fact-strip"
    - path: "site/src/components/PriceBoard.astro"
      provides: "Letter-board price section with prices from business.ts"
      contains: "price-board"
  key_links:
    - from: "site/src/components/Hero.astro"
      to: "site/src/assets/photos/03-interior-hero.jpg"
      via: "import heroPhoto from '../assets/photos/03-interior-hero.jpg'"
      pattern: "import heroPhoto"
    - from: "site/src/components/PriceBoard.astro"
      to: "site/src/data/business.ts"
      via: "import { business } from '../data/business'"
      pattern: "import.*business.*from"
---

<objective>
Create the Hero, FactStrip, and PriceBoard body-section components, each ported from the OD-5 mockup with scoped CSS and real data from business.ts.

Purpose: These three components form the primary above-the-fold and pricing content. Hero is the LCP element and must use Picture with fetchpriority=high. PriceBoard drives conversions and pulls prices from business.ts.

Output: Hero.astro, FactStrip.astro, PriceBoard.astro — all with full OD-5 markup, scoped styles, no tweaks remnants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-design-system/02-CONTEXT.md
@.planning/phases/02-data-design-system/02-PATTERNS.md

<interfaces>
<!-- Hero photo uses Picture (not Image) per D-14: -->
```astro
---
import { Picture } from 'astro:assets';
import heroPhoto from '../assets/photos/03-interior-hero.jpg';
import { business } from '../data/business';
---
<Picture
  src={heroPhoto}
  formats={['avif', 'webp']}
  alt="Inside Joe's Barbershop..."
  loading="eager"
  fetchpriority="high"
/>
```
<!-- fetchpriority is a plain HTML attribute, not an Astro prop — pass it directly -->

<!-- Business data fields used in these components: -->
<!-- Hero: business.address.street, business.address.suite, business.address.city, business.address.state, business.address.zip, business.ratings.google.value, business.ratings.google.count -->
<!-- FactStrip: business.ratings.google.value, business.ratings.google.count, business.ratings.yelp.value -->
<!-- PriceBoard: business.prices.haircut, business.prices.shave, business.prices.beardLineUp, business.prices.cleanUp, business.prices.haircutBeard -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero.astro and FactStrip.astro</name>
  <files>site/src/components/Hero.astro, site/src/components/FactStrip.astro</files>
  <read_first>
    - .planning/phases/02-data-design-system/02-PATTERNS.md "Hero.astro" section (complete markup + CSS)
    - .planning/phases/02-data-design-system/02-PATTERNS.md "FactStrip.astro" section (complete markup + CSS)
    - mockups/home-v5/index.html lines 329–382 (Hero markup) and lines 108–124 (Hero CSS + responsive at 239–240)
    - mockups/home-v5/index.html lines 384–412 (FactStrip markup) and lines 125–134 (FactStrip CSS + responsive at 241–243)
  </read_first>
  <action>
**Hero.astro** — two-column section. Strip ALL `[data-font]` and `[data-checker]` CSS variants. Port `[data-checker="standard"]` `.hero-checker-ribbon` as the unconditional base rule (drop selector wrapper, keep the CSS value). `fetchpriority` is a plain HTML attribute on `<Picture>` — NOT an Astro prop.

```astro
---
import { Picture } from 'astro:assets';
import heroPhoto from '../assets/photos/03-interior-hero.jpg';
import { business } from '../data/business';
---
<section class="hero" aria-label="Joe's Barbershop in Bostonia">
  <div class="hero-grid">
    <div class="hero-copy">
      <span class="eyebrow kicker-rule">Traditional barbering · since 2020</span>
      <h1 class="display">
        A real cut.<br />
        A real <span class="accent-word">shave.</span><br />
        Walk on in.
      </h1>
      <p class="hero-bluf">The only classic barbershop in Bostonia — three chairs, two cutters, zero pretense. Walk-ins always welcome. Cash only, ATM on site. Open Tuesday through Saturday starting at 10am.</p>
      <div class="hero-ctas">
        <a class="btn" href="https://joe-104613.square.site/" target="_blank" rel="noopener">Book a chair</a>
        <a class="btn outline" href="#visit">Get directions</a>
      </div>
      <div class="hero-meta">
        <div><strong>Hours</strong> Tue–Fri 10am–7:30pm · Sat 10am–close</div>
        <div><strong>Address</strong> {business.address.street}, {business.address.suite} / {business.address.city}, {business.address.state} {business.address.zip}</div>
        <div><strong>Payment</strong> Cash · ATM on site</div>
        <div><strong>Rating</strong> {business.ratings.google.value} ★ · {business.ratings.google.count} Google reviews</div>
      </div>
    </div>
    <div class="hero-photo">
      <Picture
        src={heroPhoto}
        formats={['avif', 'webp']}
        alt="Inside Joe's Barbershop — black and white checkerboard tile floor stretching across the shop, three barber chairs along the right wall, mix of vintage black-leather barber chair and modern silver/black quilted chairs, mirrors and counters under bright lighting"
        loading="eager"
        fetchpriority="high"
      />
      <span class="photo-credit">Bostonia · interior of Joe's, three stations.</span>
      <div class="hero-checker-ribbon" aria-hidden="true"></div>
    </div>
  </div>
</section>
<style>
  .hero { position: relative; overflow: hidden; border-bottom: 1px solid var(--border); }
  .hero-grid { display: grid; grid-template-columns: 1.05fr 1fr; gap: 0; align-items: stretch; }
  .hero-copy { padding: clamp(40px, 6vw, 88px) clamp(24px, 4vw, 64px); display: flex; flex-direction: column; justify-content: center; background: var(--bg); position: relative; }
  .hero-copy .eyebrow { margin-bottom: 22px; }
  .hero-copy h1 { font-family: var(--font-display); font-size: clamp(48px, 6.6vw, 96px); font-weight: 600; line-height: 0.96; letter-spacing: -0.012em; margin: 0 0 22px; color: var(--fg); }
  .hero-copy h1 .accent-word { color: var(--accent); }
  .hero-bluf { font-size: 18px; line-height: 1.55; color: var(--fg); max-width: 56ch; margin: 0 0 28px; }
  .hero-bluf strong { font-weight: 600; }
  .hero-ctas { display: flex; flex-wrap: wrap; gap: 14px; margin-bottom: 28px; }
  .hero-meta { border-top: 1px solid var(--border); padding-top: 20px; display: grid; grid-template-columns: 1fr 1fr; gap: 16px 28px; font-size: 14px; color: var(--muted); }
  .hero-meta strong { display: block; font-family: var(--font-board); text-transform: uppercase; letter-spacing: 0.14em; font-size: 11px; color: var(--fg); font-weight: 600; margin-bottom: 4px; }
  .hero-photo { position: relative; background: #0e0d0b; min-height: 520px; }
  .hero-photo img { width: 100%; height: 100%; object-fit: cover; object-position: center 60%; position: absolute; inset: 0; }
  .hero-photo::after { content: ""; position: absolute; inset: 0; background: linear-gradient(180deg, transparent 0%, transparent 60%, rgba(14,13,11,0.35) 100%); pointer-events: none; }
  .hero-photo .photo-credit { position: absolute; left: 16px; bottom: 16px; font-family: var(--font-board); text-transform: uppercase; letter-spacing: 0.18em; font-size: 11px; color: var(--bg); opacity: 0.78; z-index: 2; font-weight: 500; }
  .hero-checker-ribbon { position: absolute; inset: auto 0 0 auto; width: 220px; height: 28px; background-image: conic-gradient(from 90deg at 50% 50%, var(--check-fg) 25%, var(--check-bg) 0 50%, var(--check-fg) 0 75%, var(--check-bg) 0); background-size: 28px 28px; pointer-events: none; }
  @media (max-width: 980px) {
    .hero-grid { grid-template-columns: 1fr; }
    .hero-photo { min-height: 380px; }
  }
</style>
```

**FactStrip.astro** — 5-column grid of fact tiles. No photos needed.

```astro
---
import { business } from '../data/business';
---
<section class="fact-strip tight" aria-label="Quick facts about Joe's Barbershop">
  <div class="wrap">
    <div class="fact">
      <span class="label">Established</span>
      <span class="value">2020</span>
      <span class="sub">Five years on Bradley Ave.</span>
    </div>
    <div class="fact">
      <span class="label">Google rating</span>
      <span class="value"><span class="star">★</span> {business.ratings.google.value}</span>
      <span class="sub">{business.ratings.google.count} reviews · Yelp {business.ratings.yelp.value}</span>
    </div>
    <div class="fact">
      <span class="label">Neighborhood</span>
      <span class="value">Bostonia</span>
      <span class="sub">East County · El Cajon CA</span>
    </div>
    <div class="fact">
      <span class="label">Walk-ins</span>
      <span class="value">Always</span>
      <span class="sub">No appointment required.</span>
    </div>
    <div class="fact">
      <span class="label">Barbers</span>
      <span class="value">Joe + Alex</span>
      <span class="sub">Three chairs, two cutters.</span>
    </div>
  </div>
</section>
<style>
  .fact-strip { background: var(--surface); border-bottom: 1px solid var(--border); }
  .fact-strip .wrap { display: grid; grid-template-columns: repeat(5, 1fr); gap: 0; padding-block: 0; }
  .fact { padding: 22px 18px; border-right: 1px solid var(--border); display: flex; flex-direction: column; gap: 6px; }
  .fact:last-child { border-right: 0; }
  .fact .label { font-family: var(--font-board); text-transform: uppercase; letter-spacing: 0.16em; font-size: 10.5px; color: var(--muted); font-weight: 600; }
  .fact .value { font-family: var(--font-display); font-size: 22px; font-weight: 600; line-height: 1.1; }
  .fact .value .star { color: var(--accent); }
  .fact .sub { font-size: 13.5px; color: var(--muted); }
  @media (max-width: 980px) {
    .fact-strip .wrap { grid-template-columns: repeat(2, 1fr); }
    .fact { border-right: 0; border-top: 1px solid var(--border); }
    .fact:nth-child(odd) { border-right: 1px solid var(--border); }
  }
</style>
```
  </action>
  <verify>
    <automated>test -f /Users/darrelltang/dtconsulting/joesbarbershop/site/src/components/Hero.astro &amp;&amp; test -f /Users/darrelltang/dtconsulting/joesbarbershop/site/src/components/FactStrip.astro &amp;&amp; echo "both exist"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f site/src/components/Hero.astro` exits 0
    - `test -f site/src/components/FactStrip.astro` exits 0
    - `grep "import heroPhoto from" site/src/components/Hero.astro` returns 1 match (imported object)
    - `grep "fetchpriority" site/src/components/Hero.astro` returns 1 match
    - `grep "loading=\"eager\"" site/src/components/Hero.astro` returns 1 match
    - `grep "hero-checker-ribbon" site/src/components/Hero.astro` returns 1 match (standard checker as unconditional base)
    - `grep "data-checker\|data-font" site/src/components/Hero.astro` returns NO matches
    - `grep "business.ratings.google.value" site/src/components/FactStrip.astro` returns 1 match
    - `grep "data-checker\|data-font" site/src/components/FactStrip.astro` returns NO matches
  </acceptance_criteria>
  <done>Hero.astro and FactStrip.astro created with full OD-5 content, no tweaks remnants</done>
</task>

<task type="auto">
  <name>Task 2: Create PriceBoard.astro and run build verification</name>
  <files>site/src/components/PriceBoard.astro</files>
  <read_first>
    - .planning/phases/02-data-design-system/02-PATTERNS.md "PriceBoard.astro" section (complete markup + CSS)
    - mockups/home-v5/index.html lines 416–458 (PriceBoard markup) and lines 143–158 (CSS + responsive at 244, 252–253)
    - site/src/components/SectionMark.astro (already created in Plan 04 — just verify it exists before referencing it here)
  </read_first>
  <action>
**PriceBoard.astro** — letter-board price section. Uses `<span class="section-mark">` inline (per PATTERNS.md SectionMark note — acceptable to use `.section-mark` class directly without importing SectionMark component in this context, since section-mark is a utility class in utilities.css). Import business for prices. Strip ALL `[data-font]` variants.

```astro
---
import { business } from '../data/business';
---
<section class="price-section" id="prices" aria-label="Service prices">
  <div class="wrap">
    <div class="section-head">
      <span class="section-mark" aria-hidden="true"></span>
      <div class="section-head-text">
        <span class="eyebrow">The board · what it costs</span>
        <h2>Posted prices.<br />Same as the wall.</h2>
      </div>
    </div>
    <div class="price-grid">
      <div class="price-board" aria-label="Service prices — letter board">
        <div class="head">Joe's Barbershop</div>
        <ul>
          <li><span>Haircut</span><span class="dots"></span><span class="num">${business.prices.haircut}</span></li>
          <li><span>Shave</span><span class="dots"></span><span class="num">${business.prices.shave}</span></li>
          <li><span>Beard line-up</span><span class="dots"></span><span class="num">${business.prices.beardLineUp}</span></li>
          <li><span>Clean up</span><span class="dots"></span><span class="num">${business.prices.cleanUp}</span></li>
          <li><span>Haircut + beard</span><span class="dots"></span><span class="num">${business.prices.haircutBeard}</span></li>
        </ul>
        <div class="foot"><span class="red">Cash only</span> · ATM on site</div>
      </div>
      <div class="price-aside">
        <h3>What to expect<br />when you sit down.</h3>
        <p>Every service starts with a consultation. Joe or Alex will check in on what you're going for before picking up the clippers. Clean lines, honest work.</p>
        <p>No tip jars. No upsells. The board price is what you pay.</p>
        <blockquote class="quote">"Best $30 haircut in East County. Every time." — Google review</blockquote>
      </div>
    </div>
  </div>
</section>
<style>
  .price-section { background: var(--bg); }
  .price-grid { display: grid; grid-template-columns: 1.1fr 1fr; gap: clamp(28px, 4vw, 64px); align-items: start; }
  .price-board { background: var(--board-bg); color: var(--board-fg); padding: 36px 32px 40px; border: 8px solid #2a2418; box-shadow: inset 0 0 0 2px rgba(255,255,255,0.04), 0 24px 40px -24px rgba(0,0,0,0.35); background-image: repeating-linear-gradient(0deg, rgba(255,255,255,0.012) 0 1px, transparent 1px 6px), repeating-linear-gradient(90deg, rgba(255,255,255,0.012) 0 1px, transparent 1px 6px); font-family: var(--font-board); font-weight: 600; text-transform: uppercase; letter-spacing: 0.10em; }
  .price-board .head { text-align: center; border-bottom: 1px solid rgba(255,255,255,0.16); padding-bottom: 16px; margin-bottom: 16px; font-size: 22px; letter-spacing: 0.22em; }
  .price-board ul { list-style: none; margin: 0; padding: 0; }
  .price-board li { display: flex; align-items: baseline; justify-content: space-between; padding-block: 10px; font-size: 26px; letter-spacing: 0.14em; border-bottom: 1px dashed rgba(255,255,255,0.08); }
  .price-board li:last-child { border-bottom: 0; }
  .price-board li .dots { flex: 1; margin-inline: 14px; border-bottom: 2px dotted rgba(255,255,255,0.18); transform: translateY(-6px); }
  .price-board li .num { font-family: var(--font-board); font-weight: 600; color: var(--board-fg); }
  .price-board .foot { margin-top: 22px; padding-top: 18px; border-top: 1px solid rgba(255,255,255,0.16); text-align: center; font-size: 16px; letter-spacing: 0.28em; color: oklch(80% 0.04 80); }
  .price-board .foot .red { color: oklch(72% 0.18 28); font-weight: 700; }
  .price-aside h3 { font-family: var(--font-display); font-size: 32px; font-weight: 600; line-height: 1.1; margin: 0 0 14px; letter-spacing: -0.005em; }
  .price-aside p { margin: 0 0 14px; color: var(--fg); font-size: 17px; line-height: 1.6; }
  .price-aside .quote { border-left: 3px solid var(--accent); padding: 4px 0 4px 18px; margin: 24px 0 0; font-style: italic; color: var(--muted); font-size: 15.5px; }
  @media (max-width: 980px) { .price-grid { grid-template-columns: 1fr; } }
  @media (max-width: 600px) {
    .price-board li { font-size: 20px; }
    .price-board .head { font-size: 18px; letter-spacing: 0.18em; }
  }
</style>
```

After writing, run `cd site && npm run build` to confirm no TypeScript errors from the new components.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/dtconsulting/joesbarbershop/site &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f site/src/components/PriceBoard.astro` exits 0
    - `grep "business.prices.haircut" site/src/components/PriceBoard.astro` returns 1 match
    - `grep "business.prices.haircutBeard" site/src/components/PriceBoard.astro` returns 1 match
    - `grep "price-board" site/src/components/PriceBoard.astro` returns at least 2 matches (section + class)
    - `grep "data-checker\|data-font" site/src/components/PriceBoard.astro` returns NO matches
    - `cd site && npm run build` exits 0
  </acceptance_criteria>
  <done>PriceBoard.astro created with letter-board powered by business.ts prices; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static component file creation. No user input, no network calls, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | Spoofing | N/A | accept | N/A — no user identity or auth |
| T-02-26 | Tampering | N/A | accept | N/A — static component files, version-controlled |
| T-02-27 | Repudiation | N/A | accept | N/A — no transactions |
| T-02-28 | Information Disclosure | N/A | accept | N/A — only public business data (prices, ratings) |
| T-02-29 | Denial of Service | N/A | accept | N/A — static components, no rate-limiting surface |
| T-02-30 | Elevation of Privilege | N/A | accept | N/A — no auth, no roles |

All STRIDE categories are N/A — this plan creates static Astro component files with no user input, network calls, auth, or secrets.
</threat_model>

<verification>
- `test -f site/src/components/Hero.astro` exits 0
- `test -f site/src/components/FactStrip.astro` exits 0
- `test -f site/src/components/PriceBoard.astro` exits 0
- `grep "fetchpriority" site/src/components/Hero.astro` returns 1 match
- `grep "data-checker\|data-font" site/src/components/Hero.astro` returns no matches
- `cd site && npm run build` exits 0
</verification>

<success_criteria>
- Hero, FactStrip, PriceBoard all created with full OD-5 bodies
- Hero photo uses Picture with eager loading and fetchpriority=high
- PriceBoard prices come from business.ts (not hardcoded)
- No tweaks/data-font/data-checker remnants
- build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-design-system/02-05-SUMMARY.md`
</output>
