---
phase: 02-data-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - site/src/styles/tokens.css
  - site/src/styles/utilities.css
  - site/src/layouts/Base.astro
autonomous: true
requirements: [DESN-01]

must_haves:
  truths:
    - "tokens.css exists with :root custom properties, box-sizing reset, base html/body/img/a rules, and global section spacing"
    - "utilities.css exists with .wrap, .eyebrow, .kicker-rule, .display, .check-divider, .section-mark, .btn, .section-head shared atoms"
    - "Both CSS files imported in Base.astro frontmatter (not inside <style>), so all components can use design tokens without re-importing"
    - "Google Fonts <link> tags are in Base.astro <head> for IM Fell English, Playfair Display, DM Serif Display, Newsreader, Oswald, JetBrains Mono"
    - "No [data-font] or [data-checker] variant rules exist in either CSS file (tweaks dead code stripped)"
    - "npm run build exits 0 with no TypeScript errors after this plan"
  artifacts:
    - path: "site/src/styles/tokens.css"
      provides: "CSS custom properties (:root vars) + box-sizing reset + base rules"
      contains: "--bg:"
    - path: "site/src/styles/utilities.css"
      provides: "Shared utility classes for all components"
      contains: ".check-divider"
    - path: "site/src/layouts/Base.astro"
      provides: "Modified layout with CSS imports + Google Fonts <link>"
      contains: "tokens.css"
  key_links:
    - from: "site/src/layouts/Base.astro"
      to: "site/src/styles/tokens.css"
      via: "frontmatter import"
      pattern: "import.*tokens\\.css"
    - from: "site/src/layouts/Base.astro"
      to: "site/src/styles/utilities.css"
      via: "frontmatter import"
      pattern: "import.*utilities\\.css"
---

<objective>
Extract the OD-5 design token layer — :root CSS custom properties, base reset, shared utility classes — into two dedicated files, and wire them as global imports in Base.astro.

Purpose: Every component in Wave 2 and beyond can reference `var(--bg)`, `.wrap`, `.check-divider`, etc. without importing anything themselves. Tokens and utilities cascade globally via the Base.astro frontmatter import.

Output: `site/src/styles/tokens.css`, `site/src/styles/utilities.css`, and updated `site/src/layouts/Base.astro` with both imports + Google Fonts links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-design-system/02-CONTEXT.md
@.planning/phases/02-data-design-system/02-RESEARCH.md
@.planning/phases/02-data-design-system/02-PATTERNS.md

<interfaces>
<!-- From site/src/layouts/Base.astro (current state from 01-03-SUMMARY.md): -->
```astro
---
import UtilBar from '../components/UtilBar.astro';
import Masthead from '../components/Masthead.astro';
import Footer from '../components/Footer.astro';

interface Props {
  title: string;
  description?: string;
}

const { title, description } = Astro.props;
---
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    {description && <meta name="description" content={description} />}
    <slot name="head" />
  </head>
  <body>
    <UtilBar />
    <Masthead />
    <main><slot /></main>
    <Footer />
  </body>
</html>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tokens.css and utilities.css from OD-5 mockup</name>
  <files>site/src/styles/tokens.css, site/src/styles/utilities.css</files>
  <read_first>
    - mockups/home-v5/index.html lines 37–78 (the :root vars, reset, and shared atoms)
    - mockups/home-v5/index.html lines 104–107 (.btn rules)
    - mockups/home-v5/index.html lines 135–142 (section spacing + .section-head)
    - .planning/phases/02-data-design-system/02-PATTERNS.md "site/src/styles/tokens.css" section (complete content to port)
    - .planning/phases/02-data-design-system/02-PATTERNS.md "site/src/styles/utilities.css" section (complete content to port)
  </read_first>
  <action>
Create `site/src/styles/` directory if it does not exist, then create two files.

**tokens.css** — port from mockup lines 37–66 + 135–136. Strip ALL `[data-font]` and `[data-checker]` variant rules (dead code after tweaks removal). Strip lines 67–68, 74, 76, and any other `[data-font]`/`[data-checker]` selectors. The complete file:

```css
/* Design tokens: Joe's Barbershop OD-5 — ported from mockups/home-v5/index.html */
:root {
  --bg:        oklch(95% 0.022 80);
  --surface:   oklch(99% 0.008 80);
  --fg:        oklch(15% 0.02 60);
  --muted:     oklch(45% 0.025 50);
  --border:    oklch(86% 0.025 80);
  --hairline:  oklch(80% 0.030 75);
  --accent:    oklch(56% 0.17 28);
  --accent-2:  oklch(36% 0.10 260);
  --gold:      oklch(68% 0.10 80);
  --board-bg:  oklch(13% 0.005 60);
  --board-fg:  oklch(96% 0.005 80);
  --check-fg:  oklch(15% 0.005 60);
  --check-bg:  oklch(96% 0.005 80);
  --font-display-fell:     'IM Fell English', 'Iowan Old Style', Georgia, serif;
  --font-display-playfair: 'Playfair Display', 'Iowan Old Style', Georgia, serif;
  --font-display-dm:       'DM Serif Display', 'Tiempos Headline', Georgia, serif;
  --font-display:          var(--font-display-fell);
  --font-body:  'Newsreader', 'Iowan Old Style', Georgia, serif;
  --font-board: 'Oswald', 'Bebas Neue', 'Arial Narrow', sans-serif;
  --font-mono:  'JetBrains Mono', ui-monospace, Menlo, monospace;
  --maxw: 1240px;
  --gutter: clamp(20px, 4vw, 56px);
}

*, *::before, *::after { box-sizing: border-box; }
html, body { margin: 0; padding: 0; }
body { background: var(--bg); color: var(--fg); font-family: var(--font-body); font-size: 17px; line-height: 1.55; text-rendering: optimizeLegibility; -webkit-font-smoothing: antialiased; }
img { display: block; max-width: 100%; height: auto; }
a { color: inherit; }

section { padding-block: clamp(56px, 7vw, 96px); position: relative; }
section.tight { padding-block: clamp(40px, 5vw, 64px); }
```

**utilities.css** — port from mockup lines 66–79, 104–107, 137–142. Strip ALL `[data-checker]` and `[data-font]` variant selectors. Port the "standard" checker state as the unconditional base rule (already unconditional in mockup). The complete file:

```css
/* Shared utility classes: Joe's Barbershop OD-5 — ported from mockups/home-v5/index.html */
.display { font-family: var(--font-display); font-weight: 600; line-height: 1.02; letter-spacing: -0.005em; }

.eyebrow { font-family: var(--font-board); text-transform: uppercase; letter-spacing: 0.18em; font-size: 12px; font-weight: 600; color: var(--muted); }
.kicker-rule { display: inline-flex; align-items: center; gap: 12px; }
.kicker-rule::before { content: ""; width: 28px; height: 2px; background: var(--accent); }

.wrap { width: 100%; max-width: var(--maxw); margin: 0 auto; padding-inline: var(--gutter); }

.check-divider { height: 22px; width: 100%; background-image: conic-gradient(from 90deg at 50% 50%, var(--check-fg) 25%, var(--check-bg) 0 50%, var(--check-fg) 0 75%, var(--check-bg) 0); background-size: 22px 22px; }

.section-mark { width: 22px; height: 22px; border-radius: 999px; background: radial-gradient(circle at 50% 50%, var(--gold) 0 5px, transparent 6px), conic-gradient(from 0deg, var(--check-fg) 0 90deg, var(--check-bg) 0 180deg, var(--check-fg) 0 270deg, var(--check-bg) 0); border: 2px solid var(--check-fg); flex: 0 0 auto; }

.btn { display: inline-flex; align-items: center; gap: 10px; padding: 12px 20px; background: var(--fg); color: var(--bg); font-family: var(--font-board); text-transform: uppercase; letter-spacing: 0.16em; font-size: 12.5px; font-weight: 600; text-decoration: none; border-radius: 0; border: 1px solid var(--fg); transition: background .15s, color .15s; }
.btn:hover { background: var(--accent); border-color: var(--accent); }
.btn.outline { background: transparent; color: var(--fg); }
.btn.outline:hover { background: var(--fg); color: var(--bg); }

.section-head { display: flex; align-items: center; gap: 14px; margin-bottom: 28px; }
.section-head h2 { font-family: var(--font-display); font-size: clamp(34px, 4vw, 52px); font-weight: 600; line-height: 1.05; letter-spacing: -0.005em; margin: 0; }
.section-head .eyebrow { margin-bottom: 6px; }
.section-head-text { display: flex; flex-direction: column; }
```

Do NOT add `@import url(...)` for Google Fonts here — that goes in Base.astro `<head>` as `<link>` tags (serial round-trip penalty for `@import`).
  </action>
  <verify>
    <automated>test -f /Users/darrelltang/dtconsulting/joesbarbershop/site/src/styles/tokens.css &amp;&amp; echo "tokens.css exists" &amp;&amp; test -f /Users/darrelltang/dtconsulting/joesbarbershop/site/src/styles/utilities.css &amp;&amp; echo "utilities.css exists"</automated>
    <automated>grep -c '\-\-bg:' /Users/darrelltang/dtconsulting/joesbarbershop/site/src/styles/tokens.css</automated>
    <automated>grep -v '^#' /Users/darrelltang/dtconsulting/joesbarbershop/site/src/styles/tokens.css | grep -c 'data-font\|data-checker' || true</automated>
  </verify>
  <acceptance_criteria>
    - `test -f site/src/styles/tokens.css` exits 0
    - `test -f site/src/styles/utilities.css` exits 0
    - `grep -c '\-\-bg:' site/src/styles/tokens.css` returns 1 (the :root var is present)
    - `grep 'data-font\|data-checker' site/src/styles/tokens.css` returns no matches (exit 1 or empty)
    - `grep 'data-font\|data-checker' site/src/styles/utilities.css` returns no matches
    - `grep -c '\.check-divider' site/src/styles/utilities.css` returns at least 1
    - `grep -c '\.wrap' site/src/styles/utilities.css` returns at least 1
    - `grep -c '\.btn' site/src/styles/utilities.css` returns at least 1
  </acceptance_criteria>
  <done>tokens.css and utilities.css both exist with correct OD-5 content; no tweaks dead code present</done>
</task>

<task type="auto">
  <name>Task 2: Update Base.astro — add CSS imports and Google Fonts links</name>
  <files>site/src/layouts/Base.astro</files>
  <read_first>
    - site/src/layouts/Base.astro (current file — MUST read before editing)
    - .planning/phases/02-data-design-system/02-PATTERNS.md "site/src/layouts/Base.astro" section (Phase 2 diff)
    - .planning/phases/02-data-design-system/02-CONTEXT.md D-25 (section order locked) and D-07 (CSS import in frontmatter)
  </read_first>
  <action>
Read `site/src/layouts/Base.astro` first (it currently has 28 lines per 01-03-SUMMARY.md).

Add exactly these two lines to the frontmatter block (inside the `---` fences), immediately after the `import Footer` line and before the `interface Props` declaration:

```astro
import '../styles/tokens.css';
import '../styles/utilities.css';
```

Add exactly these three `<link>` tags in the `<head>` block, before the `<slot name="head" />` line:

```html
<link rel="preconnect" href="https://fonts.googleapis.com" />
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=IM+Fell+English:ital@0;1&family=Playfair+Display:wght@600;800;900&family=DM+Serif+Display:ital@0;1&family=Newsreader:opsz,wght@6..72,400;6..72,500;6..72,600&family=Oswald:wght@500;600;700&family=JetBrains+Mono:wght@500&display=swap" />
```

CRITICAL (per D-25): Do NOT change the section ordering in `<body>`. The order `<UtilBar />` → `<Masthead />` → `<main><slot /></main>` → `<Footer />` is locked.

CRITICAL (per D-07 and PATTERNS.md Pitfall 8): The two CSS imports (`tokens.css`, `utilities.css`) MUST be inside the `---` frontmatter block, NOT inside a `<style>` tag. Inside `<style>` would scope/hash-mangle the tokens, making them useless as globals.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/dtconsulting/joesbarbershop/site &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*tokens\.css" site/src/layouts/Base.astro` returns 1 match
    - `grep "import.*utilities\.css" site/src/layouts/Base.astro` returns 1 match
    - Both import lines appear INSIDE the `---` frontmatter block (between the two `---` fences)
    - `grep "fonts.googleapis.com" site/src/layouts/Base.astro` returns at least 1 match (Google Fonts link present)
    - `grep "IM.Fell.English" site/src/layouts/Base.astro` returns 1 match
    - `grep "UtilBar\|Masthead\|Footer" site/src/layouts/Base.astro` returns 3 matches (section order unchanged)
    - `cd site && npm run build` exits 0
  </acceptance_criteria>
  <done>Base.astro updated with CSS imports in frontmatter and Google Fonts links in head; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Phase 2 Plan 01 is static CSS file creation + layout wiring. No user input, no network calls, no auth, no database. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Spoofing | N/A | accept | N/A — no user identity or auth in this phase |
| T-02-02 | Tampering | N/A | accept | N/A — no mutable data; files are build artifacts |
| T-02-03 | Repudiation | N/A | accept | N/A — no transactions or audit-worthy events |
| T-02-04 | Information Disclosure | N/A | accept | N/A — no secrets; CSS tokens are public design values |
| T-02-05 | Denial of Service | N/A | accept | N/A — static file serving; no rate-limiting surface |
| T-02-06 | Elevation of Privilege | N/A | accept | N/A — no auth, no roles, no privilege levels |

All STRIDE categories are N/A — this plan creates static CSS files and modifies a layout template. No user input, network calls, authentication, or secrets involved at any layer.
</threat_model>

<verification>
After both tasks complete:
- `test -f site/src/styles/tokens.css` exits 0
- `test -f site/src/styles/utilities.css` exits 0
- `grep "import.*tokens\.css" site/src/layouts/Base.astro` returns 1 match in frontmatter
- `grep "import.*utilities\.css" site/src/layouts/Base.astro` returns 1 match in frontmatter
- `grep -r "data-font\|data-checker" site/src/styles/` returns no matches
- `cd site && npm run build` exits 0
</verification>

<success_criteria>
- tokens.css and utilities.css both exist with complete OD-5 content, no tweaks dead code
- Base.astro frontmatter imports both CSS files (not inside a style tag)
- Google Fonts link for all 6 typefaces is in Base.astro head
- `npm run build` passes without TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-design-system/02-01-SUMMARY.md`
</output>
