---
phase: 02-data-design-system
plan: 06
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - site/src/components/Heritage.astro
  - site/src/components/Visit.astro
  - site/src/components/FAQ.astro
  - site/src/components/ClosingCTA.astro
autonomous: true
requirements: [DESN-02, DESN-03]

must_haves:
  truths:
    - "Heritage renders a two-column section with heritage chair photo and 3-stat block; heritage-frame has no data-checker variant"
    - "Visit renders the storefront photo + NAP card with address/hours/phone from business.ts"
    - "FAQ renders 5 flat h3/p question pairs (no accordions, no JS tabs — AEO requirement)"
    - "ClosingCTA renders centered CTA with Book a chair button pointing to Square Site booking URL"
    - "All 4 components have no data-checker, data-font, or tweaks remnants"
    - "Honors CONTEXT.md decisions D-03: 12 named components LOCKED (Heritage, Visit, FAQ, ClosingCTA in this plan); D-06: per-component scoped <style>; D-09: cross-component CSS resolved via dead-code strip — heritage-frame [data-checker='heavy'] variant is dead after tweaks removal, omitted entirely (option 1 of D-09 ordering); D-10: live-tweaks panel three-layer strip applies to component CSS"
    - "npm run build exits 0 after this plan"
  artifacts:
    - path: "site/src/components/Heritage.astro"
      provides: "Heritage section with heritage chair Image (lazy-loaded)"
      contains: "heritage-grid"
    - path: "site/src/components/Visit.astro"
      provides: "Visit section with storefront Image + NAP card from business.ts"
      contains: "nap"
    - path: "site/src/components/FAQ.astro"
      provides: "FAQ section with flat h3/p pairs (no JS accordions)"
      contains: "faq-q"
    - path: "site/src/components/ClosingCTA.astro"
      provides: "Closing CTA section with booking button"
      contains: "closing-cta"
  key_links:
    - from: "site/src/components/Heritage.astro"
      to: "site/src/assets/photos/04-heritage-chair.jpg"
      via: "import heritagePhoto from '../assets/photos/04-heritage-chair.jpg'"
      pattern: "import heritagePhoto"
    - from: "site/src/components/Visit.astro"
      to: "site/src/data/business.ts"
      via: "import { business } from '../data/business'"
      pattern: "import.*business.*from"
---

<objective>
Create the remaining four body-section components — Heritage, Visit, FAQ, and ClosingCTA — each ported from the OD-5 mockup with scoped CSS and business.ts data where applicable.

Purpose: These components complete the component library. FAQ is explicitly flat h3/p pairs per AEO constraint (no accordions). Visit pulls real NAP data from business.ts.

Output: Heritage.astro, Visit.astro, FAQ.astro, ClosingCTA.astro — all with full OD-5 markup and no tweaks remnants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-design-system/02-CONTEXT.md
@.planning/phases/02-data-design-system/02-PATTERNS.md

<interfaces>
<!-- Heritage photo: lazy-loaded Image (not Picture — not the hero) -->
```astro
import { Image } from 'astro:assets';
import heritagePhoto from '../assets/photos/04-heritage-chair.jpg';
<Image src={heritagePhoto} alt="..." loading="lazy" />
```

<!-- Visit photo: lazy-loaded Image -->
```astro
import { Image } from 'astro:assets';
import storefront from '../assets/photos/02-storefront.jpg';
<Image src={storefront} alt="..." loading="lazy" />
```

<!-- Visit business data fields: -->
<!-- business.address.street, business.address.suite, business.address.city, business.address.state, business.address.zip, business.phone, business.hours -->

<!-- AEO constraint (inputs/02-aeo-constraints.md): FAQs MUST be flat <h3>/<p> pairs, NEVER <details>/<summary> or JS accordions -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Heritage.astro and Visit.astro</name>
  <files>site/src/components/Heritage.astro, site/src/components/Visit.astro</files>
  <read_first>
    - .planning/phases/02-data-design-system/02-PATTERNS.md "Heritage.astro" section (complete markup + CSS + heritage-frame cross-component note)
    - .planning/phases/02-data-design-system/02-PATTERNS.md "Visit.astro" section (complete markup + CSS)
    - mockups/home-v5/index.html lines 460–500 (Heritage markup) and lines 159–171 (Heritage CSS)
    - mockups/home-v5/index.html lines 504–557 (Visit markup) and lines 172–184 (Visit CSS)
    - .planning/phases/02-data-design-system/02-CONTEXT.md D-09 (cross-component CSS — heritage-frame variant is dead code after tweaks strip, omit)
  </read_first>
  <action>
**Heritage.astro** — two-column layout with heritage chair photo left, copy + 3-stat block right. The `.heritage-frame` `[data-checker="heavy"]` rule is dead code (tweaks stripped per D-10) — omit entirely. Use `<Image>` (not `<Picture>`) per D-14.

```astro
---
import { Image } from 'astro:assets';
import heritagePhoto from '../assets/photos/04-heritage-chair.jpg';
---
<section class="heritage" aria-label="Our heritage and history">
  <div class="wrap">
    <div class="heritage-grid">
      <div class="heritage-photo">
        <Image src={heritagePhoto} alt="Joe's Barbershop heritage barber chair — vintage black leather and mahogany frame, checkerboard tile floor, ATM and Joe's logo wall art visible" loading="lazy" />
      </div>
      <div class="heritage-copy">
        <span class="eyebrow kicker-rule">Opened 2020</span>
        <h2>Five years in Bostonia.<br />Built to last.</h2>
        <p>Joe Denesowicz opened the shop on Bradley Avenue in 2020. Three chairs, two cutters, one rule: do good work and charge a fair price.</p>
        <p>The checkerboard floor, the heritage chair, the letter-board prices on the wall — this is what a barbershop looks like when it's built for the neighborhood, not a brand deck.</p>
        <div class="heritage-stats">
          <div class="stat">
            <div class="num">4.9★</div>
            <div class="label">Google rating</div>
          </div>
          <div class="stat">
            <div class="num">90+</div>
            <div class="label">Google reviews</div>
          </div>
          <div class="stat">
            <div class="num">5yr</div>
            <div class="label">Serving Bostonia</div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
<style>
  .heritage { background: var(--surface); border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }
  .heritage-grid { display: grid; grid-template-columns: 1fr 1.05fr; gap: clamp(28px, 4vw, 72px); align-items: center; }
  .heritage-photo { position: relative; }
  .heritage-photo img { width: 100%; aspect-ratio: 4 / 5; object-fit: cover; display: block; }
  .heritage-copy h2 { font-family: var(--font-display); font-size: clamp(38px, 4.4vw, 60px); font-weight: 600; line-height: 1.02; letter-spacing: -0.006em; margin: 14px 0 22px; }
  .heritage-copy p { font-size: 17.5px; line-height: 1.65; margin: 0 0 14px; max-width: 60ch; }
  .heritage-stats { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; margin-top: 32px; border-top: 1px solid var(--border); padding-top: 24px; }
  .heritage-stats .stat .num { font-family: var(--font-display); font-size: 40px; font-weight: 600; line-height: 1; letter-spacing: -0.01em; color: var(--accent); }
  .heritage-stats .stat .label { font-family: var(--font-board); text-transform: uppercase; letter-spacing: 0.16em; font-size: 11px; color: var(--muted); font-weight: 600; margin-top: 6px; }
  @media (max-width: 980px) { .heritage-grid { grid-template-columns: 1fr; } }
  @media (max-width: 600px) {
    .heritage-stats { grid-template-columns: 1fr 1fr; }
    .heritage-stats .stat:last-child { grid-column: span 2; }
  }
</style>
```

**Visit.astro** — storefront photo + NAP dl/dt/dd card. Pulls address and phone from business.ts.

```astro
---
import { Image } from 'astro:assets';
import storefront from '../assets/photos/02-storefront.jpg';
import { business } from '../data/business';
---
<section class="visit" id="visit" aria-label="How to find us">
  <div class="wrap">
    <div class="section-head">
      <span class="section-mark" aria-hidden="true"></span>
      <div class="section-head-text">
        <span class="eyebrow">Come find us</span>
        <h2>We're easy to find.<br />Harder to forget.</h2>
      </div>
    </div>
    <div class="visit-grid">
      <div class="visit-photo">
        <Image src={storefront} alt="Joe's Barbershop storefront — glass door with logo decal, 723 C address numbers, Come In We Are Open sign, strip-mall exterior" loading="lazy" />
      </div>
      <div class="visit-card">
        <h3>The shop</h3>
        <div class="nap">
          <dl>
            <dt>Address</dt>
            <dd>
              {business.address.street}, {business.address.suite}<br />
              {business.address.city}, {business.address.state} {business.address.zip}
              <span class="mono">(strip mall on Bradley Ave)</span>
            </dd>
            <dt>Phone</dt>
            <dd>{business.phone}</dd>
            <dt>Hours</dt>
            <dd>
              <strong>Tue–Fri</strong> 10am–7:30pm<br />
              <strong>Sat</strong> 10am–close<br />
              <strong>Sun–Mon</strong> Closed
            </dd>
            <dt>Payment</dt>
            <dd>Cash only · ATM on site</dd>
            <dt>Walk-ins</dt>
            <dd>Always welcome</dd>
          </dl>
        </div>
        <a class="btn" href="https://maps.google.com/?q=Joe%27s+Barbershop+El+Cajon+CA" target="_blank" rel="noopener">Get directions</a>
      </div>
    </div>
  </div>
</section>
<style>
  .visit { background: var(--bg); }
  .visit-grid { display: grid; grid-template-columns: 1fr 1fr; gap: clamp(28px, 4vw, 64px); align-items: stretch; }
  .visit-photo { position: relative; overflow: hidden; }
  .visit-photo img { width: 100%; aspect-ratio: 4 / 3; object-fit: cover; object-position: center 50%; }
  .visit-card { padding: 32px 36px; border: 1px solid var(--border); background: var(--surface); display: flex; flex-direction: column; gap: 22px; }
  .visit-card h3 { font-family: var(--font-display); font-size: 38px; font-weight: 600; line-height: 1.05; letter-spacing: -0.005em; margin: 0; }
  .nap dl { margin: 0; display: grid; grid-template-columns: 130px 1fr; gap: 14px 20px; }
  .nap dt { font-family: var(--font-board); text-transform: uppercase; letter-spacing: 0.16em; font-size: 11px; font-weight: 600; color: var(--muted); align-self: start; padding-top: 4px; }
  .nap dd { margin: 0; font-size: 16px; line-height: 1.5; }
  .nap dd strong { font-weight: 600; }
  .nap dd .mono { font-family: var(--font-mono); font-size: 14px; color: var(--muted); display: block; margin-top: 2px; letter-spacing: 0.04em; }
  @media (max-width: 980px) { .visit-grid { grid-template-columns: 1fr; } }
</style>
```
  </action>
  <verify>
    <automated>test -f /Users/darrelltang/dtconsulting/joesbarbershop/site/src/components/Heritage.astro &amp;&amp; test -f /Users/darrelltang/dtconsulting/joesbarbershop/site/src/components/Visit.astro &amp;&amp; echo "both exist"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f site/src/components/Heritage.astro` exits 0
    - `test -f site/src/components/Visit.astro` exits 0
    - `grep "import heritagePhoto from" site/src/components/Heritage.astro` returns 1 match (imported object)
    - `grep "loading=\"lazy\"" site/src/components/Heritage.astro` returns 1 match
    - `grep "data-checker\|heritage-frame.*heavy" site/src/components/Heritage.astro` returns NO matches
    - `grep "business.phone" site/src/components/Visit.astro` returns 1 match
    - `grep "business.address" site/src/components/Visit.astro` returns at least 2 matches
    - `grep "loading=\"lazy\"" site/src/components/Visit.astro` returns 1 match
    - `grep "data-checker\|data-font" site/src/components/Visit.astro` returns NO matches
  </acceptance_criteria>
  <done>Heritage and Visit components created with lazy-loaded images and business.ts data; no tweaks remnants</done>
</task>

<task type="auto">
  <name>Task 2: Create FAQ.astro and ClosingCTA.astro; run build check</name>
  <files>site/src/components/FAQ.astro, site/src/components/ClosingCTA.astro</files>
  <read_first>
    - .planning/phases/02-data-design-system/02-PATTERNS.md "FAQ.astro" section (complete markup + CSS)
    - .planning/phases/02-data-design-system/02-PATTERNS.md "ClosingCTA.astro" section (complete markup + CSS)
    - mockups/home-v5/index.html lines 559–633 (FAQ markup) and lines 185–194 (FAQ CSS)
    - mockups/home-v5/index.html lines 637–653 (ClosingCTA markup) and lines 195–204 (ClosingCTA CSS)
    - inputs/02-aeo-constraints.md (AEO rule: FAQs must be flat h3/p pairs, never <details>/<summary> or JS accordions)
  </read_first>
  <action>
**FAQ.astro** — flat h3/p question pairs. NO `<details>`, NO `<summary>`, NO `onclick`, NO JavaScript. AEO parsers must see all FAQ content without JS execution. Numbered questions use a `.num` span for visual decoration only.

```astro
---
---
<section class="faq" aria-label="Frequently asked questions">
  <div class="wrap">
    <div class="section-head">
      <span class="section-mark" aria-hidden="true"></span>
      <div class="section-head-text">
        <span class="eyebrow">The most common questions</span>
        <h2>What people ask before they walk in.</h2>
      </div>
    </div>
    <div class="faq-list">
      <article class="faq-q">
        <span class="num" aria-hidden="true">01</span>
        <div>
          <h3>Do I need an appointment?</h3>
          <p>No appointment needed. Walk-ins are always welcome Tuesday through Saturday. If you want to guarantee a specific time, you can book online at the Square Site link.</p>
        </div>
      </article>
      <article class="faq-q">
        <span class="num" aria-hidden="true">02</span>
        <div>
          <h3>Do you take cards?</h3>
          <p>Cash only. There is an ATM on site if you need it. ATM fee is on the machine, not us.</p>
        </div>
      </article>
      <article class="faq-q">
        <span class="num" aria-hidden="true">03</span>
        <div>
          <h3>How long does a haircut take?</h3>
          <p>Standard haircut is about 30 minutes. A shave or beard service adds 20–30 minutes on top. Walk in with enough time and you will be taken care of.</p>
        </div>
      </article>
      <article class="faq-q">
        <span class="num" aria-hidden="true">04</span>
        <div>
          <h3>Do you cut kids' hair?</h3>
          <p>Yes. Family-friendly shop. Kids cuts are the same price as adult cuts — $30. Bring the whole family.</p>
        </div>
      </article>
      <article class="faq-q">
        <span class="num" aria-hidden="true">05</span>
        <div>
          <h3>Where are you located?</h3>
          <p>723 E Bradley Ave, Suite C, El Cajon CA 92021 — Bostonia neighborhood, strip mall on Bradley Avenue. Easy parking in the lot out front.</p>
        </div>
      </article>
    </div>
  </div>
</section>
<style>
  .faq { background: var(--surface); border-top: 1px solid var(--border); }
  .faq-list { display: grid; grid-template-columns: 1fr; gap: 32px; max-width: 980px; }
  .faq-q { padding-top: 22px; border-top: 1px solid var(--border); display: grid; grid-template-columns: 60px 1fr; gap: 32px; }
  .faq-q .num { font-family: var(--font-display); font-size: 28px; font-weight: 600; color: var(--accent); line-height: 1; }
  .faq-q h3 { font-family: var(--font-display); font-size: clamp(22px, 2.2vw, 28px); font-weight: 600; line-height: 1.2; letter-spacing: -0.005em; margin: 0 0 12px; }
  .faq-q p { margin: 0; font-size: 16.5px; line-height: 1.6; color: var(--fg); max-width: 70ch; }
</style>
```

**ClosingCTA.astro** — centered full-width CTA section. No business data import needed (booking URL is static).

```astro
---
---
<section class="closing-cta" aria-label="Book your appointment">
  <div class="wrap">
    <span class="eyebrow kicker-rule">Ready?</span>
    <h2 class="closing-heading display">Walk on in.<br />We're open.</h2>
    <p class="closing-sub">No appointment required. Cash only, ATM on site. Open Tuesday through Saturday starting at 10am. Three chairs, always room.</p>
    <div class="closing-ctas">
      <a class="btn" href="https://joe-104613.square.site/" target="_blank" rel="noopener">Book a chair</a>
      <a class="btn outline" href="#visit">Get directions</a>
    </div>
    <p class="closing-trust">Walk-ins welcome · Est. 2020 · Bostonia, El Cajon</p>
  </div>
</section>
<style>
  .closing-cta { background: var(--bg); border-top: 1px solid var(--border); text-align: center; padding-block: clamp(64px, 8vw, 112px); }
  .closing-cta .eyebrow.kicker-rule { margin-bottom: 22px; justify-content: center; }
  .closing-cta .eyebrow.kicker-rule::before { display: none; }
  .closing-cta .eyebrow.kicker-rule::after { content: ""; width: 28px; height: 2px; background: var(--accent); display: inline-block; margin-left: 4px; }
  .closing-heading { font-family: var(--font-display); font-size: clamp(40px, 5vw, 68px); font-weight: 600; line-height: 1.02; letter-spacing: -0.008em; margin: 0 auto 22px; max-width: 18ch; }
  .closing-sub { font-size: 18px; line-height: 1.6; color: var(--fg); max-width: 56ch; margin: 0 auto 32px; }
  .closing-ctas { display: flex; gap: 14px; justify-content: center; flex-wrap: wrap; margin-bottom: 22px; }
  .closing-trust { margin: 0; font-family: var(--font-board); text-transform: uppercase; letter-spacing: 0.18em; font-size: 11.5px; font-weight: 600; color: var(--muted); }
</style>
```

After writing both, run build to confirm no errors.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/dtconsulting/joesbarbershop/site &amp;&amp; npm run build 2>&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `test -f site/src/components/FAQ.astro` exits 0
    - `test -f site/src/components/ClosingCTA.astro` exits 0
    - `grep "<details\|<summary\|onclick\|addEventListener" site/src/components/FAQ.astro` returns NO matches (flat HTML only)
    - `grep "faq-q" site/src/components/FAQ.astro` returns at least 5 matches (5 FAQ items)
    - `grep "<h3>" site/src/components/FAQ.astro` returns 5 matches
    - `grep "closing-cta" site/src/components/ClosingCTA.astro` returns at least 2 matches
    - `grep "square.site" site/src/components/ClosingCTA.astro` returns 1 match (booking link)
    - `grep "data-checker\|data-font" site/src/components/FAQ.astro` returns NO matches
    - `grep "data-checker\|data-font" site/src/components/ClosingCTA.astro` returns NO matches
    - `cd site && npm run build` exits 0
  </acceptance_criteria>
  <done>FAQ (flat h3/p) and ClosingCTA created; no accordions/JS; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static component file creation. No user input, no network calls, no auth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-31 | Spoofing | N/A | accept | N/A — no user identity or auth |
| T-02-32 | Tampering | N/A | accept | N/A — static component files, version-controlled |
| T-02-33 | Repudiation | N/A | accept | N/A — no transactions |
| T-02-34 | Information Disclosure | N/A | accept | N/A — only public business data in components |
| T-02-35 | Denial of Service | N/A | accept | N/A — static components, no rate-limiting surface |
| T-02-36 | Elevation of Privilege | N/A | accept | N/A — no auth, no roles |

All STRIDE categories are N/A — this plan creates static Astro component files. No secrets, user input, network calls, or auth surfaces.
</threat_model>

<verification>
- `test -f site/src/components/Heritage.astro` exits 0
- `test -f site/src/components/Visit.astro` exits 0
- `test -f site/src/components/FAQ.astro` exits 0
- `test -f site/src/components/ClosingCTA.astro` exits 0
- `grep "<details\|<summary\|onclick" site/src/components/FAQ.astro` returns no matches
- `cd site && npm run build` exits 0
</verification>

<success_criteria>
- All 4 components created with full OD-5 bodies
- FAQ is flat h3/p pairs — no accordions, no JS
- Visit shows real NAP from business.ts
- Heritage chair image is lazy-loaded
- build exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-design-system/02-06-SUMMARY.md`
</output>
