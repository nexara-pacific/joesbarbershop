---
phase: 03-unique-pages
plan: 10
type: execute
wave: 4
depends_on: ["03-04", "03-05", "03-06", "03-07", "03-08", "03-09"]
files_modified:
  - site/src/pages/dev-mockup-parity.astro
autonomous: false
requirements: ["PAGE-01", "PAGE-02", "PAGE-03", "PAGE-04", "PAGE-05", "PAGE-06"]

must_haves:
  truths:
    - "site/src/pages/dev-mockup-parity.astro is deleted (per D-20, after Plan 04 homepage parity sign-off)"
    - "site/dist/ build no longer lists /dev-mockup-parity as a route"
    - "All 4 ROADMAP success criteria for Phase 3 are provably met (full audit suite + manual visual checks)"
    - "All 6 unique pages return 200 in npm run dev"
  artifacts:
    - path: "site/src/pages/dev-mockup-parity.astro"
      provides: "DELETED — Phase 2 scratch page no longer needed"
      contains: ""
  key_links:
    - from: "Plan 04 visual parity sign-off"
      to: "Plan 10 deletion gate"
      via: "Plan 04 SUMMARY records human-verify approval; Plan 10 reads SUMMARY before deleting"
      pattern: "approved"
    - from: "audit.sh full suite"
      to: "site/dist/* (all 6 pages)"
      via: "every check function runs against built HTML"
      pattern: "passed|failed"
---

<objective>
Final cleanup + end-to-end Phase 3 verification. Deletes `dev-mockup-parity.astro` per D-20 (gated on Plan 04's visual parity sign-off). Runs the full audit suite (`audit.sh` no-arg mode) across all 6 built pages. Manual final pass: human visual check of all 6 pages at desktop/980/600 — confirms no regressions from cumulative Plan 04-09 work.

Purpose: Ships the phase. After this plan, the phase is verifiable against ROADMAP success criteria #1-4. Plan 04 verified `/` parity in isolation; Plan 10 verifies the cumulative result and removes the scratch page.

Output: `dev-mockup-parity.astro` removed; full audit suite green across all 6 pages; phase verification report committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-unique-pages/03-CONTEXT.md
@.planning/phases/03-unique-pages/03-VALIDATION.md
@.planning/phases/03-unique-pages/03-04-SUMMARY.md
@.planning/phases/03-unique-pages/03-05-SUMMARY.md
@.planning/phases/03-unique-pages/03-06-SUMMARY.md
@.planning/phases/03-unique-pages/03-07-SUMMARY.md
@.planning/phases/03-unique-pages/03-08-SUMMARY.md
@.planning/phases/03-unique-pages/03-09-SUMMARY.md
@.planning/phases/03-unique-pages/scripts/audit.sh
@.planning/phases/03-unique-pages/scripts/canonical-slugs.txt

<!--
Phase 3 ROADMAP success criteria (verified in Task 2 + Task 3):
  1. / matches mockups/home-v5/index.html pixel-for-pixel at desktop, 980px, 600px (Plan 04 Task 2 sign-off + Task 3 re-check here)
  2. /east-county-traditional-barbershop renders 6 FAQ Q&As as flat <h3>/<p> pairs with no JS accordions; areaServed list visible in DOM (Plan 05 audits + Task 2 here)
  3. /2026-east-county-barbershop-cost-guide links to all 6 service pages and 5 neighborhood pages (Plan 06 audits + Task 2 here — interpreted per D-16 as "no broken slugs", end-to-Phase-4 for 200 resolution)
  4. /about names Joe Denesowicz and Alex; /reviews shows pulled quotes from Google + Yelp; /faq has 10+ visible Q&As (Plans 07/08/09 audits + Task 2 here)
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete dev-mockup-parity.astro</name>
  <files>site/src/pages/dev-mockup-parity.astro</files>
  <action>
**Pre-flight gate:** verify Plan 04's SUMMARY records human-verify approval (`grep -i "approved\|PASS" .planning/phases/03-unique-pages/03-04-SUMMARY.md`). If Plan 04 did NOT sign off (no approval recorded), HALT this task and ask user — D-20 mandates parity verification BEFORE deletion. The dev-mockup-parity page is the ONLY way to side-by-side compare against the production homepage; deleting before sign-off destroys the verification surface.

**Delete the file:** `rm site/src/pages/dev-mockup-parity.astro`

**Build verification:** `cd site && npm run build` — must exit 0. Verify build output no longer lists `/dev-mockup-parity` as a route (`grep -o '/dev-mockup-parity' site/dist/index.html` returns 0 matches; the page itself no longer builds).

**Repo-grep verification (per RESEARCH § Pitfall 5):** `grep -r "dev-mockup-parity\|dev_mockup_parity" site/src/` returns 0 matches. The page is fully gone — no leftover references in components, layouts, data, or other pages.

**Atomic commit per D-05 + D-20:** one commit `chore(03-10): delete dev-mockup-parity scratch page after homepage parity verified`.

Reference: D-20 (deletion gated on parity verification), RESEARCH § Pitfall 5 (don't forget to delete).
  </action>
  <verify>
    <automated>! test -f site/src/pages/dev-mockup-parity.astro &amp;&amp; cd site &amp;&amp; npm run build &amp;&amp; cd .. &amp;&amp; ! grep -r "dev-mockup-parity\|dev_mockup_parity" site/src/ 2>/dev/null</automated>
  </verify>
  <done>File `site/src/pages/dev-mockup-parity.astro` no longer exists; `npm run build` exits 0; built `site/dist/` does NOT contain a `dev-mockup-parity/` directory; no references to the page remain in `site/src/`.</done>
</task>

<task type="auto">
  <name>Task 2: Run full audit suite + assert all ROADMAP success criteria</name>
  <files>(no file write — produces verification report for Task 3 checkpoint)</files>
  <action>
**Step 1 — Build:** `cd site && npm run build` exits 0.

**Step 2 — Full audit suite:** `bash .planning/phases/03-unique-pages/scripts/audit.sh` (no `--check` filter — runs ALL 14 named checks across all 6 built pages).

Expected output:
- `homepage-faq` — pass (5 or 6)
- `niche-faq` — pass (6)
- `niche-areaserved` — pass (5 unique neighborhood links)
- `cost-guide-slugs` — pass (all 11 canonical slugs present)
- `cost-guide-entries` — pass (≥ 4)
- `about-staff-names` — pass (Joe Denesowicz + Alex)
- `about-pending-photos` — pass (2)
- `reviews-cards` — pass (6, 7, or 8)
- `reviews-sources` — pass (Google + Yelp)
- `faq-master-count` — pass (≥ 10)
- `no-client-directives` — pass (0 matches)
- `no-anti-patterns` — pass (0 matches)
- `no-accordions` — pass (0 matches)
- `bluf-position` — pass (BLUF before prose/entries/grid/faq on every non-homepage page)

If any check fails, halt and surface the failing assertion. Per D-05, do NOT proceed to Task 3 with broken pages.

**Step 3 — ROADMAP success criteria assertion** (the audit checks above map to these — explicit re-verification):

1. **Criterion #1: `/` matches mockup pixel-for-pixel at desktop / 980 / 600.**
   - Plan 04 Task 2 was the human visual sign-off. Re-confirm Plan 04's SUMMARY shows `approved`. Task 3 (this plan's checkpoint) re-checks all 6 pages including `/` for any regressions from cumulative Plan 04-09 work.

2. **Criterion #2: `/east-county-traditional-barbershop` renders 6 FAQ Q&As as flat H3/p with no JS accordions; areaServed list visible in DOM.**
   - `audit.sh --check niche-faq` (= 6) + `--check niche-areaserved` (= 5) + `--check no-accordions` (= 0) + `--check no-client-directives` (= 0). All pass.

3. **Criterion #3: `/2026-east-county-barbershop-cost-guide` links to all 6 service pages and 5 neighborhood pages (no 404s from those links).**
   - Per D-16 interpretation: "no broken slugs" verified by `audit.sh --check cost-guide-slugs` against canonical-slugs.txt. The 200-resolution gate is end-of-Phase-4. Phase 3 satisfies criterion #3 by ensuring all 11 slugs appear as href targets in built HTML.

4. **Criterion #4 (multi-part):**
   - `/about` names Joe Denesowicz and Alex in plain text → `audit.sh --check about-staff-names` passes.
   - `/reviews` shows pulled quotes from Google and Yelp → `audit.sh --check reviews-sources` confirms both labels.
   - `/faq` has 10+ visible Q&As → `audit.sh --check faq-master-count` returns ≥ 10.

**Step 4 — Smoke check 200 status for all 6 pages:** start dev server (`cd site && npm run dev` in background, or use `npm run preview` after a fresh build) and curl each URL:

```bash
for p in / /east-county-traditional-barbershop /2026-east-county-barbershop-cost-guide /about /reviews /faq; do
  status=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:4321${p}")
  echo "${p} → ${status}"
done
```

All 6 should return 200. Stop the dev server.

**Step 5 — Compile verification report into Task 3's checkpoint description.**

Reference: ROADMAP success criteria #1-4, D-16 (cost-guide criterion interpretation), VALIDATION.md § Per-Task Verification Map (the 14 audit checks), D-20 (cleanup), D-05 (halt on broken).
  </action>
  <verify>
    <automated>cd site &amp;&amp; npm run build &amp;&amp; cd .. &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh</automated>
  </verify>
  <done>`npm run build` exits 0; full audit suite (no `--check` filter) passes all 14 checks; smoke check confirms all 6 pages return 200 in dev/preview; ROADMAP success criteria #1-4 are provably met (criterion #1 via Plan 04 SUMMARY + Task 3 re-check; #2-4 via audit checks + counts).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final human visual + factual review across all 6 pages</name>
  <what-built>
Phase 3 built and verified the 6 unique pages of Joe's Barbershop:
- `/` — pixel-parity port of OD-5 mockup with FAQ block (Plan 04, sign-off recorded)
- `/east-county-traditional-barbershop` — niche-query landing with BLUF + 6 FAQs + 5-neighborhood areaServed list (Plan 05)
- `/2026-east-county-barbershop-cost-guide` — comparative listicle with 4-6 entries + See-Also block of all 11 Phase 4 slugs (Plan 06)
- `/about` — Joe + Alex bios with CSS-only portrait placeholders (Plan 07)
- `/reviews` — 6-8 review cards from Google + Yelp (Plan 08; real-extracted or D-10 placeholder per Plan 02 outcome)
- `/faq` — master FAQ with 10+ Q&As grouped under 5 topic H2s (Plan 09)

Plan 10 Task 1 deleted `dev-mockup-parity.astro`. Plan 10 Task 2 ran the full audit suite — all 14 named checks passed; all 6 pages return 200 in dev/preview. ROADMAP success criteria #1-4 are mechanically verified.

This checkpoint is the final eye-test before Phase 3 is marked complete: human review of voice/factual quality across all 6 pages (D-02 — review on the deployed/preview URL, not in the editor).
  </what-built>
  
  <action>This is a human-verify checkpoint. See &lt;how-to-verify&gt; for instructions; the executor performs no automated work in this task — pauses for human review per the resume-signal.</action>
  <how-to-verify>
1. **Start dev server:** `cd site && npm run dev` — note the URL (default `http://localhost:4321/`).

2. **Visit each page in order, top-to-bottom, on desktop:**
   - `http://localhost:4321/`
   - `http://localhost:4321/east-county-traditional-barbershop`
   - `http://localhost:4321/2026-east-county-barbershop-cost-guide`
   - `http://localhost:4321/about`
   - `http://localhost:4321/reviews`
   - `http://localhost:4321/faq`

3. **For each page, check:**
   a. **Visual rhythm:** spacing, typography, color discipline (no unexpected accent uses), CheckDividers in correct positions on `/`.
   b. **Voice:** does the prose read as heritage / no-frills / Western-Victorian (Joe's voice)? Or does it drift toward Brooklyn-grooming-bro / luxury-spa / SaaS-coded? If drift on any page, note which page + section.
   c. **Factual correctness:** spot-check 2-3 facts per page (hours, prices, address, phone, owner name). All should match `business.json` verbatim.
   d. **AEO discipline:** BLUF first, FAQ flat (no toggles), no hidden content, no text-as-image. Open DevTools to confirm areaServed list on `/east-county-traditional-barbershop` is real `<a>` elements (not images).
   e. **Cross-link integrity:** click 2-3 cross-links on the cost guide. They will 404 in this build (Phase 4 routes don't exist yet) — that's expected per D-16. Confirm the URL bar shows the canonical slug (`/fades`, `/bostonia-barber`, etc.) and not a typo (e.g., `/fade` singular).

4. **Resize to 980px and 600px** and re-check `/` for parity (Plan 04 already signed off but cumulative work could regress); spot-check 2 other pages at each breakpoint for layout integrity.

5. **Log result:**
   - PASS: type "approved" with a one-line voice/factual summary. Document any minor deltas worth fixing later but not blocking.
   - FAIL with specifics: which page, what voice/factual/visual issue, severity (blocking / non-blocking). If blocking, halt phase and revise (likely another iteration of the offending page's skill chain per D-04 deferred ideas — adding voice guides as `product-marketing-context` inputs).

**Quit dev server (`Ctrl+C`) after verification.**

Reference: D-02 (review on preview URL not in editor), VALIDATION.md § Manual-Only Verifications, ROADMAP success criteria #1-4 final gate.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 pages read clean (voice + factual + visual); or describe issues per page. If issues are non-blocking minor polish (typo, awkward phrasing), proceed to commit but log them in the SUMMARY for follow-up. If issues are blocking (hallucinated facts, off-brand voice, broken layout), halt and revise the offending plan(s) before committing this final checkpoint.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plan 04 SUMMARY → Plan 10 deletion gate | Task 1 reads SUMMARY for `approved` token before deleting scratch page |
| audit.sh full suite → ROADMAP criteria | mechanical verification; Task 3 layers human voice/factual review on top |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10-01 | T (Tampering) | premature deletion of dev-mockup-parity before Plan 04 sign-off | mitigate | Task 1 pre-flight gate explicitly reads `03-04-SUMMARY.md` for `approved` token; halts if absent. D-20 explicit ordering. |
| T-03-10-02 | I (Information disclosure) | leftover `dev-mockup-parity` route in production sitemap | mitigate | Task 1 verification: `grep -r 'dev-mockup-parity' site/src/` returns 0 matches AND `site/dist/dev-mockup-parity/` does not exist after rebuild. RESEARCH § Pitfall 5 documents the failure mode. |
| T-03-10-03 | T (Tampering) | full audit suite false-positive (script bug masking real failure) | accept | Plan 00 Task 2 self-test verified script invariants. Audit functions are simple greps; bug surface is small. Manual Task 3 review provides second-pass check. |
| T-03-10-04 | E (Elevation of privilege) | regression in cumulative Plan 04-09 work breaking earlier sign-offs | mitigate | Task 3 re-checks `/` at all 3 breakpoints (Plan 04's gate) plus spot-checks 2 other pages at each breakpoint. Catches regressions from cumulative work. |
| T-03-10-05 | R (Repudiation) | hallucinated facts surviving all per-plan spot-checks | mitigate | Task 3 voice + factual review is the final human gate. Per-plan spot-checks (Plan 05/07/08/09 done criteria) are first pass; cross-page review at this checkpoint catches inconsistencies (e.g., `/about` and `/faq` cite different hours). |
</threat_model>

<verification>
- `! test -f site/src/pages/dev-mockup-parity.astro`
- `cd site && npm run build` exits 0
- `! test -d site/dist/dev-mockup-parity` (no built scratch route)
- `! grep -r 'dev-mockup-parity' site/src/` (no leftover references)
- `bash .planning/phases/03-unique-pages/scripts/audit.sh` (no-arg, full suite) — all 14 checks pass
- `for p in / /east-county-traditional-barbershop /2026-east-county-barbershop-cost-guide /about /reviews /faq; do test -f site/dist${p%/}/index.html || test -f site/dist${p}index.html; done` — all 6 pages built
- Plan 04 SUMMARY contains `approved` token (Task 1 pre-flight gate)
- Task 3 human verification approval recorded in this plan's SUMMARY
</verification>

<success_criteria>
1. `dev-mockup-parity.astro` deleted; build does not generate the route; no leftover references in `site/src/`
2. Full audit suite green (all 14 named checks pass)
3. All 6 pages return 200 in `npm run dev` / `npm run preview`
4. ROADMAP success criteria #1-4 mechanically verified
5. Human voice + factual review approves all 6 pages (Task 3 sign-off)
6. Phase 3 is complete and unblocks Phase 4 (templated services + neighborhoods pages)
</success_criteria>

<output>
After completion, create `.planning/phases/03-unique-pages/03-10-SUMMARY.md` documenting:
- Plan 04 SUMMARY pre-flight gate result (approved / failed)
- Audit suite full-run output (all 14 checks listed with pass/fail)
- All 6 page URL → HTTP status results
- Per-page voice + factual review log from Task 3 (one paragraph per page; what worked, any deltas)
- Cumulative Phase 3 verification statement: "ROADMAP success criteria #1, #2, #3, #4 are met as of [date]" with the audit assertions that prove each
- For Phase 4 hand-off: confirmation that 11 canonical Phase 4 slugs are referenced from Phase 3 pages (cost guide + niche-landing + masthead) and resolve to 200 once Phase 4 templated routes ship
- For Phase 5 hand-off: list of pages awaiting schema injection (`HairSalon`, `LocalBusiness` on `/`; `FAQPage` on `/east-county-...`, `/faq`, etc.; `Article` on cost-guide and niche-landing; `Person` on `/about`; `Review` + `AggregateRating` on `/reviews`)
- Outstanding `_showcase_review_pending` markers in `business.json` (the 4 about.* entries from Plan 07 + any Plan 02 review/competitor fallback entries)
</output>
