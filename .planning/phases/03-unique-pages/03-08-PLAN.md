---
phase: 03-unique-pages
plan: 08
type: execute
wave: 3
depends_on: ["03-00", "03-01", "03-02", "03-04"]
files_modified:
  - site/src/pages/reviews.astro
autonomous: true
requirements: ["PAGE-05"]

must_haves:
  truths:
    - "/reviews shows pulled quotes from Google + Yelp (or D-10 placeholder fallback) — both source labels appear in built HTML"
    - "Page renders 6–8 review cards in the locked grid (2-col desktop, 2-col 980px, 1-col 600px)"
    - "BLUF answers primary query 'Joe's Barbershop reviews / ratings' with rating + count"
    - "reviews.json drives card rendering — no inline duplication of review data"
  artifacts:
    - path: "site/src/pages/reviews.astro"
      provides: "PAGE-05 — reviews page with 6–8 review cards"
      min_lines: 100
      contains: "reviews.map"
  key_links:
    - from: "site/src/pages/reviews.astro"
      to: "site/src/data/reviews.json"
      via: "import + .map() over review-card articles"
      pattern: "import reviews"
    - from: "site/src/pages/reviews.astro"
      to: "site/src/data/business.json"
      via: "BLUF interpolates business.ratings.{google,yelp}"
      pattern: "business.ratings"
---

<objective>
Build the reviews page at `site/src/pages/reviews.astro` per D-08..D-11 + UI-SPEC § Review Card. Imports `reviews.json` (Plan 02 output), renders 6–8 review cards in the locked grid layout. AggregateRating schema is deferred to Phase 5; visible quote layout ships here. No FAQ component on this page (UI-SPEC explicit).

Purpose: PAGE-05 satisfies ROADMAP success criterion #4 (`/reviews` shows pulled quotes from Google + Yelp). Whether real-extracted or D-10 placeholder, the layout and contract are identical — Phase 5 schema injection wraps the same DOM structure.

Output: `reviews.astro` with article-shape composition, `reviews.json`-driven card grid, no FAQ component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-unique-pages/03-CONTEXT.md
@.planning/phases/03-unique-pages/03-PATTERNS.md
@.planning/phases/03-unique-pages/03-UI-SPEC.md
@.planning/phases/03-unique-pages/03-VALIDATION.md
@inputs/02-aeo-constraints.md
@.agents/aeo-frame.md
@.agents/product-marketing-context.md
@site/src/data/reviews.json
@site/src/data/business.json
@site/src/components/ClosingCTA.astro
@site/src/components/FAQ.astro
@site/src/components/PriceBoard.astro
@site/src/layouts/Base.astro

<!--
COMPOSITION (per D-22 + UI-SPEC § Review Card + Reviews Page Header):
  Article header (H1 "Reviews — Joe's Barbershop" + dateModified)
  → BLUF capsule (~80-110 words: "Joe's Barbershop is rated 4.9★ across 91 Google + 33 Yelp reviews...")
  → Review-card grid (6-8 cards from reviews.json)
  → ClosingCTA

NO FAQ component on reviews page (UI-SPEC § Review Card explicit).
NO Hero/FactStrip/PriceBoard/Heritage/Visit (D-17).

PRIMARY QUERY (D-01 step 2):
  "Joe's Barbershop reviews / ratings"

REVIEW DATA: imported from site/src/data/reviews.json (Plan 02 output)
  Schema per record: { name, rating, quote, source: 'Google'|'Yelp', date }

UI-SPEC sections that govern this page:
- § Article Header
- § BLUF Capsule
- § Review Card (grid: 2-col desktop, 2-col 980px, 1-col 600px; padding 28/24/20; star row at top in accent;
  quote 17px Newsreader; reviewer H3 IM Fell 20px; source-line Oswald 11px muted)
- § Reviews Page Header
- § Section Spacing Rhythm
- § Copywriting Contract (eyebrow "Reviews · Google + Yelp")

NO accent on platform name (no Google-blue / Yelp-red brand colors). Source label uses var(--muted).
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run marketing-skills:copywriting for reviews page BLUF only</name>
  <files>(no file write — produces in-context output for Task 2)</files>
  <action>
**Working directory invariant:** `pwd` returns the repo root.

Invoke `marketing-skills:copywriting` skill with:

1. **Page primary query** (D-01 step 2): `"Joe's Barbershop reviews / ratings"`
2. **Section requirements:**
   - **BLUF capsule ONLY** (80–110 words, entity-first, first sentence in `<strong>`): "Joe's Barbershop is rated 4.9★ across 91 Google and 33 Yelp reviews..." — declarative, NAP + ratings up-front. Highlight the cash-only / walk-ins / heritage angle in 1–2 sentences. The page is short — BLUF is the AEO-load-bearing prose; the cards do the rest.
   - **DO NOT generate review quotes** — those come verbatim from `reviews.json` (Plan 02). Skill writes only the BLUF.
3. **Explicit citation:** "Use guidance in `.agents/aeo-frame.md` and brand context in `.agents/product-marketing-context.md`. Use `business.ratings.google.value`, `business.ratings.google.count`, `business.ratings.yelp.value`, `business.ratings.yelp.count` from `business.json` for the rating numbers — do NOT invent or round."

**Skill output review (D-02 + D-05):**
- Banned-phrase pre-check (standard list)
- Hallucination pre-check: BLUF must use the EXACT rating values + counts from `business.json`:
  - `ratings.google.value`: 4.9
  - `ratings.google.count`: 91
  - `ratings.yelp.value`: 4.9
  - `ratings.yelp.count`: 33
  - Combined count phrasing: "across 91 Google + 33 Yelp reviews" or "across 124 reviews on Google and Yelp" (sum: 91+33=124). EITHER phrasing is acceptable; both math-check.

Reference: D-08, D-09, D-22, D-01 step 3, RESEARCH § Pattern 6 Step 3.
  </action>
  <verify>
    <automated>test -s .agents/aeo-frame.md &amp;&amp; test -s site/src/data/reviews.json &amp;&amp; test -s site/src/data/business.json</automated>
  </verify>
  <done>Skill produced BLUF (~80-110 words). No banned phrases. Rating values match `business.json` exactly. No review-quote generation (data comes from `reviews.json`).</done>
</task>

<task type="auto">
  <name>Task 2: Create reviews.astro with data-driven card grid</name>
  <files>site/src/pages/reviews.astro</files>
  <action>
Create `site/src/pages/reviews.astro`. Composition pattern in PATTERNS.md § `reviews.astro` — copy that pattern verbatim, embed Task 1's BLUF output, and apply scoped CSS from PATTERNS.md + UI-SPEC § Review Card.

**Frontmatter:**

```astro
--- // (astro frontmatter)
import Base from '../layouts/Base.astro';
import ClosingCTA from '../components/ClosingCTA.astro';
import { business } from '../data/business';
import reviews from '../data/reviews.json';

const lastUpdated = 'May 2026';
--- // (astro frontmatter)
```

**Body** (per UI-SPEC § Article Header / § BLUF Capsule / § Review Card / § Reviews Page Header + PATTERNS.md):

```astro
<Base
  title="Reviews · Joe's Barbershop · 4.9★ Google + Yelp"
  description="Joe's Barbershop is rated 4.9★ across 91 Google and 33 Yelp reviews. Real customer quotes from East County, El Cajon."
>
  <header class="article-head">
    <div class="wrap">
      <span class="eyebrow kicker-rule">Reviews · Google + Yelp</span>
      <h1>What customers say.</h1>
      <p class="datestamp">UPDATED {lastUpdated.toUpperCase()}</p>
    </div>
  </header>

  <section class="bluf" aria-label="Answer capsule">
    <div class="wrap">
      <p class="bluf-lead">
        <strong>[Skill BLUF first sentence using business.ratings values exactly, in <strong>]</strong>
        [Skill BLUF rest, ~80-110 words]
      </p>
    </div>
  </section>

  <section class="reviews-section">
    <div class="wrap">
      <div class="review-grid">
        {reviews.map(r => (
          <article class="review-card">
            <div class="stars" aria-label={`${r.rating} out of 5 stars`}>
              {'★'.repeat(r.rating)}
            </div>
            <p class="review-quote">"{r.quote}"</p>
            <h3 class="reviewer">{r.name}</h3>
            <p class="review-source">{r.source}{r.date ? ` · ${r.date}` : ''}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <ClosingCTA />
</Base>
```

**Scoped CSS** — copy from PATTERNS.md § reviews.astro CSS + UI-SPEC § Review Card. Required selectors:
- `.article-head`, `.bluf`, `.bluf-lead` (mirror niche-landing/about CSS — same pattern)
- `.reviews-section` (background `var(--bg)`, padding `clamp(56px, 7vw, 96px) 0`)
- `.review-grid` (`display: grid; grid-template-columns: repeat(2, 1fr); gap: 28px;`)
  - `@media (max-width: 980px)`: `gap: 22px;` (still 2-col)
  - `@media (max-width: 600px)`: `grid-template-columns: 1fr; gap: 20px;`
- `.review-card` (`background: var(--surface); border: 1px solid var(--border); padding: 28px 28px 24px;`)
  - `@media (max-width: 980px)`: `padding: 24px 24px 20px;`
  - `@media (max-width: 600px)`: `padding: 20px 20px 16px;`
- `.review-card .stars` (`font-size: 18px; color: var(--accent); letter-spacing: 2px; margin-bottom: 14px;`)
- `.review-card .review-quote` (Newsreader 17px / 1.55 / `color: var(--fg)`; `margin: 0 0 18px;`; uses curly quotes inline `"..."` NOT `<blockquote>`)
- `.review-card h3.reviewer` (IM Fell 20px / 600 / `line-height: 1.2`; `margin: 0 0 4px;`)
- `.review-card .review-source` (Oswald uppercase `0.14em` tracking, 11px / 600, `color: var(--muted);` margin 0)

**Critical structural invariants:**
1. BLUF first under `<main>` after article header.
2. NO `<FAQ />` component import or render (UI-SPEC § Review Card explicit).
3. NO `<Hero />`, `<FactStrip />`, `<PriceBoard />`, `<Heritage />`, `<Visit />` (D-17).
4. NO `<blockquote>` for review-quote — use plain `<p class="review-quote">` with curly quotes inside (UI-SPEC § Review Card explicit: "to keep schema-clean").
5. Each card has stars row + quote + reviewer name (H3) + source-line in that order.
6. Source-line color is `var(--muted)` — NO Google-blue / Yelp-red brand color.
7. `reviews.json` schema is `{name, rating, quote, source, date}` — Wave 2 Plan 02 guarantees 6-8 records. Do NOT modify the data file in this plan.
8. NO `client:*`, NO `<details>`, NO accordion markup.

**Atomic commit per D-05:** one commit `feat(03-08): build /reviews page with 6-8 review cards`.

Reference: D-08, D-09, D-10, D-11, D-22, UI-SPEC § Review Card / § Reviews Page Header, PATTERNS.md § reviews.astro, RESEARCH § Pattern 7.
  </action>
  <verify>
    <automated>cd site &amp;&amp; npm run build &amp;&amp; cd .. &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check reviews-cards &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check reviews-sources &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-anti-patterns &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-accordions &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check bluf-position</automated>
  </verify>
  <done>`npm run build` exits 0; `site/dist/reviews/index.html` is generated; `audit.sh --check reviews-cards` returns 6, 7, or 8; `audit.sh --check reviews-sources` confirms both Google + Yelp labels appear; `audit.sh --check no-anti-patterns / --check no-accordions / --check no-client-directives / --check bluf-position` all pass; manual spot-read confirms NO FAQ component renders, NO `<blockquote>` element used for review quotes, BLUF rating numbers match `business.json` exactly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviews.json → page rendering | Plan 02 guarantees data shape; Path A/B/C from Plan 02 may produce real or placeholder content |
| BLUF rating claim → business.json | rating values must match exactly; off-by-one creates citation error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08-01 | T (Tampering) | BLUF rating numbers off from business.json | mitigate | Task 1 hallucination pre-check enumerates exact values (4.9 / 91 / 4.9 / 33). Manual spot-read in Task 2 done criteria verifies. Future-proofing: Plan 09 (FAQ) and other rating-citing pages have the same risk. |
| T-03-08-02 | T (Tampering) | banned anti-pattern phrases in BLUF | mitigate | `audit.sh --check no-anti-patterns` part of Task 2 verify command. |
| T-03-08-03 | I (Information disclosure) | leaking placeholder text `[Quote pending]` to production unintentionally | accept | If Plan 02 took D-10 fallback path, placeholders are intentional — they appear in built HTML as part of the data shape. `_showcase_review_pending` array tracks the swap need. Manual spot-read confirms placeholder presence is intentional, not accidental. |
| T-03-08-04 | E (Elevation of privilege) | `client:*` or accordion markup | mitigate | `audit.sh --check no-client-directives / --check no-accordions` part of Task 2 verify command. |
| T-03-08-05 | R (Repudiation) | review attribution accuracy | accept | Plan 02 Task 2 anti-fabrication gate handles this; Plan 08 trusts the data shape. Per D-09 + D-11, first-name-last-initial attribution is safe-harbor. |
</threat_model>

<verification>
- `cd site && npm run build` exits 0
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check reviews-cards` returns 6, 7, or 8
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check reviews-sources` confirms both Google + Yelp present
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-anti-patterns` passes
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-accordions` passes
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-client-directives` passes
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check bluf-position` passes
- `grep -c '<article class="review-card"' site/dist/reviews/index.html` returns 6-8
- `! grep '<blockquote' site/dist/reviews/index.html` (no blockquote per UI-SPEC)
- `! grep '<article class="faq-q"' site/dist/reviews/index.html` (no FAQ component)
- `grep -E '4\.9.*91|91.*Google' site/dist/reviews/index.html` (rating values match business.json)
</verification>

<success_criteria>
1. `site/src/pages/reviews.astro` exists with article-shape composition + review-card grid + ClosingCTA
2. 6-8 review cards rendered from `reviews.json`
3. Each card: stars row (5 ★ in accent) + quote + reviewer (H3) + source/date line
4. Source labels appear: at least 1 Google + at least 1 Yelp (or all placeholder per D-10 — both still appear since data shape is preserved)
5. BLUF rating numbers match `business.json` exactly (4.9, 91, 33)
6. NO FAQ component, NO `<blockquote>`, NO Hero/FactStrip/PriceBoard/Heritage/Visit
7. NO client:*, NO accordions, NO banned phrases
8. ROADMAP success criterion #4 (reviews page shows pulled quotes from Google + Yelp) is provably met
</success_criteria>

<output>
After completion, create `.planning/phases/03-unique-pages/03-08-SUMMARY.md` documenting:
- Skill output sources cited
- BLUF first-sentence strong-wrap text + the rating numbers used
- Number of cards rendered (6, 7, or 8) and Google/Yelp split
- Whether path A (real) / B (mixed) / C (placeholder) from Plan 02 was visible in built HTML
- Confirmation of no FAQ component and no `<blockquote>` element
- For Phase 5: review cards have name/rating/quote/source/date data ready for `Review` schema wrapping
</output>
