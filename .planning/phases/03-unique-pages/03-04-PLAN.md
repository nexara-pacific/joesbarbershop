---
phase: 03-unique-pages
plan: 04
type: execute
wave: 3
depends_on: ["03-00", "03-01"]
files_modified:
  - site/src/pages/index.astro
autonomous: false
requirements: ["PAGE-01"]

must_haves:
  truths:
    - "/ renders pixel-parity with mockups/home-v5/index.html at desktop (≥981px), 980px, and 600px"
    - "Homepage FAQ block contains 5–6 Q&As as flat <article class=\"faq-q\"><h3>...</h3><p>...</p></article> markup with no JS"
    - "Homepage uses the locked composition from D-18: CheckDivider → Hero → FactStrip → CheckDivider → PriceBoard → Heritage → CheckDivider → Visit → FAQ → CheckDivider → ClosingCTA"
    - "title and description set on <Base> for dev visibility (full meta suite is Phase 5)"
  artifacts:
    - path: "site/src/pages/index.astro"
      provides: "Production homepage replacing Phase 1 placeholder"
      min_lines: 12
      contains: "<Hero />"
  key_links:
    - from: "site/src/pages/index.astro"
      to: "site/src/layouts/Base.astro"
      via: "Base wrapping with title + description"
      pattern: "<Base[^>]*title="
    - from: "site/src/pages/index.astro"
      to: "site/src/components/{Hero,FactStrip,PriceBoard,Heritage,Visit,FAQ,ClosingCTA,CheckDivider}.astro"
      via: "component imports + composition"
      pattern: "import (Hero|FactStrip|PriceBoard|Heritage|Visit|FAQ|ClosingCTA|CheckDivider)"
---

<objective>
Build the production homepage at `site/src/pages/index.astro` with pixel-parity to `mockups/home-v5/index.html`. Per D-18, the homepage is a copy of `dev-mockup-parity.astro` with production `title`/`description` props on `<Base>` and the existing `FAQ.astro` component (already populated with 5 Q&As matching the homepage subset per RESEARCH § Code Examples Example 1) — no other changes.

Purpose: PAGE-01 (`/` parity) is ROADMAP success criterion #1 — the strictest visual gate of the phase. Building this first proves the homepage anchors the visual baseline before the other 5 pages diverge into bespoke layouts.

This plan has a `checkpoint:human-verify` task because pixel-parity is a human visual gate (no Playwright snapshot infra in this phase per VALIDATION.md). The checkpoint is AFTER the automated implementation is committed.

Output: `site/src/pages/index.astro` matching the OD-5 mockup at all 3 breakpoints, FAQ populated with the existing 5 Q&As (or 6 per the planner's discretion if the skill chain produces a 6th), and a human visual sign-off recorded in the plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-unique-pages/03-CONTEXT.md
@.planning/phases/03-unique-pages/03-PATTERNS.md
@.planning/phases/03-unique-pages/03-UI-SPEC.md
@.planning/phases/03-unique-pages/03-VALIDATION.md
@site/src/pages/dev-mockup-parity.astro
@site/src/components/FAQ.astro
@site/src/layouts/Base.astro
@mockups/home-v5/index.html

<!--
Composition (D-18, locked):
  CheckDivider → Hero → FactStrip → CheckDivider → PriceBoard → Heritage
  → CheckDivider → Visit → FAQ → CheckDivider → ClosingCTA

The existing dev-mockup-parity.astro is the working template. Only differences:
1. Replace dev-only title/description with production strings
2. Remove DEV-ONLY comment block from frontmatter

Do NOT modify FAQ.astro to accept props (UI-SPEC § FAQ Master Page Layout decision —
"Phase 3 does NOT modify FAQ.astro to accept dynamic items"). The 5 hard-coded Q&As
(appointment, cards/cash, time, kids, location) are already the homepage subset per D-19.

Do NOT consume <slot name="head" /> in Base.astro — reserved for Phase 5 schema (Phase 2 D-26).

Do NOT add CheckDivider before/after the canonical 5 inserts — mockup parity is the gate.
-->

<!--
title + description per RESEARCH § Code Examples Example 1 (suggested; planner-discretion final wording):
  title="Joe's Barbershop — Bostonia, El Cajon"
  description="Traditional barbershop in Bostonia, East County San Diego. Walk-ins welcome, cash only. Tue–Sat 10am–7:30pm."
Per RESEARCH § Pitfall 4: do NOT add Open Graph, Twitter Card, robots, or any meta beyond title + description.
Phase 5 owns META-01..04 and will rewrite.
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite index.astro for pixel-parity homepage</name>
  <files>site/src/pages/index.astro</files>
  <action>
Read `site/src/pages/dev-mockup-parity.astro` (entire file — it is the working template).

Read `site/src/pages/index.astro` (current Phase 1 placeholder — to be replaced).

**Write `site/src/pages/index.astro`** with the exact composition from D-18 + RESEARCH § Code Examples Example 1:

```astro
--- // (astro frontmatter)
import Base from '../layouts/Base.astro';
import CheckDivider from '../components/CheckDivider.astro';
import Hero from '../components/Hero.astro';
import FactStrip from '../components/FactStrip.astro';
import PriceBoard from '../components/PriceBoard.astro';
import Heritage from '../components/Heritage.astro';
import Visit from '../components/Visit.astro';
import FAQ from '../components/FAQ.astro';
import ClosingCTA from '../components/ClosingCTA.astro';
--- // (astro frontmatter)
<Base
  title="Joe's Barbershop — Bostonia, El Cajon"
  description="Traditional barbershop in Bostonia, East County San Diego. Walk-ins welcome, cash only. Tue–Sat 10am–7:30pm."
>
  <CheckDivider />
  <Hero />
  <FactStrip />
  <CheckDivider />
  <PriceBoard />
  <Heritage />
  <CheckDivider />
  <Visit />
  <FAQ />
  <CheckDivider />
  <ClosingCTA />
</Base>
```

**Critical invariants (per D-18, RESEARCH § Pitfall 8, UI-SPEC § Component Inventory):**
1. NO scoped `<style>` block on this page — all styling lives in component files (homepage is pure composition).
2. NO `<slot name="head" />` consumption (Phase 5 territory per Phase 2 D-26).
3. NO `client:*` directives anywhere.
4. Component order EXACTLY matches D-18 — 5 `<CheckDivider />` instances total, in the specific positions listed.
5. NO modification of `FAQ.astro` to accept props (per RESEARCH § Code Examples Example 1 final paragraph + UI-SPEC § FAQ Master Page Layout). Existing 5 Q&As are the homepage subset (D-19).

**FAQ Q&A count check:** Per D-19, homepage shows 5–6 Q&As. The current `FAQ.astro` component contains 5 Q&As (verified — appointment, cards/cash, time, kids, location). 5 is within the 5–6 range. **Do NOT add a 6th to `FAQ.astro` in this plan** — it would force a component-API change for one use case and risks visual drift on the parity check. If user later wants 6, that's a follow-up after parity verification.

**`title` and `description` choice (Phase 3 minimum, per RESEARCH § Pitfall 4):**
- `title`: `"Joe's Barbershop — Bostonia, El Cajon"` (matches the entity-first declarative tone for `/`'s primary query "barbershop in Bostonia / El Cajon / East County")
- `description`: `"Traditional barbershop in Bostonia, East County San Diego. Walk-ins welcome, cash only. Tue–Sat 10am–7:30pm."` (one factual sentence, NAP + hours + payment — Phase 5 will rewrite)

**Do NOT add other Base props** (no `og:image`, no canonical URL, no Twitter Card). Phase 5 expands.

**Atomic commit per D-05:** one commit `feat(03-04): build production homepage at parity with OD-5 mockup`.

**Skill chain note (D-01):** The homepage's BLUF is already inside `Hero.astro` (`.hero-bluf`, locked from Phase 2). No skill invocation needed for the homepage body. The 5 FAQ Q&As are already in `FAQ.astro`. **The marketing-skills:copywriting skill is NOT invoked for this plan** — homepage copy is fully covered by Phase 2 component content. (Wave 3 plans 05–09 invoke copywriting per page; this one is the exception.)

Reference: D-18 (composition), D-19 (FAQ subset), D-20 (cleanup deferred to Plan 10), RESEARCH § Pitfalls 4 + 8, UI-SPEC § Component Inventory + § FAQ Master Page Layout, PATTERNS.md `index.astro` (REWRITE) section.
  </action>
  <verify>
    <automated>cd site &amp;&amp; npm run build &amp;&amp; cd .. &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check homepage-faq &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-client-directives &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-anti-patterns &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-accordions</automated>
  </verify>
  <done>`npm run build` exits 0; built `site/dist/index.html` exists; `audit.sh --check homepage-faq` returns "5 or 6 faq-q articles" (pass); `audit.sh --check no-client-directives` returns 0 matches in `site/src/pages/`; `grep -c '<CheckDivider />' site/src/pages/index.astro` returns 5; `grep -c 'import.*from .*components' site/src/pages/index.astro` returns 8 (Hero, FactStrip, PriceBoard, Heritage, Visit, FAQ, ClosingCTA, CheckDivider).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual parity check at 3 breakpoints</name>
  <what-built>
The production homepage at `site/src/pages/index.astro` was rewritten in Task 1 to match `dev-mockup-parity.astro` composition (D-18) with production `title`/`description` props. The FAQ block uses the existing `FAQ.astro` component's 5 hard-coded Q&As (D-19 subset). No scoped CSS, no `client:*` directives, no schema injection.

`npm run build` exited 0 and `audit.sh --check homepage-faq` passed. The remaining gate is pixel-parity at 3 breakpoints — a human visual check per VALIDATION.md § Manual-Only Verifications because no Playwright snapshot infra ships in this phase.
  </what-built>
  
  <action>This is a human-verify checkpoint. See &lt;how-to-verify&gt; for instructions; the executor performs no automated work in this task — pauses for human review per the resume-signal.</action>
  <how-to-verify>
1. **Start dev server:** `cd site && npm run dev` — note the printed URL (default `http://localhost:4321/`).

2. **Open two browser tabs side-by-side:**
   - Tab A: `http://localhost:4321/` (the new production homepage)
   - Tab B: `http://localhost:4321/dev-mockup-parity` (the Phase 2 scratch page — still present, will be deleted in Plan 10)

3. **At desktop viewport (resize to 1440px wide):**
   - Visually compare both tabs from top to bottom.
   - Check section ordering: UtilBar → Masthead → CheckDivider → Hero → FactStrip → CheckDivider → PriceBoard → Heritage → CheckDivider → Visit → FAQ → CheckDivider → ClosingCTA → Footer.
   - Confirm: hero photo loads, FactStrip 4 columns visible, PriceBoard letter-board visible, Heritage 2-col grid, Visit storefront photo, FAQ 5 questions with "01"/"02"/"03"/"04"/"05" numbering, ClosingCTA visible.
   - Acceptable difference: NONE (the 5 FAQ Q&As are identical between tabs).

4. **At 980px viewport (resize the window to ~980px or use DevTools device toolbar):**
   - Multi-column grids should collapse to 1fr (Heritage 1-col, Footer 2-col).
   - Nav.primary should hide (only burger / minimal masthead).
   - Compare side-by-side. Acceptable difference: NONE.

5. **At 600px viewport:**
   - PriceBoard font-size reduces, Footer collapses to 1fr, util-bar phone reflows.
   - Visit + Heritage become 1-col.
   - Compare side-by-side. Acceptable difference: NONE.

6. **Also open `mockups/home-v5/index.html` in a 4th tab** (file:// path or via `python3 -m http.server 8000` from `mockups/home-v5/`) and spot-check the desktop view against `/`. The dev-mockup-parity page already passed this gate in Phase 2; this task confirms `/` matches `dev-mockup-parity.astro` (transitive parity to the OD-5 mockup).

7. **Log result:**
   - PASS: type "approved" with no issues. Document the date/time of the check in the plan SUMMARY.
   - FAIL: describe the visual delta(s) — section, breakpoint, what looks different, screenshot if useful. Common deltas to look for: extra/missing CheckDivider, wrong padding, missing FAQ entries, unstyled elements (CSS not loading).

**Quit dev server (`Ctrl+C`) after verification.**

Reference: ROADMAP success criterion #1 (this is the gate), VALIDATION.md § Manual-Only Verifications, RESEARCH § Pitfall 8 (parity drift recovery — "copy dev-mockup-parity.astro to index.astro first, populate FAQ, verify visually before any other Phase 3 page work").
  </how-to-verify>
  <resume-signal>Type "approved" if all 3 breakpoints match; or describe the visual deltas if any. If FAIL, halt and revise — do NOT proceed to Plan 05+ until homepage parity is confirmed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| index.astro → Base.astro | composition wrapping; props (title, description) cross the boundary as plain strings |
| index.astro → 8 component imports | static imports at build time; no runtime composition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | T (Tampering) | scoped `<style>` block accidentally added | mitigate | Task 1 explicitly forbids a `<style>` block on index.astro. Verification: `grep -c '<style>' site/src/pages/index.astro` returns 0. |
| T-03-04-02 | I (Information disclosure) | dev-only comment leaks into production | mitigate | Task 1 removes the `// DEV-ONLY SCRATCH PAGE` comment from `dev-mockup-parity.astro` when copying. Verification: `grep -i "dev-only\|scratch" site/src/pages/index.astro` returns 0 matches. |
| T-03-04-03 | E (Elevation of privilege) | `client:*` directive enabling JS execution | mitigate | `audit.sh --check no-client-directives` runs in Task 1 verify command and gates the commit. AEO requires zero-JS DOM. |
| T-03-04-04 | D (Denial of service) | Astro build fails on missing component import | mitigate | `npm run build` is part of Task 1 verify command; build failure halts commit. |
</threat_model>

<verification>
- `cd site && npm run build` exits 0
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check homepage-faq` passes
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-client-directives` passes
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-anti-patterns` passes
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-accordions` passes
- `grep -c '<style>' site/src/pages/index.astro` returns 0
- `grep -ci 'dev-only\|scratch' site/src/pages/index.astro` returns 0
- `grep -c '<CheckDivider />' site/src/pages/index.astro` returns 5
- Human visual parity check (Task 2) signed off at desktop / 980 / 600
</verification>

<success_criteria>
1. `site/src/pages/index.astro` exists with the D-18 composition (5 CheckDividers, 7 component instances + 5th CheckDivider)
2. Production `title` + `description` set on `<Base>`; no other meta props
3. `npm run build` succeeds; `site/dist/index.html` is generated
4. Homepage FAQ block has 5 Q&As as flat `<article class="faq-q"><h3><p></article>` markup (verified via `audit.sh --check homepage-faq`)
5. No `client:*` directives, no `<details>`, no anti-pattern phrases
6. Human visual parity check passes at desktop, 980px, 600px (Task 2 sign-off)
7. ROADMAP success criterion #1 is provably met for the rest of Phase 3 — no Plan 05+ work touches `index.astro`
</success_criteria>

<output>
After completion, create `.planning/phases/03-unique-pages/03-04-SUMMARY.md` documenting:
- The exact final `title` and `description` strings used
- Confirmation that 5 CheckDividers are in the specified positions
- Visual parity check outcome — date/time, breakpoints checked, PASS/FAIL with deltas
- Note that `dev-mockup-parity.astro` is intentionally NOT deleted in this plan (Plan 10 owns deletion per D-20 — homepage parity must be verified BEFORE the scratch-page reference is removed)
- For Plan 10 cleanup: the parity check is sign-off-recorded, so deletion is unblocked
</output>
