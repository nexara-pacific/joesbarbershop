---
phase: 03-unique-pages
plan: 11
type: execute
wave: 5
gap_closure: true
depends_on: [03-04, 03-01, 03-00]
files_modified:
  - site/src/pages/index.astro
autonomous: true
requirements: [PAGE-01]

must_haves:
  truths:
    - "Homepage Hero BLUF answers 'barbershop in Bostonia / El Cajon / East County' and does NOT verbatim repeat the full hours string 'Tue–Sat 10am–7:30pm' in body prose (hours are owned by /faq)"
    - "Homepage copy is differentiated from /about — mentions Joe Denesowicz by name only in brief reference form, not full bio"
    - "Homepage does not contain the full price board recitation in prose — prices tease in BLUF but the full board is the visual PriceBoard component"
    - "Banned phrases absent: 'we're more than a barbershop', 'experience the difference', 'discover', 'click here', 'we might'"
    - "audit.sh 18/18 PASS after edit (no AEO regression)"
    - "Astro type check exits 0 errors / 0 warnings"
  artifacts:
    - path: "site/src/pages/index.astro"
      provides: "Refreshed homepage with conversion-aware BLUF and hero copy"
      contains: "Joe's Barbershop"
  key_links:
    - from: "site/src/pages/index.astro"
      to: "site/src/components/FAQ.astro"
      via: "<FAQ /> component import"
      pattern: "import FAQ"
---

<objective>
Refresh homepage copy via marketing-skills:copywriting with cross-page fact ownership awareness. The homepage's BLUF and Hero copy currently repeat hours/prices verbatim in full — the same presentation used on niche-landing and FAQ. This pass differentiates the homepage angle: conversion-first (walk-in CTA, heritage identity anchor) rather than informational-first (full hours + full price recitation).

Purpose: The homepage owns the walk-in conversion angle and the heritage identity statement. /faq owns hours detail. /2026-east-county-barbershop-cost-guide owns the full price breakdown. Homepage copy should tease prices and hours, not duplicate them.

Output: Revised site/src/pages/index.astro — copy-only edits, no structural or CSS changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-unique-pages/03-CONTEXT.md
@.planning/phases/03-unique-pages/03-VERIFICATION.md
@.planning/phases/03-unique-pages/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invoke copywriting skill for homepage and apply refined copy</name>

  <read_first>
    1. site/src/pages/index.astro — the file being edited; understand current Hero component usage
    2. site/src/components/Hero.astro — Hero BLUF and h1 copy slots are in this component, NOT index.astro (index.astro is pure composition). Read to understand which copy is in the component vs the page.
    3. site/src/components/FAQ.astro — homepage FAQ Q&A text lives here; these are the 5 Q&As that may need differentiation
    4. .planning/phases/03-unique-pages/03-VERIFICATION.md — owned-vs-referenced fact map (load-bearing: homepage owns walk-in/cash-only stance, NOT hours detail or full price board)
    5. .agents/aeo-frame.md — per-page primary query for homepage: "barbershop in Bostonia / El Cajon / East County"; BLUF rule (first 100 words), 130–160-word capsule rule
    6. .agents/product-marketing-context.md — brand voice, banned phrases, conversion actions
    7. site/src/pages/faq.astro — read BLUF (lines 19–25) to understand what /faq owns (hours, cash-only detail) so homepage doesn't duplicate it verbatim
    8. site/src/pages/east-county-traditional-barbershop.astro — read BLUF to understand how that page uses hours/prices so homepage angle differs
  </read_first>

  <action>
    IMPORTANT: The homepage is pure composition — index.astro has no inline copy. Copy lives inside Hero.astro, FAQ.astro, Heritage.astro, Visit.astro, ClosingCTA.astro. You will need to edit the component files that hold copy, not index.astro itself. Confirm this by reading index.astro first.

    Step 1 — Map which components hold editable copy:
    - Hero.astro: h1 accent word, hero BLUF paragraph, hero meta line — EDITABLE
    - FAQ.astro: 5 Q&A pairs (h3 + p) — EDITABLE if differentiation is needed
    - Heritage.astro: heritage prose paragraph — EDITABLE if it duplicates /about copy verbatim
    - Visit.astro, ClosingCTA.astro: factual/structural — likely no edits needed

    Step 2 — Invoke Skill(skill="marketing-skills:copywriting") with this structured brief:

    Page: Homepage (/)
    Primary query: "barbershop in Bostonia / El Cajon / East County"
    AEO frame: .agents/aeo-frame.md
    Voice: .agents/product-marketing-context.md

    Owned facts (homepage owns these — must appear here):
    - Walk-in stance: "walk-ins always welcome" — hero CTA anchor
    - Cash-only as positioning: ATM on site, no booking app required
    - Heritage identity: checkerboard floors, Western/Victorian visual, strip-mall storefront
    - Rating proof point: 4.9★ as a trust anchor for first-timers

    Referenced facts (homepage should tease, not fully recite):
    - Hours: "Tue–Sat" is enough — /faq owns "10am to 7:30pm" full detail
    - Prices: "$30 cuts" is enough — /2026-east-county-barbershop-cost-guide owns the full board
    - Joe + Alex bios: one-line reference max — /about owns the full bios

    Current copy gap to fix:
    - Hero BLUF currently: likely recites full hours + full prices verbatim. Differentiate to conversion angle.
    - FAQ.astro Q&As: confirm they cover highest-homepage-value topics (walk-ins, cash, kids, timing, location) without duplicating /faq master verbatim
    - Heritage prose: if it duplicates /about Joe bio detail, shorten to a 1–2 sentence heritage statement

    Copy constraints (mandatory per D-01..D-05):
    - BLUF must still answer primary query in first 100 words (AEO Rule 1)
    - Entity-first declarative tone: "Joe's Barbershop is..." not "We are..."
    - No banned phrases: "we're more than a barbershop", "experience the difference", "discover", "click here to learn more", "we might", "could"
    - No Brooklyn-grooming-bro register, no luxury-spa framing
    - D-18 composition order is LOCKED — do NOT reorder components

    Step 3 — Apply skill output:
    - Edit only the specific copy strings inside each component
    - Hero.astro: update hero BLUF paragraph and/or h1 accent word if skill proposes differentiated copy
    - FAQ.astro: update any Q&A where the answer duplicates /faq master verbatim (use shorter, teaser-style answers that reference /faq for full detail)
    - Heritage.astro: if prose duplicates /about, trim to 1–2 sharp heritage-identity sentences
    - Do NOT change any CSS, layout, component composition, or structural HTML
    - Do NOT add new components or remove existing ones from index.astro

    Step 4 — Verify no banned phrases crept in:
    grep -iE "we're more than a barbershop|experience the difference|click here|we might|could potentially|discover" site/src/components/Hero.astro site/src/components/FAQ.astro site/src/components/Heritage.astro

    Step 5 — Build and run audits:
    cd site && npm run build
    bash .planning/phases/03-unique-pages/scripts/audit.sh
    cd site && npx --no-install astro check

    Step 6 — Diff check (structural guard):
    git diff site/src/pages/index.astro
    Confirm: zero lines changed in index.astro itself (all edits are in component files).
    git diff site/src/components/
    Confirm: only text strings changed, no CSS properties, no HTML element types, no class names.
  </action>

  <verify>
    <automated>
      cd /Users/darrelltang/dtconsulting/joesbarbershop/site && npm run build && bash /Users/darrelltang/dtconsulting/joesbarbershop/.planning/phases/03-unique-pages/scripts/audit.sh && npx --no-install astro check
    </automated>
  </verify>

  <acceptance_criteria>
    - `npm run build` exits 0
    - `bash audit.sh` (no-arg, all 18 checks) exits 0
    - `npx --no-install astro check` reports 0 errors / 0 warnings
    - Homepage BLUF does NOT contain the string "10am to 7:30pm" verbatim (hours detail owned by /faq): `grep -v "10am to 7:30pm" site/src/components/Hero.astro` returns the file content without that string
    - Homepage Hero BLUF first sentence starts with "Joe's Barbershop" (entity-first): `grep -m1 "strong" site/dist/index.html | grep -q "Joe's Barbershop"`
    - No banned phrases in edited component files: `grep -iE "we're more than a barbershop|experience the difference|click here|we might|could potentially" site/src/components/Hero.astro site/src/components/FAQ.astro site/src/components/Heritage.astro` exits 1 (no matches)
    - index.astro unchanged (pure composition preserved): `git diff --name-only site/src/pages/index.astro` returns empty
    - audit.sh --check homepage-faq passes (5 or 6 faq-q articles): `bash .planning/phases/03-unique-pages/scripts/audit.sh --check homepage-faq`
  </acceptance_criteria>

  <done>
    Homepage components (Hero.astro, and optionally FAQ.astro/Heritage.astro) have conversion-aware copy that owns the walk-in/cash-only/heritage angle without verbatim-repeating the full hours or full price board recitation from other pages. All audits pass. index.astro untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill output → .astro components | Skill-generated text is applied to component files. Risk: banned phrases or off-voice copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11-01 | Tampering | Hero.astro / FAQ.astro | mitigate | Grep gate for banned phrases before commit; structural diff confirms no CSS/layout changes |
| T-03-11-02 | Information Disclosure | none (static page, no PII) | accept | Static site, no user data, no trust boundary crossed |
</threat_model>

<verification>
After task completes:
- npm run build exits 0
- bash audit.sh (all checks) exits 0
- astro check exits 0 errors/0 warnings
- git diff shows only text string changes in component files, not index.astro
- No banned phrases present in edited files
</verification>

<success_criteria>
Homepage copy is differentiated: conversion-first (walk-in CTA, heritage identity, rating proof) rather than informational-first (full hours recitation, full price board recitation). AEO discipline maintained: BLUF still answers primary query, entity-first tone, no banned phrases.
</success_criteria>

<output>
After completion, create .planning/phases/03-unique-pages/03-11-SUMMARY.md following the summary template.
</output>
