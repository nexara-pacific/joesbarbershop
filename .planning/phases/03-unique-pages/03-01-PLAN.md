---
phase: 03-unique-pages
plan: 01
type: execute
wave: 1
depends_on: ["03-00"]
files_modified:
  - .agents/product-marketing-context.md
  - .agents/aeo-frame.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Marketing context document captures Joe's positioning, audience, tone, and anti-prompts"
    - "AEO frame document encodes the load-bearing AEO rules for downstream copywriting invocations"
    - "Both files exist at the repo root in `.agents/` (NOT under `site/`)"
  artifacts:
    - path: ".agents/product-marketing-context.md"
      provides: "12-section marketing context per marketing-skills:product-marketing-context SKILL.md"
      min_lines: 80
    - path: ".agents/aeo-frame.md"
      provides: "AEO load-bearing rules + per-page primary queries"
      min_lines: 40
  key_links:
    - from: ".agents/product-marketing-context.md"
      to: "inputs/00-brief.md + ~/Documents/DT Vault/1-projects/dt-consulting-llc/joes-barbershop-sandbox.md"
      via: "skill input ingestion"
      pattern: "Joe Denesowicz|Bostonia|East County"
    - from: ".agents/aeo-frame.md"
      to: "inputs/02-aeo-constraints.md"
      via: "skill encodes load-bearing rules"
      pattern: "BLUF|130-160|FAQ.*flat"
---

<objective>
Run the marketing-skills chain Step 1 + Step 2 to produce the two reference documents that every Wave 3 page-build plan reads. This plan is the bottleneck for Wave 3 — every per-page copywriting invocation auto-reads `.agents/product-marketing-context.md` and explicitly cites `.agents/aeo-frame.md`.

Purpose: Per CONTEXT D-01..D-04 + RESEARCH § Pattern 6, the marketing-skills chain mandates these two artifacts before any page-level copy is written. D-02 explicitly bans manual review between skill output and commit, so these two reference docs ARE the voice/quality control system. Get these right; the per-page copy follows.

Output: `.agents/product-marketing-context.md` (skill-generated) and `.agents/aeo-frame.md` (executor saves the in-context output of `marketing-skills:ai-seo` since that skill itself does not write a file per RESEARCH § Pattern 6 Step 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-unique-pages/03-CONTEXT.md
@.planning/phases/03-unique-pages/03-RESEARCH.md
@inputs/00-brief.md
@inputs/02-aeo-constraints.md

<!-- Skill SKILL.md files for verification (research already read these — executors do not need to re-read): -->
<!-- /Users/darrelltang/.claude/plugins/marketplaces/marketingskills/skills/product-marketing-context/SKILL.md -->
<!-- /Users/darrelltang/.claude/plugins/marketplaces/marketingskills/skills/ai-seo/SKILL.md -->

<interfaces>
<!-- Critical execution invariant from RESEARCH § Pitfall 1: -->
<!-- BOTH skills MUST be invoked from the repo root /Users/darrelltang/dtconsulting/joesbarbershop -->
<!-- NOT from site/. The skills resolve `.agents/` relative to cwd. -->
<!-- A `site/.agents/` directory is a Pitfall 1 failure — fix by `mv site/.agents .agents`. -->

<!-- Vault reference path (read-only input — do not write to vault): -->
<!-- ~/Documents/DT Vault/1-projects/dt-consulting-llc/joes-barbershop-sandbox.md -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run product-marketing-context skill</name>
  <files>.agents/product-marketing-context.md</files>
  <action>
**Working directory invariant:** Confirm `pwd` returns `/Users/darrelltang/dtconsulting/joesbarbershop` BEFORE invoking the skill. If `pwd` returns anything ending in `/site`, `cd ..` first.

Invoke the `marketing-skills:product-marketing-context` skill (per D-01 step 1) with explicit input pointing to BOTH source documents (D-03):
1. `inputs/00-brief.md` (audience + tone + visual cues + anti-prompts)
2. `~/Documents/DT Vault/1-projects/dt-consulting-llc/joes-barbershop-sandbox.md` (audit baseline + sandbox experiment log + strategic depth)

Both inputs are mandatory per D-03. Brief alone misses the strategic AEO depth (why this site exists, what queries it targets); audit baseline alone misses the visual / anti-prompt detail.

When the skill prompts for confirmation, provide it. Per D-02, no manual review gate — accept the skill's draft and let it write `.agents/product-marketing-context.md`.

The output document MUST contain the 12 sections documented in product-marketing-context/SKILL.md Step 3:
1. Product Overview
2. Target Audience
3. Personas
4. Problems & Pain Points
5. Competitive Landscape
6. Differentiation
7. Objections
8. Switching Dynamics
9. Customer Language
10. Brand Voice
11. Proof Points
12. Goals

The document MUST encode (verify by post-write grep — Pitfall 3):
- Audience: East County working-class Latino + Anglo, 25–65, family-friendly, walk-ins, cash-only
- Tone: heritage / no-frills / Western-Victorian
- Anti-prompts (banned voices): no Brooklyn-grooming-bro, no luxury-spa, no SaaS-coded
- Brand context: Joe Denesowicz + Alex, ESTD. 2020, strip-mall storefront, 4.9★ ratings
- Banned phrases: "we're more than a barbershop, we're a community", "experience the difference", "discover", "click here", "innovate", "streamline" (per UI-SPEC § Copywriting Contract)

If the skill output omits any of these or includes any banned phrase as approved language, halt commit per D-05 and ask user before proceeding.

**Anti-Pitfall 1 check:** After skill writes the file, verify `ls .agents/product-marketing-context.md` (repo root) returns the file. If the file appeared at `site/.agents/product-marketing-context.md` instead, `mv site/.agents .agents` and `rmdir site/.agents 2>/dev/null` to correct location.

Reference: D-01 step 1, D-02 (no manual review), D-03 (dual inputs), D-05 (halt on broken output), RESEARCH § Pattern 6 Step 1, Pitfall 1 (`.agents/` location), Pitfall 3 (banned phrases).
  </action>
  <verify>
    <automated>test -s .agents/product-marketing-context.md && grep -qiE "Bostonia|East County" .agents/product-marketing-context.md && grep -qiE "Joe Denesowicz|joe denesowicz" .agents/product-marketing-context.md && ! grep -iE "(we're more than a barbershop|experience the difference|click here|innovate|streamline)" .agents/product-marketing-context.md</automated>
  </verify>
  <done>File exists at `.agents/product-marketing-context.md` (repo root, NOT `site/.agents/`); contains the 12 documented sections; mentions Joe Denesowicz, Bostonia, East County, walk-ins, cash-only; does NOT contain any of the 6 banned phrases. `wc -l` returns ≥ 80 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Run ai-seo skill and save output to .agents/aeo-frame.md</name>
  <files>.agents/aeo-frame.md</files>
  <action>
**Working directory invariant:** Same as Task 1 — confirm `pwd` is the repo root before invoking.

Invoke the `marketing-skills:ai-seo` skill (per D-01 step 2 + D-04). The skill will auto-read `.agents/product-marketing-context.md` from Task 1 (per ai-seo/SKILL.md "Check for product marketing context first").

Pass explicit context to the skill: the load-bearing AEO rules in `inputs/02-aeo-constraints.md`. Tell the skill: "Encode these rules into a frame document the copywriting skill will consume per page. Plus add per-page primary queries from the table below."

**Per-page primary queries (D-01 step 2 — verbatim from CONTEXT.md):**

| Page | Primary query |
|------|---------------|
| `/` | "barbershop in Bostonia / El Cajon / East County" |
| `/east-county-traditional-barbershop` | "traditional barbershop East County" |
| `/2026-east-county-barbershop-cost-guide` | "how much does a barbershop cost in East County / El Cajon 2026" |
| `/about` | "who owns Joe's Barbershop in El Cajon" |
| `/reviews` | "Joe's Barbershop reviews / ratings" |
| `/faq` | "Joe's Barbershop hours, payment, walk-ins, kids cuts" |

**Critical detail per RESEARCH § Pattern 6 Step 2:** the ai-seo skill itself does NOT write a file — it produces in-conversation output. The executor MUST take that output and write it verbatim to `.agents/aeo-frame.md` using the Write tool. Do NOT skip this save step.

The saved frame document MUST encode (verify by post-write grep):
- BLUF first 100 words rule
- 130–160 word answer capsules per H2/H3
- Declarative tone — no "we" without entity context
- No tabs/accordions
- FAQ flat H3/p
- No text-as-image
- The 6 per-page primary queries above (each appears as plain text)

Per D-02, accept skill output without manual review. Per D-05, halt commit if output is obviously wrong (omits a primary query, contradicts AEO rules in `inputs/02-aeo-constraints.md`, or recommends banned anti-patterns).

Reference: D-01 step 2, D-04 (frame storage location is `.agents/aeo-frame.md`), RESEARCH § Pattern 6 Step 2 (executor writes the file, not the skill), Pitfall 1 (.agents/ at repo root).
  </action>
  <verify>
    <automated>test -s .agents/aeo-frame.md && grep -qiE "BLUF.*100|first 100 words" .agents/aeo-frame.md && grep -qiE "130.*160|130-160" .agents/aeo-frame.md && grep -q "traditional barbershop East County" .agents/aeo-frame.md && grep -q "barbershop in Bostonia" .agents/aeo-frame.md</automated>
  </verify>
  <done>File exists at `.agents/aeo-frame.md` (repo root); contains BLUF 100-word rule, 130-160 word capsule rule, all 6 per-page primary queries verbatim; ≥ 40 lines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill output → repo | skill-generated text gets committed; voice/factual drift is the primary risk per D-02 (no manual review gate) |
| vault → repo | `joes-barbershop-sandbox.md` is read-only input; nothing flows back to vault |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | T (Tampering) | skill output containing banned anti-patterns | mitigate | Post-write grep on each `.agents/*.md` file rejects: "we're more than a barbershop", "experience the difference", "click here", "innovate", "streamline". Per D-05, halt if found and ask user. |
| T-03-01-02 | I (Information disclosure) | `.agents/` committed to repo | accept | Both files contain marketing context (audience, voice) — no PII, no secrets. Public-shareable strategy doc. |
| T-03-01-03 | E (Elevation of privilege) | skill writes outside `.agents/` (Pitfall 1) | mitigate | Pre-skill `pwd` check ensures repo-root invocation. Post-write `ls .agents/<file>.md` confirms correct location; `mv` corrects if `.agents/` lands under `site/`. |
</threat_model>

<verification>
- `test -s .agents/product-marketing-context.md && test -s .agents/aeo-frame.md`
- `! test -d site/.agents` (Pitfall 1 — `.agents/` lives at repo root)
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-anti-patterns` returns pass (audit script does not scan `.agents/` by default; manual `grep -irE "(we're more than a barbershop|experience the difference|click here|innovate|streamline)" .agents/` returns 0 matches)
- `wc -l .agents/product-marketing-context.md` ≥ 80
- `wc -l .agents/aeo-frame.md` ≥ 40
- All 6 primary queries appear verbatim in `.agents/aeo-frame.md`
</verification>

<success_criteria>
1. Both `.agents/*.md` files exist at the repo root (NOT under `site/`)
2. `product-marketing-context.md` has the 12 documented sections + Joe/Bostonia/East County/walk-ins/cash-only mentions + anti-prompts
3. `aeo-frame.md` encodes BLUF/100-word, 130–160-word capsule, declarative-tone, no-tabs, flat-FAQ, no-text-as-image rules + the 6 per-page primary queries
4. Neither file contains a banned anti-pattern phrase as approved language
5. Wave 3 page-build plans can invoke `marketing-skills:copywriting` and rely on these two reference docs being present and authoritative
</success_criteria>

<output>
After completion, create `.planning/phases/03-unique-pages/03-01-SUMMARY.md` documenting:
- The exact paths of the two created files
- A two-sentence summary of each file's authoritative voice/positioning content (so downstream plans don't have to re-read both files in full)
- Any voice anchors or banned phrases the skill emphasized (so per-page copywriting invocations can cite them)
- Confirmation that `pwd` was the repo root during both skill invocations (Pitfall 1 audit trail)
</output>
