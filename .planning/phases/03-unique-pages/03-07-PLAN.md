---
phase: 03-unique-pages
plan: 07
type: execute
wave: 3
depends_on: ["03-00", "03-01"]
files_modified:
  - site/src/pages/about.astro
  - site/src/data/business.json
autonomous: true
requirements: ["PAGE-04"]

must_haves:
  truths:
    - "/about names Joe Denesowicz and Alex in plain text"
    - "Two CSS-only portrait placeholders carry data-pending-photo='joe' / data-pending-photo='alex' markers"
    - "BLUF answers primary query 'who owns Joe's Barbershop in El Cajon'"
    - "business.json _showcase_review_pending array has 4 new entries: about.bios.joe, about.bios.alex, about.portraits.joe, about.portraits.alex"
    - "Visit component reused from Phase 2 (already imports storefront photo + NAP)"
  artifacts:
    - path: "site/src/pages/about.astro"
      provides: "PAGE-04 — about page with Joe + Alex bios + portrait placeholders"
      min_lines: 100
      contains: "data-pending-photo"
    - path: "site/src/data/business.json"
      provides: "_showcase_review_pending entries for Phase 3 about-page swap needs"
      contains: "about.portraits.joe"
  key_links:
    - from: "site/src/pages/about.astro"
      to: "site/src/components/Visit.astro"
      via: "<Visit /> component import + render"
      pattern: "import Visit"
    - from: "site/src/pages/about.astro"
      to: ".agents/aeo-frame.md + .agents/product-marketing-context.md"
      via: "marketing-skills:copywriting reads at generation time"
      pattern: "Joe Denesowicz|Alex"
---

<objective>
Build the about page at `site/src/pages/about.astro` per D-06..D-07 + UI-SPEC § About Page Portrait Placeholder / § About Page Layout. Skill-drafted bios for Joe Denesowicz and Alex; CSS-only initials portrait placeholders carrying `data-pending-photo` markers for post-Phase-3 photo swap. Visit component (Phase 2) is reused. Append `_showcase_review_pending` entries to `business.json` documenting the bio + portrait swap needs.

Purpose: PAGE-04 satisfies ROADMAP success criterion #4 (`/about` names Joe Denesowicz and Alex in plain text). Person schema for both is deferred to Phase 5; Phase 3 ships only the visible bio prose + placeholder portraits.

Output: `about.astro` with article-shape composition, two `data-pending-photo` portrait blocks, skill-drafted bios, reused `<Visit />`, plus 4 new entries in `business.json._showcase_review_pending`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-unique-pages/03-CONTEXT.md
@.planning/phases/03-unique-pages/03-PATTERNS.md
@.planning/phases/03-unique-pages/03-UI-SPEC.md
@.planning/phases/03-unique-pages/03-VALIDATION.md
@inputs/02-aeo-constraints.md
@.agents/aeo-frame.md
@.agents/product-marketing-context.md
@site/src/pages/about.astro
@site/src/components/Heritage.astro
@site/src/components/Visit.astro
@site/src/components/ClosingCTA.astro
@site/src/layouts/Base.astro
@site/src/data/business.json

<!--
COMPOSITION (per D-22 + UI-SPEC § About Page Layout):
  Article header (H1 "About Joe's Barbershop" + dateModified)
  → BLUF capsule (~100 words: "Joe's Barbershop is owned by Joe Denesowicz, who opened the shop in 2020...")
  → Joe section: 2-col grid (portrait placeholder LEFT + bio RIGHT, grid-template-columns: 1fr 1.4fr)
  → Alex section: 2-col grid mirrored (bio LEFT + portrait placeholder RIGHT)
  → <Visit /> (existing component — already imports storefront photo + NAP)
  → ClosingCTA

PRIMARY QUERY (D-01 step 2):
  "who owns Joe's Barbershop in El Cajon"

PORTRAIT PLACEHOLDERS (D-07 + UI-SPEC § About Page Portrait Placeholder):
  - <div class="portrait-placeholder" data-pending-photo="joe">
      <span class="initials">JD</span>
      <span class="pending">PORTRAIT TO COME</span>
    </div>
  - <div class="portrait-placeholder" data-pending-photo="alex">
      <span class="initials">A</span>
      <span class="pending">PORTRAIT TO COME</span>
    </div>
  Aspect ratio 4:5; bg var(--fg); initials in var(--bg) cream-on-dark; pending tag in oklch(72% 0.04 80) Oswald uppercase.

UI-SPEC sections that govern this page:
- § Article Header
- § BLUF Capsule
- § About Page Portrait Placeholder
- § About Page Layout
- § Section Spacing Rhythm
- § Copywriting Contract (banned phrases, eyebrow text "The shop · the barbers")

ANALOG: Heritage.astro (.heritage-grid 2-col image+prose layout — exact pattern to mirror)
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run marketing-skills:copywriting for Joe + Alex bios + BLUF</name>
  <files>(no file write — produces in-context output for Task 2)</files>
  <action>
**Working directory invariant:** `pwd` returns the repo root.

Invoke `marketing-skills:copywriting` skill with:

1. **Page primary query** (D-01 step 2): `"who owns Joe's Barbershop in El Cajon"`
2. **Section requirements:**
   - **BLUF capsule** (80–110 words, entity-first, first sentence in `<strong>`): "Joe's Barbershop is owned by Joe Denesowicz, who opened the shop in 2020 in Bostonia, El Cajon. Lead barber Alex cuts alongside Joe at three chairs in a strip-mall storefront on Bradley Avenue..." Banned: "we're more than a barbershop", "experience the difference", "discover", "click here", "we might", "could", "innovate", "streamline", "passionate" (vague), "craft" (Brooklyn-coded).
   - **Joe bio** (per D-06 — 2-3 paragraphs, ~120-200 words): owner since 2020; East County native or longtime resident framing (the audit baseline supports "longtime East County" — defer to skill output if specifics differ; do NOT fabricate hometown); heritage-barbershop framing (Western/Victorian brand lineage from `.agents/product-marketing-context.md`); working-class East County positioning; $30 base price as value philosophy. **Conservative on biographical specifics** — only assert what's in the brief / vault baseline. If skill output adds specifics not in source artifacts (e.g., "Joe trained in Chicago in 1995"), flag for user before commit per D-05.
   - **Alex bio** (per D-06 — 2-3 paragraphs, **conservative-and-minimal**, ~80-150 words): "Alex is the lead barber alongside Joe…" — biographical specifics MINIMAL until Joe confirms at showcase. Bio is plausible-but-conservative. Skill should produce a bio that is FACTUALLY ACCURATE without fabricated career history, training credentials, or personal details. If skill output adds specifics, flag for user before commit.
3. **Explicit citation:** "Use guidance in `.agents/aeo-frame.md` and brand context in `.agents/product-marketing-context.md`. The brief's anti-prompts (no Brooklyn-grooming-bro, no luxury-spa, no SaaS-coded) apply. Bio voice is heritage / no-frills / Western-Victorian."

**Skill output review (D-02 + D-05 + D-06 boundary):**
- D-02: no manual review gate, BUT
- D-05: halt commit if obviously wrong
- D-06: bios are skill-drafted; specifics minimal pending Joe's confirmation. ANY biographical claim not directly traceable to `inputs/00-brief.md` or `~/Documents/DT Vault/1-projects/dt-consulting-llc/joes-barbershop-sandbox.md` is a hallucination — flag and ask user.

**Banned-phrase pre-check:** scan skill output for the standard banned list + "passionate", "craft" (overused luxury-positioning words; UI-SPEC § Copywriting Contract anti-prompts).

**Hallucination pre-check (critical for /about):** verify EVERY factual claim against source artifacts. Specifically:
- Joe's last name: Denesowicz (per CONTEXT D-06)
- Year established: 2020
- Lead barber name: Alex (no last name disclosed)
- Location: Bostonia, El Cajon
- Three chairs (per `.agents/product-marketing-context.md` brand context)
- Strip-mall storefront on Bradley Avenue
- $30 base haircut

Any specifics OUTSIDE this list (e.g., schools attended, prior shops, family details) ARE HALLUCINATIONS unless skill cites the source.

Reference: D-06, D-07, D-22, D-02, D-05, RESEARCH § Pattern 6 Step 3, Pitfall 3 (banned phrases), CONTEXT § Specifics (about page is high-stakes for factual accuracy).
  </action>
  <verify>
    <automated>test -s .agents/aeo-frame.md &amp;&amp; test -s .agents/product-marketing-context.md</automated>
  </verify>
  <done>Skill produced BLUF + Joe bio + Alex bio. No banned phrases. All factual claims traceable to source artifacts; no hallucinated biographical specifics. Bios stay heritage/no-frills voice — not Brooklyn-grooming-bro / luxury-spa / SaaS-coded.</done>
</task>

<task type="auto">
  <name>Task 2: Create about.astro + append pending markers to business.json</name>
  <files>site/src/pages/about.astro, site/src/data/business.json</files>
  <action>
**Step 1: Rewrite `site/src/pages/about.astro`** (Phase 1 placeholder gets replaced).

**Frontmatter:**

```astro
--- // (astro frontmatter)
import Base from '../layouts/Base.astro';
import Visit from '../components/Visit.astro';
import ClosingCTA from '../components/ClosingCTA.astro';
import { business } from '../data/business';

const lastUpdated = 'May 2026';
--- // (astro frontmatter)
```

**Body** (per UI-SPEC § About Page Layout + § About Page Portrait Placeholder + PATTERNS.md § about.astro):

```astro
<Base
  title="About Joe Denesowicz · Joe's Barbershop"
  description="Joe Denesowicz opened Joe's Barbershop in 2020 in Bostonia, El Cajon. Lead barber Alex cuts alongside him. Traditional barbering, walk-ins welcome."
>
  <header class="article-head">
    <div class="wrap">
      <span class="eyebrow kicker-rule">The shop · the barbers</span>
      <h1>About Joe's Barbershop.</h1>
      <p class="datestamp">UPDATED {lastUpdated.toUpperCase()}</p>
    </div>
  </header>

  <section class="bluf" aria-label="Answer capsule">
    <div class="wrap">
      <p class="bluf-lead">
        <strong>[Skill BLUF first sentence in <strong>]</strong>
        [Skill BLUF rest, ~80-110 words]
      </p>
    </div>
  </section>

  <section class="bio-section bio-joe">
    <div class="wrap">
      <div class="bio-grid">
        <div class="portrait-placeholder" data-pending-photo="joe">
          <span class="initials">JD</span>
          <span class="pending">PORTRAIT TO COME</span>
        </div>
        <div class="bio-copy">
          <span class="eyebrow kicker-rule">Owner · ESTD. 2020</span>
          <h2>Joe Denesowicz</h2>
          <p>[Skill Joe bio paragraph 1]</p>
          <p>[Skill Joe bio paragraph 2]</p>
          <!-- optional 3rd paragraph if skill produces it -->
        </div>
      </div>
    </div>
  </section>

  <section class="bio-section bio-alex">
    <div class="wrap">
      <div class="bio-grid bio-grid-mirrored">
        <div class="bio-copy">
          <span class="eyebrow kicker-rule">Lead barber</span>
          <h2>Alex</h2>
          <p>[Skill Alex bio paragraph 1]</p>
          <p>[Skill Alex bio paragraph 2]</p>
        </div>
        <div class="portrait-placeholder" data-pending-photo="alex">
          <span class="initials">A</span>
          <span class="pending">PORTRAIT TO COME</span>
        </div>
      </div>
    </div>
  </section>

  <Visit />
  <ClosingCTA />
</Base>
```

**Scoped CSS** — copy from PATTERNS.md § about.astro CSS blocks + UI-SPEC § About Page Portrait Placeholder. Required selectors:
- `.article-head`, `.bluf`, `.bluf-lead` (same as niche-landing)
- `.bio-section` (background `var(--surface)` for Joe, `var(--bg)` for Alex — alternating; padding `clamp(56px, 7vw, 96px) 0`)
- `.bio-grid` (`display: grid; grid-template-columns: 1fr 1.4fr; gap: clamp(28px, 4vw, 56px); align-items: center;`)
- `.bio-grid-mirrored` (`grid-template-columns: 1.4fr 1fr;` — bio left, portrait right)
- `@media (max-width: 980px)`: `.bio-grid { grid-template-columns: 1fr; }` AND `.bio-grid-mirrored { grid-template-columns: 1fr; }` AND on mirrored, ensure portrait still appears below bio (or above — mockup-consistent; PATTERNS.md says stack works either way as long as visual rhythm is preserved)
- `.portrait-placeholder` (UI-SPEC verbatim: `aspect-ratio: 4 / 5; background: var(--fg); border: 1px solid var(--border);` + flexbox center)
- `.portrait-placeholder .initials` (IM Fell, `clamp(60px, 8vw, 96px)`, weight 600, `color: var(--bg)`)
- `.portrait-placeholder .pending` (Oswald uppercase 0.18em tracking, 11px weight 600, `color: oklch(72% 0.04 80)`)
- `.bio-copy h2` (IM Fell, `clamp(28px, 2.6vw, 38px)`, weight 600, line-height 1.05, margin `0 0 18px`)
- `.bio-copy p` (Newsreader 17px, line-height 1.65, max-width 60ch, margin `0 0 18px`)

**Critical structural invariants:**
1. BLUF first under `<main>` after article header.
2. Two `data-pending-photo` markers — one for joe (initials "JD"), one for alex (initials "A").
3. NO `<Image>` import, NO photo references for Joe or Alex (D-07 — placeholders only).
4. `<Visit />` reused from Phase 2 unchanged (it imports storefront photo + NAP itself).
5. NO `<Hero />`, `<FactStrip />`, `<PriceBoard />`, `<Heritage />` (D-17).
6. NO `client:*`, NO `<details>`, NO accordion markup.
7. Plain-text mentions of "Joe Denesowicz" and "Alex" appear at minimum once each in DOM (ROADMAP success criterion #4).

**Step 2: Append 4 entries to `business.json._showcase_review_pending` array.**

Read `business.json`. Append these 4 strings to the `_showcase_review_pending` array (preserving existing entries):

```
"about.bios.joe — skill-drafted bio committed; specifics minimal until Joe confirms biographical details at showcase",
"about.bios.alex — skill-drafted minimal bio committed; specifics deferred to Joe's confirmation at showcase",
"about.portraits.joe — placeholder rendered as JD initials card; replace with real headshot post-Phase-3",
"about.portraits.alex — placeholder rendered as A initial card; replace with real headshot post-Phase-3"
```

Validate JSON after edit: `node -e "JSON.parse(require('fs').readFileSync('site/src/data/business.json'))"` exits 0.

**Atomic commit per D-05:** one commit `feat(03-07): build /about page with Joe + Alex bios and portrait placeholders`.

Reference: D-06, D-07, D-22, UI-SPEC § About Page Portrait Placeholder / § About Page Layout, PATTERNS.md § about.astro, RESEARCH § Pattern 5.
  </action>
  <verify>
    <automated>cd site &amp;&amp; npm run build &amp;&amp; cd .. &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check about-staff-names &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check about-pending-photos &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-anti-patterns &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-accordions &amp;&amp; bash .planning/phases/03-unique-pages/scripts/audit.sh --check bluf-position &amp;&amp; node -e "const b=require('./site/src/data/business.json'); const m=b._showcase_review_pending.filter(x => x.startsWith('about.')); process.exit(m.length === 4 ? 0 : 1)"</automated>
  </verify>
  <done>`npm run build` exits 0; `site/dist/about/index.html` is generated; `audit.sh --check about-staff-names` returns "Joe Denesowicz" + "Alex" both present; `audit.sh --check about-pending-photos` returns 2 (one joe, one alex); `audit.sh --check no-anti-patterns / --check no-accordions / --check no-client-directives` all pass; `business.json._showcase_review_pending` has 4 new `about.*` entries; manual spot-read confirms bios stay within source-traceable specifics (no hallucinated background) and Visit component renders below the Alex section.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill output → bio markup | bios are first-person factual claims about real people; hallucination risk is highest on this page |
| portrait placeholder → photo swap pipeline | `data-pending-photo` markers tag swap targets; post-Phase-3 swap pass replaces the entire div |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | T (Tampering) | hallucinated biographical specifics about Joe or Alex | mitigate | Task 1 hallucination pre-check enumerates the only sourced facts (last name, year, location, role, three chairs, $30 base). Anything beyond this list flagged before commit per D-05. Task 2 commit message records "specifics minimal pending showcase." |
| T-03-07-02 | I (Information disclosure) | committing placeholder bio with `[Reviewer name pending]`-style markup left in production HTML | mitigate | Task 2 done criteria includes manual spot-read; built HTML pre-commit grep `! grep -E '\[.*pending.*\]' site/dist/about/index.html` would catch unfilled placeholders (no audit.sh check for this since the assertion is presence-of-actual-prose). |
| T-03-07-03 | I (Information disclosure) | accidentally including vault-only audit baseline content | mitigate | `product-marketing-context` skill output is the abstraction layer. Bios cite voice and positioning, not raw audit notes. |
| T-03-07-04 | T (Tampering) | banned anti-pattern phrases ("we're more than a barbershop", "passionate craft", etc.) | mitigate | `audit.sh --check no-anti-patterns` part of Task 2 verify command. Task 1 explicit banned list includes "passionate" and "craft" (luxury-spa-coded). |
| T-03-07-05 | E (Elevation of privilege) | `data-pending-photo` markers misplaced — landing on `<span>` instead of outer container | mitigate | RESEARCH § Pattern 5 (line ~415) explicitly: "marker lives on the OUTER `<div>`, NOT the `<span class='initials'>`." Pattern enforced by Task 2's verbatim markup template. |
</threat_model>

<verification>
- `cd site && npm run build` exits 0
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check about-staff-names` passes
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check about-pending-photos` returns 2
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-anti-patterns` passes
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-accordions` passes
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check no-client-directives` passes
- `bash .planning/phases/03-unique-pages/scripts/audit.sh --check bluf-position` passes
- `grep 'data-pending-photo="joe"' site/dist/about/index.html` AND `grep 'data-pending-photo="alex"' site/dist/about/index.html` both match
- `business.json._showcase_review_pending` has 4 new `about.*` entries
- `business.json` is valid JSON
- Hallucination spot-check: every biographical claim in bios traces to `inputs/00-brief.md` or `~/Documents/DT Vault/1-projects/dt-consulting-llc/joes-barbershop-sandbox.md`
</verification>

<success_criteria>
1. `site/src/pages/about.astro` exists with article-shape composition + 2-col bio sections + Visit + ClosingCTA
2. Plain-text mentions of "Joe Denesowicz" and "Alex" appear in DOM (ROADMAP success criterion #4)
3. Two `data-pending-photo` portrait placeholders (joe + alex), CSS-only, IM Fell initials, Oswald "PORTRAIT TO COME" tag
4. Bios are skill-drafted; no hallucinated biographical specifics; no banned phrases
5. Visit component reused from Phase 2 (no modification)
6. `business.json._showcase_review_pending` has 4 new `about.*` entries
7. NO Hero/FactStrip/PriceBoard/Heritage; NO client:*; NO accordions; NO scoped JS
</success_criteria>

<output>
After completion, create `.planning/phases/03-unique-pages/03-07-SUMMARY.md` documenting:
- Skill output sources cited
- BLUF first-sentence strong-wrap text
- Word counts for Joe bio + Alex bio
- Hallucination spot-check log: enumerate every factual claim made in either bio + the source it traces to
- The 4 new `business.json._showcase_review_pending` entries
- Confirmation that Visit component renders unchanged below the Alex section
- For Plan 10 (cleanup + verification): the 4 `about.*` pending markers track the Phase 3 swap surface for post-showcase
</output>
