---
phase: 05-aeo-performance-meta
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - site/src/pages/index.astro
  - site/src/pages/about.astro
  - site/src/pages/reviews.astro
  - site/src/pages/faq.astro
  - site/src/pages/east-county-traditional-barbershop.astro
  - site/src/pages/2026-east-county-barbershop-cost-guide.astro
  - site/src/pages/[service].astro
  - site/src/pages/[neighborhood]-barber.astro
autonomous: true
requirements: [AEO-02, AEO-03, AEO-04, AEO-05, AEO-06, AEO-07, AEO-08, PERF-03]
must_haves:
  truths:
    - "Homepage emits AggregateRating + FAQPage JSON-LD via <Fragment slot=\"head\">"
    - "/about emits Person JSON-LD for Joe Denesowicz"
    - "/reviews emits Review[] JSON-LD from reviews.json"
    - "/faq emits FAQPage JSON-LD flattened across category groups"
    - "Niche-landing emits Article + FAQPage with ogType='article' (placeholder dateModified — Plan 06 wires real value)"
    - "Cost guide emits Article + FAQPage with ogType='article' (placeholder dateModified — Plan 06 wires real value)"
    - "Each service page emits Service + FAQPage JSON-LD"
    - "Each neighborhood page emits FAQPage JSON-LD"
    - "Hero fetchpriority='high' preserved on homepage; service + neighborhood hero images get priority prop per D-20"
    - "AEO-07 — no <img alt='...'> contains price or service words (check_text_as_image baseline stub passes)"
    - "npm run build exits 0"
  artifacts:
    - path: "site/src/pages/index.astro"
      provides: "Homepage with HairSalon (auto) + AggregateRating + FAQPage"
      contains: "AggregateRating"
    - path: "site/src/pages/about.astro"
      provides: "About page with Person (Joe)"
      contains: "Person"
    - path: "site/src/pages/reviews.astro"
      provides: "Reviews page with Review[]"
      contains: "<Review"
    - path: "site/src/pages/faq.astro"
      provides: "FAQ master with FAQPage"
      contains: "FAQPage"
    - path: "site/src/pages/east-county-traditional-barbershop.astro"
      provides: "Niche-landing with Article + FAQPage; ogType article"
      contains: "Article"
    - path: "site/src/pages/2026-east-county-barbershop-cost-guide.astro"
      provides: "Cost guide with Article + FAQPage; ogType article"
      contains: "Article"
    - path: "site/src/pages/[service].astro"
      provides: "6 service pages with Service + FAQPage"
      contains: "Service"
    - path: "site/src/pages/[neighborhood]-barber.astro"
      provides: "5 neighborhood pages with FAQPage"
      contains: "FAQPage"
  key_links:
    - from: "page files"
      to: "site/src/components/schema/*.astro"
      via: "Fragment slot=\"head\""
      pattern: "slot=\"head\""
    - from: "Article-emitting pages"
      to: "git-mtimes.json (Plan 06)"
      via: "TODO Plan 06 placeholder dateModified"
      pattern: "TODO Plan 06"
---

<objective>
Thread the six page-specific schema components into the 17 routes via Astro's `<Fragment slot="head">` pattern (first consumption of the slot Phase 2 D-26 reserved). After this plan every page emits the auto-injected HairSalon (Plan 04) AND its page-archetype-appropriate overlay schemas. Also verify hero `fetchpriority="high"` per PERF-03 / D-20.

Purpose: Schema components exist (Plan 03) and Base.astro auto-emits HairSalon (Plan 04). This plan attaches each page's specific schema(s). Article pages (niche-landing + cost guide) use a placeholder `dateModified` value — Plan 06 swaps in the real git-mtime value once the manifest exists.

Output: 8 page files modified; every existing body element preserved (FAQs may be refactored from inline JSX to `faqs.map()` over an array constant, but the rendered DOM remains identical).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-aeo-performance-meta/05-CONTEXT.md
@.planning/phases/05-aeo-performance-meta/05-RESEARCH.md
@.planning/phases/05-aeo-performance-meta/05-PATTERNS.md
@site/src/components/schema/HairSalon.astro
@site/src/components/schema/AggregateRating.astro
@site/src/components/schema/FAQPage.astro
@site/src/components/schema/Service.astro
@site/src/components/schema/Person.astro
@site/src/components/schema/Article.astro
@site/src/components/schema/Review.astro
@site/src/pages/index.astro
@site/src/pages/about.astro
@site/src/pages/reviews.astro
@site/src/pages/faq.astro
@site/src/pages/east-county-traditional-barbershop.astro
@site/src/pages/2026-east-county-barbershop-cost-guide.astro
@site/src/pages/[service].astro
@site/src/pages/[neighborhood]-barber.astro
@site/src/components/FAQ.astro
@site/src/components/Hero.astro
@site/src/data/reviews.json

<interfaces>
Schema component prop signatures (from Plan 03):

`<HairSalon />` — no props (auto-emitted by Base.astro on every page)
`<AggregateRating />` — no props
`<FAQPage faqs={Array<{q,a}>} pageUrl={string} />`
`<Service entry={CollectionEntry<'services'>} pageUrl={string} />`
`<Person name jobTitle description slug />` (all strings)
`<Article headline description url datePublished dateModified authorName />` (all strings)
`<Review reviews={ReviewItem[]} />` where `ReviewItem = { name, rating, quote, source, date }`

Astro `<Fragment slot="head">` pattern:
```astro
<Base title="..." description="...">
  <Fragment slot="head">
    <SchemaA ... />
    <SchemaB ... />
  </Fragment>
  <!-- body -->
</Base>
```

ogType pattern: `article` for /east-county-traditional-barbershop and /2026-east-county-barbershop-cost-guide; default `website` everywhere else.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire schema overlays into 4 unique pages (index, about, reviews, faq)</name>
  <read_first>
    - site/src/pages/index.astro
    - site/src/pages/about.astro
    - site/src/pages/reviews.astro
    - site/src/pages/faq.astro
    - site/src/components/FAQ.astro (homepage FAQ hard-codes 5 Q&As — needed for homepageFaqs constant)
    - site/src/data/reviews.json (ReviewItem field names)
    - .planning/phases/05-aeo-performance-meta/05-PATTERNS.md (lines 777-924)
  </read_first>
  <files>site/src/pages/index.astro, site/src/pages/about.astro, site/src/pages/reviews.astro, site/src/pages/faq.astro</files>
  <action>
**index.astro** — add AggregateRating + FAQPage.

Read `site/src/components/FAQ.astro` and extract the 5 Q&A pairs into a `homepageFaqs` constant in `index.astro` frontmatter (verbatim text; strip any HTML tags from answers). This is a deliberate two-place duplication for Plan 05 scope (Phase 7 may consolidate).

Frontmatter additions:
```astro
import Base from '../layouts/Base.astro';
import AggregateRating from '../components/schema/AggregateRating.astro';
import FAQPage from '../components/schema/FAQPage.astro';
// ... existing imports unchanged ...

const homepageFaqs = [
  { q: '<Q1 verbatim from FAQ.astro>', a: '<A1 plain text>' },
  // ... 4 more from FAQ.astro
];
```

Body wrap:
```astro
<Base title="..." description="...">
  <Fragment slot="head">
    <AggregateRating />
    <FAQPage faqs={homepageFaqs} pageUrl={Astro.url.href} />
  </Fragment>
  <!-- existing body unchanged -->
</Base>
```

Preserve existing title and description. If existing title is the simple form (current `Joe's Barbershop — Bostonia, El Cajon` or similar), leave alone — homepage is exempt from D-13's pipe pattern.

---

**about.astro** — add Person for Joe.

Frontmatter additions:
```astro
import Person from '../components/schema/Person.astro';

const joeDescription = "Joe Denesowicz opened Joe's Barbershop in 2020 in Bostonia, El Cajon. Owner and barber for the shop.";
```

Body wrap:
```astro
<Base title="About Joe Denesowicz | Joe's Barbershop" description="..." variant="article">
  <Fragment slot="head">
    <Person
      name="Joe Denesowicz"
      jobTitle="Owner / Barber"
      description={joeDescription}
      slug="joe-denesowicz"
    />
  </Fragment>
  <!-- existing body unchanged -->
</Base>
```

No second `<Person />` for Alex (D-09). If current title already uses pipe form, leave it; if it uses middle-dot (`·`), keep middle-dot — D-13 allows planner discretion on existing separators.

---

**reviews.astro** — add Review[] from existing reviews array.

Read `site/src/data/reviews.json` to confirm the field names (likely `name`, `rating`, `quote`, `source`, `date` — but could be `author`, `stars`, `text`, etc. Read the file first).

Frontmatter additions:
```astro
import Review from '../components/schema/Review.astro';
```

Body wrap:
```astro
<Base title="Reviews | Joe's Barbershop" description="..." variant="article">
  <Fragment slot="head">
    <Review reviews={sortedReviews} />
  </Fragment>
  <!-- existing body unchanged -->
</Base>
```

If `sortedReviews` field names don't match the `ReviewItem` interface in `Review.astro` (`name`, `rating`, `quote`, `source`, `date`), remap inline:
```astro
<Review reviews={sortedReviews.map((r) => ({ name: r.author, rating: r.stars, quote: r.text, source: r.platform, date: r.published }))} />
```
Use the real field names from `reviews.json`.

---

**faq.astro** — add FAQPage with all 14 Q&As flattened across the 5 category groups.

If the current page renders FAQs as inline JSX (read the file to confirm), refactor into a `faqGroups` array in frontmatter, then derive `allFaqs`:
```astro
import FAQPage from '../components/schema/FAQPage.astro';

const faqGroups = [
  {
    category: 'Hours & Days',
    items: [
      { q: '...', a: '...' },
      // ...
    ],
  },
  // ... 4 more groups
];

const allFaqs = faqGroups.flatMap((g) => g.items);
```

Refactor body to render `{faqGroups.map((g) => (<section><h2>{g.category}</h2>{g.items.map(...)}</section>))}`. Preserve all existing CSS class names and tag structure.

Body wrap:
```astro
<Base title="FAQ | Joe's Barbershop" description="...">
  <Fragment slot="head">
    <FAQPage faqs={allFaqs} pageUrl={Astro.url.href} />
  </Fragment>
  <!-- body with refactored faqGroups.map() -->
</Base>
```

If refactoring the body proves too invasive, take Option B from PATTERNS line 906: leave existing DOM, create a parallel `allFaqs` constant mirroring the DOM Q&As. Mark with `// TODO Phase 7: consolidate DOM ↔ schema array duplication`.

---

After all 4 edits:
```bash
cd site && npm run build
grep -q '"@type":"AggregateRating"' dist/index.html
grep -q '"@type":"FAQPage"' dist/index.html
grep -q '"@type":"Person"' dist/about/index.html
grep -q '"@type":"Review"' dist/reviews/index.html
grep -q '"@type":"FAQPage"' dist/faq/index.html
```
  </action>
  <verify>
    <automated>cd /Users/darrelltang/dtconsulting/joesbarbershop/site && npm run build > /tmp/build.log 2>&1 && grep -q "import AggregateRating" src/pages/index.astro && grep -q "import FAQPage" src/pages/index.astro && grep -q "import Person" src/pages/about.astro && grep -q "Joe Denesowicz" src/pages/about.astro && grep -q "import Review" src/pages/reviews.astro && grep -q "import FAQPage" src/pages/faq.astro && grep -q '"@type":"AggregateRating"' dist/index.html && grep -q '"@type":"FAQPage"' dist/index.html && grep -q '"@type":"Person"' dist/about/index.html && grep -q '"@type":"Review"' dist/reviews/index.html && grep -q '"@type":"FAQPage"' dist/faq/index.html && grep -q '"@type":"HairSalon"' dist/index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - index.astro imports AggregateRating + FAQPage; body has Fragment slot="head" containing both
    - about.astro imports Person; emits one Person with `name="Joe Denesowicz"`, `slug="joe-denesowicz"`
    - reviews.astro imports Review; passes the existing sortedReviews (with remapping if field names differ)
    - faq.astro imports FAQPage; passes a flat allFaqs array
    - Built dist/index.html contains BOTH `"@type":"HairSalon"` (from Plan 04 auto-inject) AND `"@type":"AggregateRating"` AND `"@type":"FAQPage"`
    - Built dist/about/index.html contains `"@type":"Person"` AND `"Joe Denesowicz"`
    - Built dist/reviews/index.html contains `"@type":"Review"`
    - Built dist/faq/index.html contains `"@type":"FAQPage"`
    - npm run build exits 0
  </acceptance_criteria>
  <done>4 unique pages emit their schema overlays; HairSalon still auto-emits on each. Per-page meta from Base.astro still flowing correctly (title + description + canonical + OG + Twitter).</done>
</task>

<task type="auto">
  <name>Task 2: Wire Article + FAQPage into niche-landing + cost guide (placeholder dateModified)</name>
  <read_first>
    - site/src/pages/east-county-traditional-barbershop.astro
    - site/src/pages/2026-east-county-barbershop-cost-guide.astro
    - site/src/components/schema/Article.astro
    - .planning/phases/05-aeo-performance-meta/05-PATTERNS.md (lines 925-996)
    - .planning/phases/05-aeo-performance-meta/05-CONTEXT.md (D-11 visible Last updated; D-15 ogType article)
  </read_first>
  <files>site/src/pages/east-county-traditional-barbershop.astro, site/src/pages/2026-east-county-barbershop-cost-guide.astro</files>
  <action>
For both Article pages: refactor inline FAQ JSX into a `faqs` array, add Article + FAQPage in `<Fragment slot="head">`, set `ogType="article"`. Use a **placeholder ISO date** for `dateModified` — Plan 06 swaps for git-mtime. Mark with a `// TODO Plan 06` comment.

---

**east-county-traditional-barbershop.astro**

Frontmatter additions (preserve existing imports):
```astro
import Article from '../components/schema/Article.astro';
import FAQPage from '../components/schema/FAQPage.astro';

// TODO Plan 06: replace with mtimes['site/src/pages/east-county-traditional-barbershop.astro']
const dateModified = '2026-05-10T00:00:00-07:00';
const datePublished = '2026-05-08';

const faqs = [
  // Extract from current DOM FAQ section (lines 74-115 approximately).
  // Each item: { q: 'question verbatim', a: 'answer plain text' }
];
```

Body wrap:
```astro
<Base
  title="Traditional barbershop · East County · Joe's Barbershop"
  description="..."
  variant="article"
  ogType="article"
>
  <Fragment slot="head">
    <Article
      headline="What 'traditional barbershop' means in East County."
      description="<~150 char summary of page content>"
      url={Astro.url.href}
      datePublished={datePublished}
      dateModified={dateModified}
      authorName="Joe Denesowicz"
    />
    <FAQPage faqs={faqs} pageUrl={Astro.url.href} />
  </Fragment>
  <!-- existing body with FAQ section refactored to {faqs.map(...)} -->
</Base>
```

Refactor only the FAQ section. Use the same `<article class="faq-q">` markup pattern from Phase 3 D-19. Preserve every other body element including the entire `<style>` block.

---

**2026-east-county-barbershop-cost-guide.astro**

Frontmatter additions:
```astro
import Article from '../components/schema/Article.astro';
import FAQPage from '../components/schema/FAQPage.astro';

// TODO Plan 06: replace with mtimes['site/src/pages/2026-east-county-barbershop-cost-guide.astro']
const dateModified = '2026-05-10T00:00:00-07:00';
const datePublished = '2026-05-08';

const faqs = [
  // Extract 4 items from current DOM (FAQ section, lines 100-127 approximately).
];
```

Body wrap:
```astro
<Base
  title="2026 East County Barbershop Cost Guide | Joe's Barbershop"
  description="..."
  variant="article"
  ogType="article"
>
  <Fragment slot="head">
    <Article
      headline="2026 East County Barbershop Cost Guide"
      description="..."
      url={Astro.url.href}
      datePublished={datePublished}
      dateModified={dateModified}
      authorName="Darrell Tang"
    />
    <FAQPage faqs={faqs} pageUrl={Astro.url.href} />
  </Fragment>
  <!-- body with refactored faqs.map for FAQ section -->
</Base>
```

`authorName` is `Darrell Tang` for the cost guide (playbook listicle author); `Joe Denesowicz` for the niche-landing (Joe's voice). Both are defensible.

Preserve all CSS + non-FAQ body content verbatim.

---

After both edits:
```bash
cd site && npm run build
grep -q '"@type":"Article"' dist/east-county-traditional-barbershop/index.html
grep -q '"@type":"FAQPage"' dist/east-county-traditional-barbershop/index.html
grep -q '"@type":"Article"' dist/2026-east-county-barbershop-cost-guide/index.html
grep -q '"@type":"FAQPage"' dist/2026-east-county-barbershop-cost-guide/index.html
grep -q 'property="og:type" content="article"' dist/east-county-traditional-barbershop/index.html
grep -q 'property="og:type" content="article"' dist/2026-east-county-barbershop-cost-guide/index.html
```
  </action>
  <verify>
    <automated>cd /Users/darrelltang/dtconsulting/joesbarbershop/site && npm run build > /tmp/build.log 2>&1 && grep -q "import Article" src/pages/east-county-traditional-barbershop.astro && grep -q "import Article" src/pages/2026-east-county-barbershop-cost-guide.astro && grep -q 'ogType="article"' src/pages/east-county-traditional-barbershop.astro && grep -q 'ogType="article"' src/pages/2026-east-county-barbershop-cost-guide.astro && grep -q "TODO Plan 06" src/pages/east-county-traditional-barbershop.astro && grep -q "TODO Plan 06" src/pages/2026-east-county-barbershop-cost-guide.astro && grep -q '"@type":"Article"' dist/east-county-traditional-barbershop/index.html && grep -q '"@type":"FAQPage"' dist/east-county-traditional-barbershop/index.html && grep -q '"@type":"Article"' dist/2026-east-county-barbershop-cost-guide/index.html && grep -q '"@type":"FAQPage"' dist/2026-east-county-barbershop-cost-guide/index.html && grep -q 'property="og:type" content="article"' dist/east-county-traditional-barbershop/index.html && grep -q 'property="og:type" content="article"' dist/2026-east-county-barbershop-cost-guide/index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Both pages import Article + FAQPage from schema components
    - Both pages pass `ogType="article"` to Base
    - Both pages contain `// TODO Plan 06` marker noting dateModified placeholder
    - Built niche-landing HTML contains `"@type":"Article"` AND `"@type":"FAQPage"` AND `og:type" content="article"`
    - Built cost guide HTML contains `"@type":"Article"` AND `"@type":"FAQPage"` AND `og:type" content="article"`
    - All Article emissions have `headline`, `datePublished`, `dateModified`, `author`, `image` (image inherited from Article.astro)
    - npm run build exits 0
    - Each page's existing `<style>` block is preserved verbatim (no CSS regression)
  </acceptance_criteria>
  <done>Both Article pages emit Article + FAQPage with placeholder dateModified. Plan 06 will replace the placeholder with the real git-mtime import.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Service + FAQPage into [service].astro and FAQPage into [neighborhood]-barber.astro (with hero priority verification)</name>
  <read_first>
    - site/src/pages/[service].astro
    - site/src/pages/[neighborhood]-barber.astro
    - site/src/components/Hero.astro (confirm fetchpriority="high" already in place)
    - .planning/phases/05-aeo-performance-meta/05-PATTERNS.md (lines 998-1052 — template page edit guides)
    - .planning/phases/05-aeo-performance-meta/05-CONTEXT.md (D-20 hero priority per archetype)
  </read_first>
  <files>site/src/pages/[service].astro, site/src/pages/[neighborhood]-barber.astro</files>
  <action>
Both files are dynamic SSG routes using `getStaticPaths`. Each currently uses `entry.data.faqs` from its content collection. Add `<Service entry={entry}/>` (services only) and `<FAQPage faqs={entry.data.faqs}/>` (both) to the head slot.

---

**[service].astro**

Frontmatter additions (preserve all existing imports + getStaticPaths + destructure):
```astro
import Service from '../components/schema/Service.astro';
import FAQPage from '../components/schema/FAQPage.astro';
```

Body wrap (insert after the opening `<Base>` tag, before the existing body):
```astro
<Base
  title={`${entry.data.title} | Joe's Barbershop`}
  description={entry.data.bluf}
  variant="article"
>
  <Fragment slot="head">
    <Service entry={entry} pageUrl={Astro.url.href} />
    <FAQPage faqs={entry.data.faqs} pageUrl={Astro.url.href} />
  </Fragment>
  <!-- existing body unchanged -->
</Base>
```

Hero image: per CONTEXT D-20, service-page hero is above-the-fold and should use `priority` (Astro 6 shorthand for `fetchpriority="high" loading="eager" decoding="sync"`). Locate the `<Image>` tag in `[service].astro` body that renders the service hero. If it currently has `loading="lazy"`, change to `priority`:

Before:
```astro
<Image src={...} alt="..." loading="lazy" width={...} height={...} />
```

After:
```astro
<Image src={...} alt="..." priority width={...} height={...} />
```

The `priority` prop is a single-token shorthand. Only ONE image per page should carry priority (Lighthouse penalty for multiple priority images per RESEARCH Pitfall 4).

---

**[neighborhood]-barber.astro**

Frontmatter additions:
```astro
import FAQPage from '../components/schema/FAQPage.astro';
```

Body wrap:
```astro
<Base
  title={`Barber in ${entry.data.name} | Joe's Barbershop`}
  description={entry.data.bluf}
  variant="article"
>
  <Fragment slot="head">
    <FAQPage faqs={entry.data.faqs} pageUrl={Astro.url.href} />
  </Fragment>
  <!-- existing body unchanged -->
</Base>
```

No `<Service>` overlay (per PATTERNS line 1050: rely on auto-injected HairSalon for identity; per-neighborhood areaServed would duplicate homepage HairSalon). Skip — only FAQPage emits.

Use the actual `entry.data.<field>` field name for the neighborhood display (could be `name`, `slug`, `neighborhood` — read the file first).

Hero image: per D-20 neighborhoods use `02-storefront.jpg` as hero. If the current `<Image>` for the hero uses `loading="lazy"`, change to `priority`. Same one-image-per-page rule.

---

After both edits:
```bash
cd site && npm run build
# All 6 service pages must emit Service + FAQPage:
for slug in fades classic-cut kids-cuts beard-trim line-up hot-towel-shave; do
  grep -q '"@type":"Service"' "dist/${slug}/index.html" && echo "OK $slug Service" || echo "FAIL $slug Service"
  grep -q '"@type":"FAQPage"' "dist/${slug}/index.html" && echo "OK $slug FAQPage" || echo "FAIL $slug FAQPage"
done
# All 5 neighborhood pages must emit FAQPage:
for slug in bostonia-barber el-cajon-barber santee-barber lakeside-barber la-mesa-barber; do
  grep -q '"@type":"FAQPage"' "dist/${slug}/index.html" && echo "OK $slug FAQPage" || echo "FAIL $slug FAQPage"
done
# Verify hero fetchpriority="high" present on homepage + sampled service + sampled neighborhood:
grep -c 'fetchpriority="high"' dist/index.html
grep -c 'fetchpriority="high"' dist/fades/index.html
grep -c 'fetchpriority="high"' dist/bostonia-barber/index.html
```

Each `grep -c` should print exactly `1` (one hero image with fetchpriority="high" per page). If a page prints `0`, the `priority` prop didn't render an attribute; if it prints `2+`, multiple images were marked priority — fix to one.

Then verify the AEO-07 text-as-image baseline holds (no new regressions):
```bash
bash .planning/phases/03-unique-pages/scripts/audit.sh text-as-image
```

(The stub from Plan 01 always passes, but real check in Plan 07 must also pass — so verify by hand that no `<img alt="...">` in any modified page contains price markers or service words.)
  </action>
  <verify>
    <automated>cd /Users/darrelltang/dtconsulting/joesbarbershop/site && npm run build > /tmp/build.log 2>&1 && grep -q "import Service" "src/pages/[service].astro" && grep -q "import FAQPage" "src/pages/[service].astro" && grep -q "import FAQPage" "src/pages/[neighborhood]-barber.astro" && for slug in fades classic-cut kids-cuts beard-trim line-up hot-towel-shave; do grep -q '"@type":"Service"' "dist/${slug}/index.html" || { echo "FAIL Service missing on $slug"; exit 1; }; grep -q '"@type":"FAQPage"' "dist/${slug}/index.html" || { echo "FAIL FAQPage missing on $slug"; exit 1; }; done && for slug in bostonia-barber el-cajon-barber santee-barber lakeside-barber la-mesa-barber; do grep -q '"@type":"FAQPage"' "dist/${slug}/index.html" || { echo "FAIL FAQPage missing on $slug"; exit 1; }; done && [ "$(grep -c 'fetchpriority="high"' dist/index.html)" = "1" ] && bash ../.planning/phases/03-unique-pages/scripts/audit.sh && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - [service].astro imports Service + FAQPage; Fragment slot="head" contains both
    - [neighborhood]-barber.astro imports FAQPage; Fragment slot="head" contains it (no Service overlay)
    - All 6 built service pages contain `"@type":"Service"` AND `"@type":"FAQPage"` AND `"@type":"HairSalon"`
    - All 5 built neighborhood pages contain `"@type":"FAQPage"` AND `"@type":"HairSalon"`
    - dist/index.html contains exactly one `fetchpriority="high"` attribute
    - npm run build exits 0
    - bash audit.sh full suite exits 0 (no Phase 3/4 regression)
  </acceptance_criteria>
  <done>All 11 templated pages emit their archetype-appropriate schemas; hero priority verified; full audit suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| entry.data.* (collection markdown) → schema component props | Per-page data flows from MDX frontmatter into JSON-LD |
| Astro.url.href → schema @id + pageUrl props | Page URL flows into FAQPage + Service `@id` fragments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-14 | Tampering | Collection markdown FAQ text → FAQPage JSON-LD | mitigate | Plan 07 validate-schema.mjs greps every emitted JSON-LD block for `</script>` substring; current FAQ content is short answer prose with no risk |
| T-05-15 | Information Disclosure | Reviews quote text emitted to JSON-LD | accept | All reviews data is from public GBP/Yelp scrapes; quotes are intentional discovery surface |
| T-05-16 | Tampering | Astro.url manipulation | accept | Astro.url is build-time-computed from getStaticPaths output, not user input; no manipulation vector at runtime (static site) |
</threat_model>

<verification>
```bash
cd /Users/darrelltang/dtconsulting/joesbarbershop/site && npm run build && \
  echo "--- Schema emission audit ---" && \
  for page in index about reviews faq east-county-traditional-barbershop 2026-east-county-barbershop-cost-guide fades classic-cut kids-cuts beard-trim line-up hot-towel-shave bostonia-barber el-cajon-barber santee-barber lakeside-barber la-mesa-barber; do
    if [ "$page" = "index" ]; then file=dist/index.html; else file=dist/${page}/index.html; fi
    types=$(grep -oE '"@type":"[A-Za-z]+"' "$file" | sort -u | tr '\n' ' ')
    echo "  /$page : $types"
  done && \
  bash ../.planning/phases/03-unique-pages/scripts/audit.sh && \
  echo "PLAN 05 COMPLETE"
```

Every page should list at minimum `"@type":"HairSalon"`. Each archetype gets its additional overlays.
</verification>

<success_criteria>
- 8 page files modified, each emitting archetype-appropriate schemas
- HairSalon auto-emits on all 17 pages (Plan 04 verified)
- AggregateRating + FAQPage on /
- Person on /about
- Review[] on /reviews
- FAQPage on /faq + all 6 services + all 5 neighborhoods + both Article pages + /
- Article on niche-landing + cost guide (with placeholder dateModified marked TODO Plan 06)
- ogType="article" on the two Article pages
- One hero image per page with fetchpriority="high" (PERF-03 baseline)
- npm run build + bash audit.sh both exit 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-aeo-performance-meta/05-05-SUMMARY.md`. Note the two TODO Plan 06 markers and the homepageFaqs / FAQ.astro duplication.
</output>
