---
phase: 04-templated-pages
plan: 04
type: execute
wave: 2
depends_on:
  - "04-02"
files_modified:
  - site/src/content/neighborhoods/bostonia.md
  - site/src/content/neighborhoods/el-cajon.md
  - site/src/content/neighborhoods/santee.md
  - site/src/content/neighborhoods/lakeside.md
  - site/src/content/neighborhoods/la-mesa.md
autonomous: true
requirements:
  - PAGE-08
must_haves:
  truths:
    - "Each of the 5 neighborhood collection markdown files has real landmarks (NOT 'Bostonia area' / 'East County San Diego' placeholders)"
    - "Each landmarks array has at least 2 real, neighborhood-specific landmark strings"
    - "Each neighborhood file has a real distance string (NOT 'local')"
    - "Each neighborhood file has a populated bluf field (NOT the Phase 2 stub one-liner)"
    - "Each neighborhood file has a populated faqs array (3-4 Q&As per inputs/01-page-list.md)"
    - "Each neighborhood file has a populated markdown body (NOT 'Stub content — Phase 4 replaces')"
    - "/bostonia-barber and /el-cajon-barber received the per-page copywriting polish per D-14"
    - "Built /bostonia-barber, /el-cajon-barber, /santee-barber, /lakeside-barber, /la-mesa-barber render the populated landmarks and FAQs"
  artifacts:
    - path: "site/src/content/neighborhoods/bostonia.md"
      provides: "Polished /bostonia-barber content; primary local-citation surface (Joe's home neighborhood)"
      contains: "Sycuan Casino"
    - path: "site/src/content/neighborhoods/el-cajon.md"
      provides: "Polished /el-cajon-barber content; biggest market-name page"
      contains: "Westfield Parkway"
    - path: "site/src/content/neighborhoods/santee.md"
      provides: "Bulk-baseline /santee-barber content"
      contains: "Santee"
    - path: "site/src/content/neighborhoods/lakeside.md"
      provides: "Bulk-baseline /lakeside-barber content"
      contains: "Lakeside"
    - path: "site/src/content/neighborhoods/la-mesa.md"
      provides: "Bulk-baseline /la-mesa-barber content"
      contains: "La Mesa"
  key_links:
    - from: "site/src/content/neighborhoods/*.md"
      to: ".agents/aeo-frame.md"
      via: "marketing-skills:programmatic-seo + ai-seo frame"
      pattern: "(AEO frame consumed by skill)"
    - from: "site/src/content/neighborhoods/*.md (landmarks field)"
      to: "vault audit baseline (joes-barbershop-sandbox.md) + planner research"
      via: "Real landmarks per neighborhood (D-11 + CONTEXT specifics)"
      pattern: "(landmark research)"
---

<objective>
Author the 5 neighborhood collection markdown files with AEO-optimized BLUF, FAQs, populated landmarks/distance, and prose body. Use the **hybrid skill chain** per CONTEXT D-14:
1. `marketing-skills:programmatic-seo` produces the bulk baseline for all 5 neighborhoods.
2. `marketing-skills:copywriting` polishes 2 strategic pages: `/bostonia-barber` (Joe's literal home neighborhood) and `/el-cajon-barber` (biggest market name; highest competition).

Critical: existing collection stubs have placeholder landmarks (`["Bostonia area", "East County San Diego"]`) and placeholder distance (`"local"`). Phase 4 populates with REAL landmarks per CONTEXT specifics + distance strings in the AI-parser-friendly `"X.X mi from shop"` format.

Purpose: Plan 2's route template renders `entry.data.landmarks`, `entry.data.distance`, `entry.data.bluf`, and `entry.data.faqs` directly in the structured "Getting here" block, BLUF capsule, and FAQ section. Plan 4 ships the visible content for those fields.

Output: 5 modified markdown files with real landmarks (≥2 per neighborhood), real distance strings, populated BLUF + FAQs + prose body. Zero "Stub content" markers remain.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templated-pages/04-CONTEXT.md
@.planning/phases/04-templated-pages/04-PATTERNS.md
@.planning/phases/03-unique-pages/03-CONTEXT.md
@CLAUDE.md
@site/src/content.config.ts
@site/src/content/neighborhoods/bostonia.md
@site/src/content/neighborhoods/el-cajon.md
@site/src/content/neighborhoods/santee.md
@site/src/content/neighborhoods/lakeside.md
@site/src/content/neighborhoods/la-mesa.md
@inputs/01-page-list.md
@inputs/02-aeo-constraints.md

<interfaces>
neighborhoods collection schema (locked at site/src/content.config.ts lines 20-29):

```typescript
schema: z.object({
  title: z.string(),
  landmarks: z.array(z.string()),
  distance: z.string(),
  bluf: z.string(),
  faqs: z.array(z.object({ q: z.string(), a: z.string() })),
})
```

Existing frontmatter values to PRESERVE (do not overwrite):

| File          | title              |
|---------------|--------------------|
| bostonia.md   | "Bostonia Barber"  |
| el-cajon.md   | "El Cajon Barber"  |
| santee.md     | "Santee Barber"    |
| lakeside.md   | "Lakeside Barber"  |
| la-mesa.md    | "La Mesa Barber"   |

Per-page primary queries (CONTEXT D-14):
- /bostonia-barber → "barber in Bostonia / Bostonia barbershop"
- /el-cajon-barber → "barber in El Cajon / El Cajon barbershop"
- /santee-barber → "barber in Santee / Santee barbershop"
- /lakeside-barber → "barber in Lakeside / Lakeside barbershop"
- /la-mesa-barber → "barber in La Mesa / La Mesa barbershop"

Real landmarks per neighborhood (from CONTEXT specifics + vault audit baseline):
- Bostonia → Sycuan Casino, Parkway Plaza, Bostonia Park (Joe's home neighborhood — strip-mall location at 723 E Bradley Ave)
- El Cajon → Westfield Parkway, Downtown El Cajon, Magnolia Avenue corridor
- Santee → Santee Town Center, Santee Lakes Recreation Preserve
- Lakeside → Lakeside Rodeo Grounds, El Capitan Reservoir
- La Mesa → La Mesa Village, Grossmont Center, Mt. Helix

(The skill or executor confirms each landmark exists via web search if any feel uncertain. Vault audit baseline `joes-barbershop-sandbox.md` is the strategic-knowledge source.)

Distance strings (from existing stubs — preserve where real, replace where stub):
- bostonia.md: currently `"local"` — REPLACE with `"0.0 mi — in Bostonia"` (Joe's shop is IN Bostonia)
- el-cajon.md: currently `"0.5 mi from shop"` — keep (already real)
- santee.md: currently `"7 mi from shop"` — keep (already real)
- lakeside.md: currently `"8 mi from shop"` — keep (already real)
- la-mesa.md: currently `"9 mi from shop"` — keep (already real)

FAQ count per neighborhood (from inputs/01-page-list.md):
- Soft target: 3-4 FAQs (less than services because the page is more concise)

Joe's actual address (for FAQ accuracy):
- 723 E Bradley Ave, Suite C, El Cajon, CA 92021 (in the Bostonia neighborhood)
- Phone: from business.json
- Hours: Tue–Sat (per business.json)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk baseline via marketing-skills:programmatic-seo for all 5 neighborhoods</name>
  <files>site/src/content/neighborhoods/bostonia.md, site/src/content/neighborhoods/el-cajon.md, site/src/content/neighborhoods/santee.md, site/src/content/neighborhoods/lakeside.md, site/src/content/neighborhoods/la-mesa.md</files>
  <read_first>
    - .agents/product-marketing-context.md (Phase 3 D-03 output)
    - .agents/aeo-frame.md (Phase 3 D-04 output — load-bearing)
    - inputs/02-aeo-constraints.md
    - inputs/01-page-list.md (neighborhood pages "3-4 FAQs" soft target)
    - All 5 existing neighborhood stub files (preserve title)
    - .planning/phases/03-unique-pages/03-CONTEXT.md D-01..D-05 (skill chain blueprint)
    - ~/Documents/DT Vault/1-projects/dt-consulting-llc/joes-barbershop-sandbox.md (audit baseline — strategic source for neighborhood-specific differentiators)
    - site/src/pages/east-county-traditional-barbershop.astro (voice anchor for the BLUF tone)
  </read_first>
  <action>
Step 1: Verify and confirm landmark data per neighborhood. Use the CONTEXT-listed landmarks as the starting set:
- Bostonia → ["Sycuan Casino", "Parkway Plaza", "Bostonia Park"]
- El Cajon → ["Westfield Parkway", "Downtown El Cajon", "Magnolia Avenue"]
- Santee → ["Santee Town Center", "Santee Lakes Recreation Preserve"]
- Lakeside → ["Lakeside Rodeo Grounds", "El Capitan Reservoir"]
- La Mesa → ["La Mesa Village", "Grossmont Center", "Mt. Helix"]

If any landmark feels uncertain (e.g., is Sycuan Casino actually closer to El Cajon than Bostonia?), web-search to confirm before committing. Vault audit baseline (`~/Documents/DT Vault/1-projects/dt-consulting-llc/joes-barbershop-sandbox.md`) is the strategic ground truth.

Step 2: Invoke `marketing-skills:programmatic-seo` ONCE for all 5 neighborhoods.

Skill inputs:
- Voice/tone source: `.agents/product-marketing-context.md`
- AEO frame source: `.agents/aeo-frame.md`
- Per-page primary queries (per D-14, listed in interfaces above)
- Per-page landmark sets (Step 1 — passed as additional context so the BLUF and prose can reference real landmarks naturally)
- Soft target: 3-4 FAQs per neighborhood (variable; skill picks based on what's actually asked about each)
- Voice anchor: same as services — working-class East County, heritage barbershop, walk-ins-welcome, $30 base, traditional cut.

Step 3: Write skill output into the 5 markdown files. Each file gets:

Frontmatter (PRESERVE existing title, REPLACE landmarks/distance/bluf/faqs with real values):

```yaml
<!-- --- -->
title: "{preserve existing}"
landmarks:
  - "{real landmark 1}"
  - "{real landmark 2}"
  - "{real landmark 3 — optional but encouraged where natural}"
distance: "{real distance per interfaces table}"
bluf: "{skill-generated ~100-word BLUF answering 'barber in {neighborhood}' query}"
faqs:
  - q: "{skill-generated question 1}"
    a: "{skill-generated 130-160 word answer}"
  - q: "{skill-generated question 2}"
    a: "{skill-generated answer}"
  # ... 3-4 total per inputs/01-page-list.md
<!-- --- -->
```

Specific frontmatter values to write:

bostonia.md:
- landmarks: `["Sycuan Casino", "Parkway Plaza", "Bostonia Park"]`
- distance: `"0.0 mi — in Bostonia"` (Joe's shop is literally IN Bostonia)
- bluf: skill-generated, must mention "Bostonia" as Joe's home neighborhood + 723 E Bradley Ave + walk-ins + $30
- faqs: skill-generated 3-4 questions including at minimum "Is Joe's Barbershop located in Bostonia?" with a yes-and-address answer

el-cajon.md:
- landmarks: `["Westfield Parkway", "Downtown El Cajon", "Magnolia Avenue"]`
- distance: `"0.5 mi from shop"` (preserve existing)
- bluf: skill-generated, "barber in El Cajon" answer; mentions Joe's location near downtown + chain-shop differentiation (cash-only, walk-in)
- faqs: 3-4 questions including "How far from downtown El Cajon is Joe's?" or similar

santee.md:
- landmarks: `["Santee Town Center", "Santee Lakes Recreation Preserve"]`
- distance: `"7 mi from shop"` (preserve)
- bluf: skill-generated, "barber in Santee" answer; mentions ~7 mi drive + walk-in friendly
- faqs: 3-4 questions including driving / parking concern

lakeside.md:
- landmarks: `["Lakeside Rodeo Grounds", "El Capitan Reservoir"]`
- distance: `"8 mi from shop"` (preserve)
- bluf: skill-generated, "barber in Lakeside" answer
- faqs: 3-4 questions

la-mesa.md:
- landmarks: `["La Mesa Village", "Grossmont Center", "Mt. Helix"]`
- distance: `"9 mi from shop"` (preserve)
- bluf: skill-generated, "barber in La Mesa" answer
- faqs: 3-4 questions

Markdown body for each (replacing the "Stub content — Phase 4 replaces" line):

```markdown
## How Joe's serves {neighborhood}

{Skill-generated H2 answer capsule (130-160 words). Answers "barber in {neighborhood}". References real landmarks naturally (e.g., "Customers driving from Westfield Parkway in El Cajon..."). Surfaces the drive context (distance + landmark anchors). Inline link to one related service where prose flow allows.}

## Why customers come from {neighborhood}

{Skill-generated H2 answer capsule (130-160 words). Surfaces Joe's differentiator framed for THIS neighborhood: $30 base vs market price, traditional walk-in, family-friendly. Inline link to /east-county-traditional-barbershop or one of the service pages where natural.}
```

Constraints:
- DO preserve title exactly.
- DO write the EXACT landmark sets above for each file (these are confirmed; skill should not "improve" them).
- DO use the EXACT distance strings above.
- DO NOT use banned phrases: "we're more than a barbershop", "experience the difference", "click here", "innovate", "streamline", "we might", "could potentially".
- DO answer each page's primary query in the BLUF first sentence.
- IF skill output hallucinates a landmark not in the approved list (e.g., invents "Bostonia Town Square"), discard and re-prompt with a tighter constraint.
- Atomic per-page commits.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/dtconsulting/joesbarbershop &amp;&amp; for f in site/src/content/neighborhoods/*.md; do echo "=== $f ==="; if grep -q "Stub content" "$f"; then echo "FAIL: stub remains"; fi; if grep -qE 'distance: "local"' "$f"; then echo "FAIL: distance still local"; fi; faq_count=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag &amp;&amp; /^  - q:/{n++} END{print n+0}' "$f"); echo "faqs: $faq_count"; if [ "$faq_count" -lt 3 ]; then echo "FAIL: $f has fewer than 3 FAQs"; fi; done</automated>
  </verify>
  <acceptance_criteria>
    - All 5 neighborhood files exist: `for f in bostonia el-cajon santee lakeside la-mesa; do test -f "site/src/content/neighborhoods/${f}.md"; done`
    - Zero stub markers remain: `! grep -l "Stub content" site/src/content/neighborhoods/*.md`
    - Zero `distance: "local"` placeholders: `! grep -l 'distance: "local"' site/src/content/neighborhoods/*.md`
    - bostonia.md landmarks contains "Sycuan Casino": `grep -q "Sycuan Casino" site/src/content/neighborhoods/bostonia.md`
    - el-cajon.md landmarks contains "Westfield Parkway": `grep -q "Westfield Parkway" site/src/content/neighborhoods/el-cajon.md`
    - santee.md landmarks contains "Santee Town Center" or "Santee Lakes": `grep -qE "Santee Town Center|Santee Lakes" site/src/content/neighborhoods/santee.md`
    - lakeside.md landmarks contains "Lakeside Rodeo" or "El Capitan": `grep -qE "Lakeside Rodeo|El Capitan" site/src/content/neighborhoods/lakeside.md`
    - la-mesa.md landmarks contains "La Mesa Village" or "Grossmont": `grep -qE "La Mesa Village|Grossmont" site/src/content/neighborhoods/la-mesa.md`
    - No stub-marker landmark patterns remain: `! grep -E '"Bostonia area"|"El Cajon", "East County"' site/src/content/neighborhoods/*.md`
    - Each file has at least 3 FAQs: `for f in site/src/content/neighborhoods/*.md; do n=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag && /^  - q:/{n++} END{print n+0}' "$f"); test "$n" -ge 3 || echo "FAIL: $f has $n FAQs"; done` → no FAIL output
    - Each file has BLUF substantially longer than Phase 2 stub: `for f in site/src/content/neighborhoods/*.md; do bluf_words=$(grep "^bluf:" "$f" | wc -w); test "$bluf_words" -ge 25 || echo "FAIL: $f bluf $bluf_words words"; done`
    - Each markdown body has H2 sections: `for f in site/src/content/neighborhoods/*.md; do count=$(grep -c "^## " "$f"); test "$count" -ge 2 || echo "FAIL: $f has $count H2 sections"; done`
    - No banned anti-pattern phrases: `! grep -irE "(we're more than a barbershop|experience the difference|click here|innovate|streamline)" site/src/content/neighborhoods/*.md`
    - Build succeeds: `cd site && npm run build` exits 0
    - All 5 dist files render: `for slug in bostonia-barber el-cajon-barber santee-barber lakeside-barber la-mesa-barber; do test -f "site/dist/${slug}/index.html"; done`
    - Each built page renders the populated landmarks list: `for slug in bostonia-barber el-cajon-barber santee-barber lakeside-barber la-mesa-barber; do count=$(grep -oE '<ul class="landmarks">' "site/dist/${slug}/index.html" | wc -l); test "$count" -ge 1 || echo "FAIL: $slug missing landmarks ul"; done`
  </acceptance_criteria>
  <done>
    All 5 neighborhood files have real landmarks (≥2 each from the approved set), real distance strings (no "local" placeholder), populated bluf (≥25 words), ≥3 FAQs, ≥2 H2 markdown body sections. Zero stub markers. Built pages render the populated landmarks via the Plan 2 template's `.landmarks` ul.
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish pass via marketing-skills:copywriting on /bostonia-barber and /el-cajon-barber</name>
  <files>site/src/content/neighborhoods/bostonia.md, site/src/content/neighborhoods/el-cajon.md</files>
  <read_first>
    - site/src/content/neighborhoods/bostonia.md (post-Task-1 baseline)
    - site/src/content/neighborhoods/el-cajon.md (post-Task-1 baseline)
    - .agents/product-marketing-context.md
    - .agents/aeo-frame.md
    - ~/Documents/DT Vault/1-projects/dt-consulting-llc/joes-barbershop-sandbox.md (audit baseline — Joe's strategic positioning relative to El Cajon chain shops + Bostonia local-citation framing)
    - .planning/phases/03-unique-pages/03-12-PLAN.md (niche-landing copy refresh — voice / strategic-angle reference for polish quality bar)
  </read_first>
  <action>
Step 1: Invoke `marketing-skills:copywriting` per page on the two strategic neighborhoods per CONTEXT D-14:

For /bostonia-barber (Joe's literal home neighborhood; primary local-citation surface):
- Skill input: post-Task-1 baseline `bostonia.md` + `.agents/product-marketing-context.md` + `.agents/aeo-frame.md` + primary query "barber in Bostonia / Bostonia barbershop".
- Strategic angle to sharpen: this is the local-citation anchor. Bostonia is where Joe's shop literally is (723 E Bradley Ave, in Bostonia, El Cajon CA 92021). Polished copy makes "Joe's IS the Bostonia barbershop" the unmissable answer. References real Bostonia anchors (Sycuan Casino visible nearby, Parkway Plaza, the strip-mall location). Customers from Bostonia don't drive — they walk or are 2-3 minutes away.
- Tighten BLUF to lead with "Joe's Barbershop is located in Bostonia at 723 E Bradley Ave — the traditional barbershop for the neighborhood." Sharpen FAQs around hyperlocal concerns: parking at the strip mall, walking distance, kids walk-ins from Bostonia families.

For /el-cajon-barber (biggest market name; highest competition with chain results):
- Skill input: post-Task-1 baseline `el-cajon.md` + same context files + primary query "barber in El Cajon / El Cajon barbershop".
- Strategic angle to sharpen: El Cajon is the biggest search term but the most competitive (chains, grooming lounges, etc.). Polished copy positions Joe's as THE traditional barbershop in El Cajon — fixed price, walk-in, no app, no upsell. Differentiates from Westfield Parkway-area chains and Magnolia Avenue grooming lounges. Acknowledges 0.5-mile distance from downtown El Cajon (the shop is technically in Bostonia, which is part of El Cajon proper — frame this clearly).
- Tighten BLUF to lead with "Joe's Barbershop serves El Cajon with traditional barbering — $30, cash only, walk-in." Sharpen FAQs around: chain-vs-traditional comparison, downtown El Cajon distance, El Cajon Boulevard accessibility.

Step 2: Replace existing bostonia.md and el-cajon.md content with polished versions. Preserve title from Task 1 baseline. Preserve landmarks arrays exactly (those were verified in Task 1). Replace bluf + faqs + markdown body with polished versions.

Step 3: Verify polish didn't break schema or anti-patterns.

Constraints:
- DO NOT touch the other 3 neighborhood files (santee, lakeside, la-mesa) in this task.
- DO NOT change title or landmarks values.
- DO commit each polished file atomically.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/dtconsulting/joesbarbershop &amp;&amp; for f in site/src/content/neighborhoods/bostonia.md site/src/content/neighborhoods/el-cajon.md; do echo "=== $f ==="; bluf_words=$(grep "^bluf:" "$f" | wc -w); echo "bluf words: $bluf_words"; faq_count=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag &amp;&amp; /^  - q:/{n++} END{print n+0}' "$f"); echo "faqs: $faq_count"; done &amp;&amp; cd site &amp;&amp; npm run build 2&gt;&amp;1 | tail -10 &amp;&amp; test -f dist/bostonia-barber/index.html &amp;&amp; test -f dist/el-cajon-barber/index.html &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - bostonia.md bluf is substantially longer than the Phase 2 one-liner: `bluf_words=$(grep "^bluf:" site/src/content/neighborhoods/bostonia.md | wc -w); test "$bluf_words" -ge 30 || echo "FAIL: bostonia bluf $bluf_words words"`
    - el-cajon.md bluf is substantially longer than the Phase 2 one-liner: `bluf_words=$(grep "^bluf:" site/src/content/neighborhoods/el-cajon.md | wc -w); test "$bluf_words" -ge 30 || echo "FAIL: el-cajon bluf $bluf_words words"`
    - bostonia.md mentions Joe's address (723 E Bradley): `grep -qE "723 E Bradley|723 East Bradley" site/src/content/neighborhoods/bostonia.md`
    - bostonia.md still contains "Sycuan Casino" landmark (not regressed): `grep -q "Sycuan Casino" site/src/content/neighborhoods/bostonia.md`
    - el-cajon.md still contains "Westfield Parkway" landmark (not regressed): `grep -q "Westfield Parkway" site/src/content/neighborhoods/el-cajon.md`
    - bostonia.md has at least 3 FAQs: `n=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag && /^  - q:/{n++} END{print n+0}' site/src/content/neighborhoods/bostonia.md); test "$n" -ge 3 || echo "FAIL: bostonia has $n FAQs"`
    - el-cajon.md has at least 3 FAQs: `n=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag && /^  - q:/{n++} END{print n+0}' site/src/content/neighborhoods/el-cajon.md); test "$n" -ge 3 || echo "FAIL: el-cajon has $n FAQs"`
    - No banned phrases: `! grep -irE "(we're more than a barbershop|experience the difference|click here|innovate|streamline)" site/src/content/neighborhoods/bostonia.md site/src/content/neighborhoods/el-cajon.md`
    - Build succeeds and both polished pages render: `cd site && npm run build && test -f site/dist/bostonia-barber/index.html && test -f site/dist/el-cajon-barber/index.html`
    - Other 3 neighborhood files unchanged from Task 1: `for f in santee lakeside la-mesa; do n=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag && /^  - q:/{n++} END{print n+0}' "site/src/content/neighborhoods/${f}.md"); test "$n" -ge 3 || echo "FAIL: $f got regressed"; done`
  </acceptance_criteria>
  <done>
    /bostonia-barber and /el-cajon-barber have polished BLUF (≥30 words each, leading with the primary-query answer + Joe's address for Bostonia + El Cajon downtown framing for El Cajon), ≥3 FAQs each, and strategic-angle differentiators (hyperlocal Bostonia citation anchor; chain-vs-traditional differentiation in El Cajon). The other 3 neighborhoods retain Task 1 baseline content unchanged. Landmarks preserved exactly.
  </done>
</task>

</tasks>

<verification>
- All 5 neighborhood collection files free of stub markers: `! grep -l "Stub content" site/src/content/neighborhoods/*.md`
- All 5 files have real landmarks (≥2), real distance, populated bluf (≥25 words), ≥3 faqs, ≥2 H2 markdown sections
- /bostonia-barber and /el-cajon-barber have polished content (≥30-word BLUF, address mentioned for Bostonia)
- `cd site && npm run build` exits 0; all 5 dist/{slug}-barber/index.html files exist
- Each built neighborhood page renders the populated landmarks via `<ul class="landmarks">` from the Plan 2 template
</verification>

<success_criteria>
- ROADMAP Phase 4 success criterion #2 achieved: each neighborhood slug returns 200 AND displays the correct landmarks and neighborhood-specific FAQs
- Voice consistency with Phase 3 niche-landing maintained per D-17
- /bostonia-barber and /el-cajon-barber polish pass differentiates them as canonical answer surfaces for their respective queries (hyperlocal Bostonia + competitive El Cajon)
</success_criteria>

<output>
After completion, create `.planning/phases/04-templated-pages/04-04-SUMMARY.md` summarizing:
- Skill invocations made (programmatic-seo bulk + copywriting × 2)
- Per-file: BLUF word count, FAQ count, body H2 count, landmarks list, distance value
- Any user-flagged outputs (skill produced wrong info → caught and not committed)
- Verification output: stub-marker grep, distance "local" grep, build result, dist files present
</output>
