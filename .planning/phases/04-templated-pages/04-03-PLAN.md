---
phase: 04-templated-pages
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - site/src/content/services/fades.md
  - site/src/content/services/classic-cut.md
  - site/src/content/services/kids-cuts.md
  - site/src/content/services/beard-trim.md
  - site/src/content/services/line-up.md
  - site/src/content/services/hot-towel-shave.md
autonomous: true
requirements:
  - PAGE-07
must_haves:
  truths:
    - "Each of the 6 service collection markdown files has a populated bluf field (NOT the Phase 2 stub one-liner)"
    - "Each service file has a populated faqs array (variable count per CONTEXT D-07; not empty)"
    - "Each service file has a populated markdown body (NOT 'Stub content — Phase 3/4 replaces')"
    - "Each service file has a heroPhoto field referencing one of the 6 photos in src/assets/photos/"
    - "/fades and /kids-cuts received the per-page copywriting polish per D-14"
    - "Built /fades, /classic-cut, /kids-cuts, /beard-trim, /line-up, /hot-towel-shave pages render the populated content"
  artifacts:
    - path: "site/src/content/services/fades.md"
      provides: "Polished /fades page content; primary query 'fade haircut El Cajon / East County'"
      contains: "heroPhoto: \"05-mid-cut.jpg\""
    - path: "site/src/content/services/classic-cut.md"
      provides: "Bulk-baseline /classic-cut content"
    - path: "site/src/content/services/kids-cuts.md"
      provides: "Polished /kids-cuts page content; family-friendly differentiator angle"
    - path: "site/src/content/services/beard-trim.md"
      provides: "Bulk-baseline /beard-trim content"
    - path: "site/src/content/services/line-up.md"
      provides: "Bulk-baseline /line-up content"
    - path: "site/src/content/services/hot-towel-shave.md"
      provides: "Bulk-baseline /hot-towel-shave content"
  key_links:
    - from: "site/src/content/services/*.md"
      to: ".agents/product-marketing-context.md"
      via: "marketing-skills:programmatic-seo input"
      pattern: "(skill chain reads context)"
    - from: "site/src/content/services/*.md"
      to: ".agents/aeo-frame.md"
      via: "marketing-skills:programmatic-seo + ai-seo frame"
      pattern: "(AEO frame consumed by skill)"
---

<objective>
Author the 6 service collection markdown files with AEO-optimized BLUF, FAQs (q/a pairs), and prose body. Use the **hybrid skill chain** per CONTEXT D-14:
1. `marketing-skills:programmatic-seo` produces the bulk baseline for all 6 services.
2. `marketing-skills:copywriting` polishes 2 strategic pages: `/fades` (highest service search volume) and `/kids-cuts` (family differentiator).

Purpose: The route template from Plan 1 reads `entry.data.bluf`, `entry.data.faqs`, and `<Content />` (markdown body) directly. Phase 4 ships visible, AEO-readable copy for these fields. Per CONTEXT D-15 outputs go directly into the existing collection markdown files, replacing the Phase 2 stubs.

Output: 6 modified markdown files. Frontmatter `bluf` and `faqs` populated; markdown body holds prose section (H2 "What this service is" + H2 "What's different at Joe's"). Zero "Stub content" markers remain. `heroPhoto` field present on all 6.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-templated-pages/04-CONTEXT.md
@.planning/phases/04-templated-pages/04-PATTERNS.md
@.planning/phases/03-unique-pages/03-CONTEXT.md
@CLAUDE.md
@site/src/content.config.ts
@site/src/content/services/fades.md
@site/src/content/services/classic-cut.md
@site/src/content/services/kids-cuts.md
@site/src/content/services/beard-trim.md
@site/src/content/services/line-up.md
@site/src/content/services/hot-towel-shave.md
@inputs/01-page-list.md
@inputs/02-aeo-constraints.md

<interfaces>
<!-- Schema contract — frontmatter MUST match exactly. -->

services collection schema (locked at site/src/content.config.ts lines 8-18):
```typescript
schema: z.object({
  title: z.string(),
  price: z.number(),
  duration: z.string(),
  bluf: z.string(),
  faqs: z.array(z.object({ q: z.string(), a: z.string() })),
  heroPhoto: z.string().optional(),
})
```

Existing frontmatter values to PRESERVE (do not overwrite):
| File              | title           | price | duration | heroPhoto         |
|-------------------|-----------------|-------|----------|-------------------|
| fades.md          | "Fades"         | 30    | "30 min" | "05-mid-cut.jpg"  |
| classic-cut.md    | "Classic Cut"   | 30    | "30 min" | "05-mid-cut.jpg"  |
| kids-cuts.md      | "Kids Cuts"     | 30    | "30 min" | (NONE — add)      |
| beard-trim.md     | "Beard Trim"    | 20    | "20 min" | (NONE — add)      |
| line-up.md        | "Line-Up"       | 20    | "20 min" | (NONE — add)      |
| hot-towel-shave.md| "Hot-Towel Shave"| 30   | "30 min" | (NONE — add)      |

heroPhoto fallback assignments (planner's call, files exist in site/src/assets/photos/):
- kids-cuts.md → "03-interior-hero.jpg" (warm interior shot reads as family-friendly)
- beard-trim.md → "06-price-board-cash-only.jpg" (board emphasizes posted-price differentiator)
- line-up.md → "06-price-board-cash-only.jpg" (same — line-up shares the cash-only board signal)
- hot-towel-shave.md → "04-heritage-chair.jpg" (heritage chair reads as traditional shave)

Per-page primary queries (CONTEXT D-14 — load-bearing skill input):
- /fades → "fade haircut El Cajon / East County"
- /classic-cut → "classic men's haircut El Cajon / traditional barbershop cut"
- /kids-cuts → "kids haircut Bostonia / East County family barbershop"
- /beard-trim → "beard trim / line-up East County"
- /line-up → "line-up barber East County"
- /hot-towel-shave → "hot towel shave El Cajon / traditional straight-razor shave"

AEO constraints (from inputs/02-aeo-constraints.md, encoded in .agents/aeo-frame.md):
- BLUF first 100 words; declarative entity-first prose; answer the page's primary query
- 130–160 word answer capsules per H2/H3
- No tabs/accordions; FAQ flat H3/p (template renders this via .faq-q markup)
- No "we're more than a barbershop" / "experience the difference" / "click here" / "innovate" / "streamline"
- No "we might / could potentially" hedging

FAQ count per service (CONTEXT D-07 — variable, not hard count):
- inputs/01-page-list.md soft target: 4-5 FAQs services
- /fades may carry up to 6 (high search volume); /line-up may carry as few as 3
- Schema enforces shape (q/a pairs), not count
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bulk baseline via marketing-skills:programmatic-seo for all 6 services</name>
  <files>
    site/src/content/services/fades.md,
    site/src/content/services/classic-cut.md,
    site/src/content/services/kids-cuts.md,
    site/src/content/services/beard-trim.md,
    site/src/content/services/line-up.md,
    site/src/content/services/hot-towel-shave.md
  </files>
  <read_first>
    - .agents/product-marketing-context.md (Phase 3 D-03 output — positioning + audience + tone for skill input)
    - .agents/aeo-frame.md (or wherever Phase 3 D-04 stored the marketing-skills:ai-seo output — load-bearing AEO frame)
    - inputs/02-aeo-constraints.md (raw AEO constraints — verify .agents/aeo-frame.md hasn't drifted)
    - inputs/01-page-list.md (per-page structural requirements — service pages "4-5 FAQs" soft target)
    - All 6 existing service stub files (preserve title/price/duration/heroPhoto frontmatter)
    - .planning/phases/03-unique-pages/03-CONTEXT.md D-01..D-05 (skill chain blueprint Phase 4 reuses)
    - site/src/pages/east-county-traditional-barbershop.astro (voice anchor — niche-landing prose is the closest reference for the voice the skill should emit)
  </read_first>
  <action>
**Step 1: Verify skill prerequisites exist.**
- Confirm `.agents/product-marketing-context.md` exists and contains positioning/audience/tone (Phase 3 D-03 output). If missing or stale, regenerate via `marketing-skills:product-marketing-context` skill before proceeding.
- Confirm `.agents/aeo-frame.md` (or whichever path Phase 3 stored the ai-seo output — check `.planning/phases/03-unique-pages/03-01-SUMMARY.md` for canonical path) exists. If missing, regenerate via `marketing-skills:ai-seo`.

**Step 2: Invoke `marketing-skills:programmatic-seo` ONCE for all 6 services.**

Skill inputs:
- Voice/tone source: `.agents/product-marketing-context.md`
- AEO frame source: `.agents/aeo-frame.md`
- Per-page primary queries (from D-14):
  - fades → "fade haircut El Cajon / East County"
  - classic-cut → "classic men's haircut El Cajon / traditional barbershop cut"
  - kids-cuts → "kids haircut Bostonia / East County family barbershop"
  - beard-trim → "beard trim / line-up East County"
  - line-up → "line-up barber East County"
  - hot-towel-shave → "hot towel shave El Cajon / traditional straight-razor shave"
- Soft target: 4-5 FAQs per service (variable per D-07 — skill picks based on what's actually asked)
- Voice anchor: working-class East County, heritage barbershop, no-frills, family-friendly, walk-ins-welcome, cash-only-as-positioning. Avoid "we're more than a barbershop" generic opens.

**Step 3: Write skill output into the 6 markdown files.** Each file gets:

Frontmatter (PRESERVE existing title/price/duration/heroPhoto, ADD/UPDATE bluf + faqs):
```yaml
<!-- --- -->
title: "{preserve existing}"
price: {preserve existing number}
duration: "{preserve existing}"
bluf: "{skill-generated ~100-word BLUF answering the page's primary query, declarative entity-first}"
faqs:
  - q: "{skill-generated question 1}"
    a: "{skill-generated 130-160 word answer}"
  - q: "{skill-generated question 2}"
    a: "{skill-generated answer}"
  # ... 3-6 total per service per D-07
heroPhoto: "{preserve OR add per fallback assignments}"
<!-- --- -->
```

heroPhoto assignments (set if not already present):
- fades.md: "05-mid-cut.jpg" (already set)
- classic-cut.md: "05-mid-cut.jpg" (already set)
- kids-cuts.md: "03-interior-hero.jpg" (ADD)
- beard-trim.md: "06-price-board-cash-only.jpg" (ADD)
- line-up.md: "06-price-board-cash-only.jpg" (ADD)
- hot-towel-shave.md: "04-heritage-chair.jpg" (ADD)

Markdown body (replaces the "Stub content — Phase 3/4 replaces" line):
```markdown
## What {service} is at Joe's Barbershop

{Skill-generated H2 answer capsule (130-160 words). Declarative entity-first prose. Answers what the service is and how Joe does it. Naturally references the price ($X) and duration where relevant. Inline link to one related service where prose flow allows (per D-08 inline mesh — e.g., "[fades](/fades) pair well with a beard line-up").}

## What's different at Joe's

{Skill-generated H2 answer capsule (130-160 words). Surfaces Joe's differentiator for THIS service: $30 base, cash only, walk-in friendly, traditional craft, family-friendly. Inline link to one neighborhood page where natural (e.g., "Customers from [Bostonia](/bostonia-barber) and El Cajon walk in...").}
```

**Constraints (per CONTEXT D-16, D-17 + inputs/02-aeo-constraints.md):**
- DO NOT modify title/price/duration values (those are canonical from Phase 2 D-21).
- DO NOT use banned phrases: "we're more than a barbershop", "experience the difference", "click here", "innovate", "streamline", "we might", "could potentially".
- DO answer each page's primary query in the BLUF first sentence.
- DO use real entity names (Joe's Barbershop, Bostonia, El Cajon, $30, Tue–Sat) — no generic "barbershop in your area" phrasing.
- IF skill output is obviously wrong (factually incorrect, off-brand, hallucinated): flag in the SUMMARY and ask user; do NOT commit broken output (per D-16).
- Commits are per-page atomic — one git commit per service file write per Phase 3 D-05 pattern.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/dtconsulting/joesbarbershop &amp;&amp; for f in site/src/content/services/*.md; do echo "=== $f ==="; if grep -q "Stub content" "$f"; then echo "FAIL: stub remains"; fi; bluf_words=$(grep "^bluf:" "$f" | wc -w); if [ "$bluf_words" -lt 25 ]; then echo "FAIL: $f bluf has only $bluf_words words (likely Phase 2 stub one-liner; threshold ≥25 — see D-07/D-14)"; fi; faq_count=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag &amp;&amp; /^  - q:/{n++} END{print n+0}' "$f"); echo "faqs: $faq_count"; if [ "$faq_count" -lt 3 ]; then echo "FAIL: $f has fewer than 3 FAQs"; fi; done</automated>
  </verify>
  <acceptance_criteria>
    - All 6 service files exist: `for f in fades classic-cut kids-cuts beard-trim line-up hot-towel-shave; do test -f "site/src/content/services/${f}.md"; done`
    - Zero stub markers remain: `! grep -l "Stub content — Phase 3/4 replaces" site/src/content/services/*.md`
    - Each file has a populated bluf above the Phase 2 stub length (≥25 words; Phase 2 one-liner stubs are ~14 words. Word-count is more reliable than pattern-matching since polished AEO copy may legitimately contain entity-first phrasings like "Joe's Barbershop offers..."): `for f in site/src/content/services/*.md; do n=$(grep "^bluf:" "$f" | wc -w); test "$n" -ge 25 || echo "FAIL: $f bluf only $n words"; done` → no FAIL output
    - Each file has at least 3 FAQs (D-07 minimum even for shorter services): `for f in site/src/content/services/*.md; do n=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag && /^  - q:/{n++} END{print n+0}' "$f"); test "$n" -ge 3 || echo "FAIL: $f has $n FAQs"; done` → no FAIL output
    - Each file has a heroPhoto field: `for f in site/src/content/services/*.md; do grep -q "^heroPhoto:" "$f" || echo "FAIL: $f missing heroPhoto"; done` → no FAIL output
    - Each heroPhoto value references a real photo in src/assets/photos/: `for f in site/src/content/services/*.md; do photo=$(grep "^heroPhoto:" "$f" | sed 's/.*"\(.*\)".*/\1/'); test -f "site/src/assets/photos/${photo}" || echo "FAIL: $f references missing photo $photo"; done` → no FAIL output
    - Each markdown body has H2 sections: `for f in site/src/content/services/*.md; do count=$(grep -c "^## " "$f"); test "$count" -ge 2 || echo "FAIL: $f has $count H2 sections"; done` → no FAIL output
    - No banned anti-pattern phrases in markdown body: `! grep -irE "(we're more than a barbershop|experience the difference|click here|innovate|streamline)" site/src/content/services/*.md`
    - Build still succeeds: `cd site && npm run build` exits 0
    - All 6 service pages still build: `for slug in fades classic-cut kids-cuts beard-trim line-up hot-towel-shave; do test -f "site/dist/${slug}/index.html"; done`
  </acceptance_criteria>
  <done>
    All 6 service collection files have populated bluf, faqs (≥3 each), markdown body with ≥2 H2 sections, and heroPhoto field. Zero stub markers. Built pages render the populated content.
  </done>
</task>

<task type="auto">
  <name>Task 2: Polish pass via marketing-skills:copywriting on /fades and /kids-cuts</name>
  <files>
    site/src/content/services/fades.md,
    site/src/content/services/kids-cuts.md
  </files>
  <read_first>
    - site/src/content/services/fades.md (post-Task-1 baseline output to polish)
    - site/src/content/services/kids-cuts.md (post-Task-1 baseline output to polish)
    - .agents/product-marketing-context.md (positioning + audience tone — same input as Task 1)
    - .agents/aeo-frame.md (AEO frame — same input as Task 1)
    - ~/Documents/DT Vault/1-projects/dt-consulting-llc/joes-barbershop-sandbox.md (audit baseline + strategic angles — load-bearing for the polish, especially the kids-cuts family-walk-in differentiator)
    - .planning/phases/03-unique-pages/03-12-PLAN.md (niche-landing copy refresh — voice / strategic-angle reference for the polish quality bar)
  </read_first>
  <action>
**Step 1: Invoke `marketing-skills:copywriting` per page on the two strategic services** per CONTEXT D-14:

**For /fades** (highest service search volume; broadest AI-citation surface):
- Skill input: post-Task-1 baseline `fades.md` content + `.agents/product-marketing-context.md` + `.agents/aeo-frame.md` + primary query "fade haircut El Cajon / East County".
- Strategic angle to sharpen: `/fades` is the highest-volume service query in East County; the polished copy should make this page the canonical answer for "where to get a fade in El Cajon" with explicit fade-style coverage (skin fade, low fade, mid fade, high fade) and price clarity.
- Tighten BLUF to lead with the answer in the first sentence (entity + service + price + neighborhood). Sharpen FAQ wording. Strengthen the "What's different at Joe's" section with the $30-cash-only-walk-in differentiators.

**For /kids-cuts** (family-friendly differentiator; ties to Joe's strategic angle):
- Skill input: post-Task-1 baseline `kids-cuts.md` + same context files + primary query "kids haircut Bostonia / East County family barbershop".
- Strategic angle to sharpen: family-walk-ins is a real differentiator in the East County market (Latino + Anglo working-class, family-friendly positioning per `joes-barbershop-sandbox.md`). The polished copy makes this the canonical answer for "family barbershop near me / kids haircut Bostonia" — kids sit alongside parents, no separate kids-only setup, no awkward handoff, same walk-in standard.
- Tighten BLUF to lead with "family-friendly walk-in barbershop" framing. Sharpen FAQs around kid-specific concerns: minimum age, parent-can-stay, what happens if the kid won't sit still, family-walk-in.

**Step 2: Replace existing fades.md and kids-cuts.md content with polished versions.** Preserve title/price/duration/heroPhoto frontmatter from Task 1. Replace bluf + faqs + markdown body with polished versions.

**Step 3: Verify polish didn't break schema or anti-patterns.**
- Frontmatter still valid (title/price/duration/heroPhoto/bluf/faqs all present).
- No banned phrases.
- BLUF answers the primary query in the first sentence.
- FAQ count still ≥3 (probably 5-6 after polish).

**Constraints:**
- DO NOT touch the other 4 service files (classic-cut, beard-trim, line-up, hot-towel-shave) in this task — they're complete from Task 1.
- DO NOT change title/price/duration/heroPhoto values.
- DO commit each polished file atomically (per-page commit).
- IF polished output drifts from Phase 3 voice (per D-17), fix the global frame `.agents/aeo-frame.md` rather than per-page tweaking.
  </action>
  <verify>
    <automated>cd /Users/darrelltang/dtconsulting/joesbarbershop &amp;&amp; for f in site/src/content/services/fades.md site/src/content/services/kids-cuts.md; do echo "=== $f ==="; faq_count=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag &amp;&amp; /^  - q:/{n++} END{print n+0}' "$f"); echo "faqs: $faq_count"; bluf_len=$(awk '/^bluf:/{print length($0); exit}' "$f"); echo "bluf chars: $bluf_len"; word_count=$(grep "^bluf:" "$f" | wc -w); echo "bluf words approx: $word_count"; done &amp;&amp; cd site &amp;&amp; npm run build 2&gt;&amp;1 | tail -10 &amp;&amp; test -f dist/fades/index.html &amp;&amp; test -f dist/kids-cuts/index.html &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - fades.md bluf is substantially longer than the Phase 2 one-liner: `bluf_words=$(grep "^bluf:" site/src/content/services/fades.md | wc -w); test "$bluf_words" -ge 30 || echo "FAIL: fades bluf only $bluf_words words"`
    - kids-cuts.md bluf is substantially longer than the Phase 2 one-liner: `bluf_words=$(grep "^bluf:" site/src/content/services/kids-cuts.md | wc -w); test "$bluf_words" -ge 30 || echo "FAIL: kids-cuts bluf only $bluf_words words"`
    - fades.md has at least 4 FAQs (popular service per D-07): `n=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag && /^  - q:/{n++} END{print n+0}' site/src/content/services/fades.md); test "$n" -ge 4 || echo "FAIL: fades has $n FAQs"`
    - kids-cuts.md has at least 4 FAQs: `n=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag && /^  - q:/{n++} END{print n+0}' site/src/content/services/kids-cuts.md); test "$n" -ge 4 || echo "FAIL: kids-cuts has $n FAQs"`
    - No banned phrases in either file: `! grep -irE "(we're more than a barbershop|experience the difference|click here|innovate|streamline)" site/src/content/services/fades.md site/src/content/services/kids-cuts.md`
    - kids-cuts.md mentions family walk-in differentiator: `grep -iE "(family|kids? sit|walk[- ]?in)" site/src/content/services/kids-cuts.md | head -5`
    - fades.md mentions fade-style coverage: `grep -iE "(skin fade|low fade|high fade|mid fade)" site/src/content/services/fades.md | head -3`
    - Build succeeds and both polished pages still render: `cd site && npm run build && test -f dist/fades/index.html && test -f dist/kids-cuts/index.html`
    - Other 4 service files unchanged from Task 1: `for f in classic-cut beard-trim line-up hot-towel-shave; do n=$(awk '/^faqs:/{flag=1; next} /^[a-z_]+:/{flag=0} flag && /^  - q:/{n++} END{print n+0}' "site/src/content/services/${f}.md"); test "$n" -ge 3 || echo "FAIL: $f got regressed"; done`
  </acceptance_criteria>
  <done>
    /fades and /kids-cuts have polished BLUF (≥30 words each, leading with the primary-query answer), ≥4 FAQs each (popular services per D-07), and strategic-angle differentiators woven into the copy (fade-style coverage on /fades; family-walk-in on /kids-cuts). The other 4 services retain Task 1 baseline content unchanged.
  </done>
</task>

</tasks>

<verification>
- All 6 service collection files free of stub markers: `! grep -l "Stub content" site/src/content/services/*.md`
- All 6 files have heroPhoto, populated bluf, ≥3 faqs, and ≥2 H2 sections in markdown body
- /fades and /kids-cuts have ≥4 FAQs and ≥30-word BLUF (polish-pass quality bar)
- `cd site && npm run build` exits 0; all 6 dist/{slug}/index.html files exist
- Each built service page renders its populated BLUF and FAQs through the Plan 1 template
</verification>

<success_criteria>
- ROADMAP Phase 4 success criterion #1 achieved for content side: each service slug returns 200 AND displays the correct price, BLUF, and FAQs from its content file
- Voice consistency with Phase 3 niche-landing maintained (per CONTEXT D-17)
- /fades and /kids-cuts polish pass differentiates them as canonical answer surfaces for their respective queries
</success_criteria>

<output>
After completion, create `.planning/phases/04-templated-pages/04-03-SUMMARY.md` summarizing:
- Skill invocations made (programmatic-seo bulk + copywriting × 2)
- Per-file: BLUF word count, FAQ count, body H2 count, heroPhoto value
- Any user-flagged outputs (skill produced wrong info → caught and not committed)
- Verification output: stub-marker grep result, build result, dist files present
</output>
