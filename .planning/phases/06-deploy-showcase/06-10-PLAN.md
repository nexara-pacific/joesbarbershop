---
phase: 06-deploy-showcase
plan: 10
type: execute
wave: 4
depends_on: [09]
files_modified:
  - .planning/phases/06-deploy-showcase/joe-approval/
autonomous: false
requirements: [SHOW-01]
must_haves:
  truths:
    - "Darrell texts Joe the preview URL + SHARE-CHECKLIST.md content per D-10"
    - "Joe's response is captured to .planning/phases/06-deploy-showcase/joe-approval/<YYYY-MM-DD>-joe-signoff.{txt,png} per D-12"
    - "If Joe approves: SHOW-01 is satisfied, Wave 5 (go-live flip) is unblocked"
    - "If Joe requests changes: captured as <YYYY-MM-DD>-joe-changes.txt, becomes input to a Wave 4.5 gap-closure cycle (NOT this plan's concern beyond capturing)"
  artifacts:
    - path: ".planning/phases/06-deploy-showcase/joe-approval/<YYYY-MM-DD>-joe-signoff.txt OR .png"
      provides: "SHOW-01 evidence: Joe's approval response, captured"
      contains: "Joe's response text or screenshot"
  key_links:
    - from: "SHARE-CHECKLIST.md + preview URL"
      to: "iMessage to Joe (619)"
      via: "Darrell's phone, text channel per repeated prior pattern"
      pattern: "joe-signoff"
---

<objective>
Send the showcase to Joe via text and capture his response as the SHOW-01 gating artifact per D-10 + D-12. This is the human-action checkpoint that converts v1 from "built" to "validated."

Per CONTEXT additional_context: "Joe-share is text-message-first." The cheat-sheet content goes into the text body (or as a paste/screenshot), preview URL is inline.

Per D-12: sign-off capture is a screenshot/copy of Joe's text response saved into `.planning/phases/06-deploy-showcase/joe-approval/`.

This is the gating event for the entire phase. Wave 5 (go-live flip) only fires after Joe approves. If Joe requests changes instead, that's also captured (as `<date>-joe-changes.txt`) but triggers a gap-closure cycle outside this plan's scope.

Output: Joe-signoff artifact committed; one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-deploy-showcase/06-CONTEXT.md
@.planning/phases/06-deploy-showcase/06-PATTERNS.md
@.planning/phases/06-deploy-showcase/SHARE-CHECKLIST.md
@.planning/phases/06-deploy-showcase/deployed-preview-url.txt
@.planning/phases/06-deploy-showcase/joe-approval/README.md

<interfaces>
<!-- Inputs to the text -->

Preview URL: read from .planning/phases/06-deploy-showcase/deployed-preview-url.txt
Cheat-sheet body: .planning/phases/06-deploy-showcase/SHARE-CHECKLIST.md
Joe's phone: known to Darrell; not in repo (no PII exposure in planning artifacts)

Capture target dir: .planning/phases/06-deploy-showcase/joe-approval/
Filename format per D-12: <YYYY-MM-DD>-joe-signoff.txt OR .png
Alternate (if Joe requests changes): <YYYY-MM-DD>-joe-changes.txt
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Text Joe + capture his response (SHOW-01)</name>
  <read_first>
    - .planning/phases/06-deploy-showcase/SHARE-CHECKLIST.md (the cheat-sheet content to send)
    - .planning/phases/06-deploy-showcase/deployed-preview-url.txt (the preview URL to include)
    - .planning/phases/06-deploy-showcase/joe-approval/README.md (filename format + capture guidance)
    - .planning/phases/06-deploy-showcase/06-CONTEXT.md (D-10/D-12)
  </read_first>
  <what-built>
    Plan 06-09 authored SHARE-CHECKLIST.md (Joe-voice, 6 sections, locked values, preview URL inline) and scaffolded joe-approval/ for the response artifact. Plan 06-07 deployed the showcase preview and verified all 17 routes 200 with noindex active. Plan 06-08 confirmed Google Rich Results pass on the deployed HTML. The site is ready for Joe.
  </what-built>
  <how-to-verify>
    **Step 1 — Compose the text body.** Two delivery options:

    **Option A (preferred for text channel) — paste the cheat-sheet content directly into iMessage.** Copy the rendered contents of `SHARE-CHECKLIST.md` (skip the `# Joe's Review — Quick Cheat-Sheet` markdown header — start with the "Hey Joe" intro line). The 6 section headings (`## 1. Does it feel like your shop?`, etc.) render in iMessage as plain text with the `##` literal; this is fine — the format reads as a numbered list, which is exactly the cheat-sheet's intent.

    **Option B (alternate) — link to the markdown file.** If iMessage truncates the message awkwardly, host the cheat-sheet on a Vercel preview path (e.g., copy SHARE-CHECKLIST.md into `site/src/pages/share.md` or paste as a Gist) and text Joe the preview URL + a "here's a quick list of things to scan: <gist-url>". Adds a click — only use if Option A bullets render badly.

    Recommendation: Option A. iMessage handles ~5KB plain text without truncation; the cheat-sheet is well under that.

    **Step 2 — Send the text.** From Darrell's phone (or iMessage on macOS), text Joe at his known number. Body: the cheat-sheet content. Subject line N/A (iMessage).

    Verify the text actually sent (not stuck in "queued" or red exclamation):
    ```
    On macOS Messages: look for a blue/green delivery checkmark below the message bubble.
    On iPhone: same.
    ```

    **Step 3 — Wait for Joe's response.** Joe operates on his own schedule; this could be minutes, hours, or days. The plan blocks here until he responds. No polling needed — this is a real-world human checkpoint.

    **Step 4 — When Joe responds, classify the response:**

    **Path A — Joe approves.** His response is some flavor of:
    - "looks good"
    - "looks great"
    - "no changes"
    - "yeah this is good, what's next?"
    - any unambiguous yes

    Capture the response to `.planning/phases/06-deploy-showcase/joe-approval/<YYYY-MM-DD>-joe-signoff.{txt,png}`:

    ```bash
    cd /Users/darrelltang/dtconsulting/joesbarbershop
    TODAY=$(date +%Y-%m-%d)

    # Preferred: plain-text transcription of Joe's exact response (grep-able, machine-readable)
    cat > .planning/phases/06-deploy-showcase/joe-approval/${TODAY}-joe-signoff.txt <<'EOF'
    From: Joe Denesowicz (Joe's Barbershop)
    Channel: iMessage
    Date: <YYYY-MM-DD HH:MM PT>

    [exact text of Joe's response goes here, verbatim]
    EOF

    # OR additionally: screenshot the iMessage thread (Cmd+Shift+4 on macOS Messages)
    # Save to .planning/phases/06-deploy-showcase/joe-approval/${TODAY}-joe-signoff.png

    # Verify capture
    ls -la .planning/phases/06-deploy-showcase/joe-approval/${TODAY}-joe-signoff.*
    ```

    Commit subject: `docs(06-10): capture Joe's sign-off — SHOW-01 satisfied (D-12)`.

    **Path B — Joe requests changes.** His response identifies things to fix:
    - "the prices are wrong, kids' is actually $20"
    - "FAQ #3 sounds weird"
    - "can you swap the storefront photo"

    Capture as `<YYYY-MM-DD>-joe-changes.txt` instead of `-joe-signoff.txt`:

    ```bash
    cd /Users/darrelltang/dtconsulting/joesbarbershop
    TODAY=$(date +%Y-%m-%d)
    cat > .planning/phases/06-deploy-showcase/joe-approval/${TODAY}-joe-changes.txt <<'EOF'
    From: Joe Denesowicz (Joe's Barbershop)
    Channel: iMessage
    Date: <YYYY-MM-DD HH:MM PT>

    [verbatim change list from Joe]

    Disposition: Gap-closure cycle required before Wave 5 go-live.
    EOF
    ```

    Commit subject: `docs(06-10): capture Joe's change requests — gap-closure required (D-12)`.

    **Then halt Wave 5.** The orchestrator should run a gap-closure cycle on Joe's change list (Plan 06-11 onwards, scoped per Joe's list) and re-cycle through Wave 4 (re-text Joe with the updated preview) before Wave 5 can fire.

    **Path C — Joe ghosts / non-responsive for >7 days.** Capture this too:
    ```bash
    cd /Users/darrelltang/dtconsulting/joesbarbershop
    TODAY=$(date +%Y-%m-%d)
    cat > .planning/phases/06-deploy-showcase/joe-approval/${TODAY}-joe-ghosted.txt <<'EOF'
    From: Joe Denesowicz (Joe's Barbershop)
    Channel: iMessage
    Date sent: <YYYY-MM-DD>
    Days waited: 7+

    No response yet. Options:
      1. Send a gentle follow-up text in another 3-7 days
      2. Visit shop in person to walk Joe through (PROJECT.md hints at this as fallback)
      3. Continue waiting

    Plan 06-10 currently blocked on Joe's response. Wave 5 (go-live) cannot fire without SHOW-01 sign-off per the PROJECT.md constraint: "No live deployment before Joe approves the showcase."
    EOF
    ```

    Commit subject: `docs(06-10): Joe non-responsive — Wave 5 still blocked on SHOW-01 (D-12)`.

    **Step 5 — Commit whichever artifact was captured.** Files in commit: just the new file in `joe-approval/`.

    The plan completes either Path A (approval — proceed to Wave 5) or Path B/C (blocked — phase pauses).
  </how-to-verify>
  <acceptance_criteria>
    - Darrell sent the text to Joe (verified by Messages delivery confirmation; this is implicit — not auto-verifiable from the repo)
    - A new file exists in `.planning/phases/06-deploy-showcase/joe-approval/` with filename matching one of: `<YYYY-MM-DD>-joe-signoff.txt`, `<YYYY-MM-DD>-joe-signoff.png`, `<YYYY-MM-DD>-joe-changes.txt`, or `<YYYY-MM-DD>-joe-ghosted.txt`
    - The file contains either Joe's verbatim response (text format) OR a screenshot of the iMessage thread (PNG format)
    - For Path A (sign-off): file is `-joe-signoff.{txt,png}`, content shows unambiguous approval
    - For Path B (changes): file is `-joe-changes.txt`, content shows a list of requested changes
    - For Path C (ghost): file is `-joe-ghosted.txt`, content documents the wait state
    - One atomic commit landed referencing the captured file with a `docs(06-10): ...` subject
    - If Path A: SHOW-01 requirement is satisfied; orchestrator can fire Wave 5 (Plan 06-11)
    - If Path B/C: Phase 6 pauses pending gap-closure or follow-up; Wave 5 does NOT fire
  </acceptance_criteria>
  <resume-signal>
    Reply with one of:
      - "joe-approved" + the captured filename — proceed to Wave 5 (Plan 06-11 go-live flip)
      - "joe-changes-requested" + the captured filename — pause Phase 6, request a gap-closure subcycle
      - "joe-ghosted" + the captured filename — Phase 6 stays paused; await user's follow-up plan
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Darrell's iMessage -> Joe's phone | Private channel; both parties consenting; no PII exposure beyond what's already in business.json (phone is publicly listed). |
| Joe's response -> joe-approval/ artifact -> git history | Tamper-evident via git commit history; one-way capture (Joe doesn't have repo access). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-26 | Repudiation | Joe later disputes that he approved | mitigate | iMessage screenshot OR verbatim transcription committed to git with timestamped commit. Sandbox-project trust model is sufficient; not a contractual dispute scenario. |
| T-06-27 | Tampering | Sign-off artifact fabricated to skip Wave 5 gate | accept | Self-attack scenario in a solo-developer project. The PROJECT.md constraint "No live deployment before Joe approves" is a self-imposed quality gate; bypassing it would only hurt Darrell. No third-party-attacker incentive. |
| T-06-28 | Information Disclosure | Joe's text contents committed to public repo | accept | Repository is private (Darrell's personal/consulting GitHub per CLAUDE.md). Joe's iMessage to Darrell has no commercial sensitivity; both parties consent to the capture as documented in joe-approval/README.md. |
| T-06-29 | Spoofing | A non-Joe sends an "approval" text from a spoofed number | accept | Solo sandbox project; no commercial trigger that would motivate spoofing. iMessage shows Joe's contact name from Darrell's address book — easy human-verify. |
</threat_model>

<verification>
After the task (Path A — sign-off path):

```bash
cd /Users/darrelltang/dtconsulting/joesbarbershop

# Some signoff artifact landed
ls .planning/phases/06-deploy-showcase/joe-approval/
SIGNOFF=$(ls .planning/phases/06-deploy-showcase/joe-approval/*joe-signoff* 2>/dev/null | head -1)
if [ -n "$SIGNOFF" ]; then
  echo "SHOW-01 satisfied: $SIGNOFF"
  test -f "$SIGNOFF"
fi

# Or changes/ghost path
CHANGES=$(ls .planning/phases/06-deploy-showcase/joe-approval/*joe-changes* 2>/dev/null | head -1)
GHOSTED=$(ls .planning/phases/06-deploy-showcase/joe-approval/*joe-ghosted* 2>/dev/null | head -1)
if [ -n "$CHANGES" ]; then echo "Wave 5 BLOCKED — changes requested: $CHANGES"; fi
if [ -n "$GHOSTED" ]; then echo "Wave 5 BLOCKED — Joe non-responsive: $GHOSTED"; fi

# Commit landed
git log --oneline -1 | grep -q '06-10'
```
</verification>

<success_criteria>
- Darrell sent the SHARE-CHECKLIST content + preview URL to Joe via text
- Joe's response (or non-response) captured as an artifact in joe-approval/
- Filename follows D-12 format
- One atomic commit landed
- For Path A: SHOW-01 satisfied; Wave 5 unblocked
- For Path B/C: phase pauses; orchestrator picks up next steps
</success_criteria>

<output>
After completion, create `.planning/phases/06-deploy-showcase/06-10-SUMMARY.md`. Note in the summary which path (A/B/C) was taken and whether Wave 5 is unblocked.
</output>
